# Repo Design Patterns — proven cinematic building blocks

Distilled from the Asset-module redesigns (Inventory, Allocations, Returns, Transfers, History, Damage, Maintenance, Audits, Disposal, Categories, Vendors). Reuse these so new work is consistent and **never repeats a sibling's signature**.

## 1. The Console hero (every section gets one)
A `<Motion as="section" class="*c">` with, in order:
- `.as-grain` + an aura (`var(--as-grad-hero)`) + `.as-blueprint-floor` + a slow-rotating ambient motif (a big faint lucide icon or SVG, ~80s spin).
- **Lead:** pill eyebrow (`<Icon/> SECTION · Control`), `clamp()` title with a gradient-clip accent word, sub, CTA row (`as-btn` primary/steel/ghost with `motion-v` `:whileHover="{y:-2,scale:1.02}" :whileTap="{scale:0.97}"`).
- **Telemetry lenses:** a `repeat(5, ...)` grid of clickable stat chips; each = icon tile + `AssetCountUp` value + label + bottom accent bar; `.on` reflects the active filter; status lenses emit `pick`, info lenses are `.stat` (no cursor).
- **A signature instrument** (the unique part — see §4).
- Emits `pick` / `new` / `go` / `focus`.

## 2. Cinematic entity card
Outer `.x-shell` runs the entrance (CSS `as-deal` with `animation-delay: calc(var(--i)*0.045s)`), inner `.x` holds the tilt so they never conflict:
```
usePointerSpotlight(cardEl)            // sets --mx/--my/--spot
.x:hover { transform: perspective(1100px)
  rotateX(calc((var(--my,.5) - .5) * -6deg))
  rotateY(calc((var(--mx,.5) - .5) *  8deg)) translateY(-2px); }
.x-glare { opacity: var(--spot,0); background: radial-gradient(...at calc(var(--mx)*100%) calc(var(--my)*100%)...); }
```
Plus: a status-colored left **spine**, a conic/gauge **ring** or meter, an inline **stepper** for lifecycle state, status `AsStamp`, action buttons (`motion-v` hover/tap), and any "live" pulse only while the row is active.

## 3. Modal recipe (`AssetModal` shell)
- Body opens with a **live preview** card that fills as fields complete (DRAFT→READY stamp, blueprint grid, the section's motif).
- Fields animate in with staggered `motion-v` (`fT(i) = {duration:.4, delay:.05+i*.06, ease:[.16,1,.3,1]}`).
- **Dropdowns** = `AsSelect` (teleported, animated, supports `{value,label,icon?,dot?}`); prepend a `{value:'', label:'(none/All …)'}` to keep it resettable.
- **Enum pickers** = segmented icon buttons (not a plain select) when ≤6 options.
- **Dates** = `HrDatePicker` (the onboarding add-asset calendar — z-5000 teleport, month-slide). The user repeatedly asks for "the onboarding assets purchase-date calendar" → that is `@/components/hr/forms/HrDatePicker.vue`.
- Confirm button shows a `Loader` spinner while saving; disable on invalid.
- Destructive/decision actions get their own confirm modal (tone-morph + reason note).

## 4. Signature-instrument ledger — DO NOT REPEAT
Each asset tab owns one mechanic; pick a fresh one for new pages:
| Tab | Metaphor / instrument |
|---|---|
| Inventory | "Fleet Bay" — pulse strip + manifest, scanline deal |
| Allocations | "Deployment Console" — **radar** sweep + deployment pipeline |
| Returns | "Re-entry Control" — **horizon timeline** w/ overdue zone + NOW gate |
| Transfers | "Transit Relay" — **conveyor** w/ packet-riding stations |
| History | "Chronicle" — **spine timeline** + tape reel |
| Maintenance | "Service Bay" — interlocking **rotating gears** (`MaintGear`) + service line |
| Audits | "Census Deck" — **sweeping scanline over a muster matrix** (`AuditScanGrid`) + recon donut |
| Disposal | "Decommission Foundry" — **molten crucible** gauge (embers, heat-haze) |
| Categories | "Taxonomy Atlas" — **proportional classification spectrum** + fleet-share rings |
| Vendors | "Supply Constellation" — **orbital node map** w/ supply-inflow beams |
| Audit Logs (Assets) | "Forensic Ledger" — **activity-pulse equalizer ribbon** (`LedgerPulseRibbon`, sweeping read-head) + **integrity seal** conic gauge + hash-stamped ledger rows w/ from→to morph + expand diff (distinct from Audits' muster matrix and History's black-box spine) |
| Audit Logs (HR Settings) | "Provenance Deck" — **change-density spectrogram** (`LedgerSpectrogram`: action-lanes × last-14-days heatmap matrix, igniting cells + NOW playhead + playback sweep) + proportional **domain spectrum bar** + action **telemetry lenses** + **spine-timeline** rows (per-action node, before→after diff via grid-rows expand, deep-link to the source settings page) + animated `SetPager` (10/page). Distinct from the Asset Forensic Ledger's pulse ribbon. Backend (`settings_audit.py`) folds `audit_logs` (dept/designation/grade/work-location) + `payroll_audit` (compliance) into the canonical `hr_settings_audit_logs` at read time. |
| Dashboard | "Fleet Command Deck" — **concentric multi-ring Status Reactor** (`DashReactor`, nested status arcs + utilization core, hover-to-inspect) + **semicircular Health gauge** (`DashHealthGauge`, needle + condition zones + weighted score) + animated type-composition bars + module-launchpad deck (keeps the 3D `HangarHero`). Multi-ring reactor ≠ allocations' radar sweep / audits' single recon donut. |
| HR — Command Center (apex `/admin/hr/dashboard`) | "Workforce Mission Control" (2026-06-28) — the apex landing above all 15 HR sub-modules. Signature instrument = **the Workforce Helix** (`WorkforceHelix.vue`): a flowing double-strand **lifecycle DNA** (two phase-shifted SVG sine paths w/ `stroke-dashoffset` flow + weaving rungs + centre rail) carrying **4 lifecycle loci** (Open Roles → Joining → **Active core** → Exiting) sized by sqrt(headcount), with **travelling pulse packets** via `<animateMotion :path>`; loci deep-link to recruitment/onboarding/employees/exit. Composition: Console hero (greeting + gradient accent + CTA row w/ pending badge + live "modules reporting" sync line) over the helix → **6 telemetry lenses** (headcount/present%/pending-approvals/open-roles/monthly-payroll/training-compliance, each w/ real sub-context, NO fake sparklines) → **command deck** (`WorkforceStrata` lifecycle stacked column · `AttendancePulse` present-rate ring gauge + real present/absent/leave/WFH bar · `ActionCenter` cross-module pending-approvals deck sorted by volume) → `InsightRail` (hiring funnel / payroll cost bars / exits-attrition) → **`ModuleLaunchCard` launchpad** (15 cinematic 3D-tilt cards, one per sub-module, live mini-stat + deep-link — the connectivity centrepiece). **Data = fan-out:** the page hits ~13 module `*/stats` & `*/dashboard` endpoints in parallel via `Promise.allSettled` (one failure ≠ broken page); `/hr/dashboard-stats` stubs `pending_leave_approvals`/`open_positions`/`todays_attendance_pct` to 0, so real values come from the per-module endpoints. Reusable bits added: `HrCountUp.vue` (hr-mono count-up) + global `.hr-card`/`.hr-grain`/`.hr-blueprint` in `hr-theme.css`. **Helix gotcha (fixed):** loci needing `translate(-50%,-50%)` centering must NOT also be `<Motion>` (motion-v writes its own transform → fight); split into a static positioning wrapper (`.whx-lo`) + inner `<Motion>` button (`.whx-lo-btn`). Distinct from every sibling — the only **lifecycle-DNA-helix** motif (≠ asset reactor/health-gauge, performance orrery, settings spectrogram/equalizer, exit flowing-current/comet-grid, shifts control-tower). |
| Travel — admin Approvals | "Approach Control / Control Tower" — **top-down radar dish** (`TowerScope`, sweeping conic + plotted blips by chain-depth) + `InboundStrip` cards (mine vs in-the-chain squadrons). |
| Exit — Asset Return | "Recovery Bay — Magnetic Reclamation Field" (stage rebuilt 2026-06-24; user rejected v1's empty dot-floor + sweeping `bay-scan` band as "repetitive… shitty", esp. with few units) — `AssetRecoveryBay.vue`'s stage is now a full-bleed **magnetic field** that reads rich at any count: a **hex containment grid** + **drifting ambient motes** (data-independent, so a 1-unit bay never looks empty) + faint **containment coils** breathing around the vault + **7 curved magnetic flux lines** flowing energy (`stroke-dashoffset`, blurred, `screen`) that **converge into the vault** (turn emerald when sealed). Held units pulse a **recall ring**, in-transit units glide with a tractor trail; recovered units live as **emerald docking ports** around an upgraded **vault door** — a notched **lock-ring** (`repeating-conic-gradient` + ring mask) that slow-spins then **locks (animation paused) + emits a seal pulse** when `unreturned===0`, around the conic recovery gauge (`@property --ex-p` on the gauge element). NO scan band (it was the "shitty" bit). Flux SVG is `viewBox 0 0 1000 300` + `slice` (no `preserveAspectRatio=none` stroke distortion); pointer-parallax on the flux layer. Per-asset **manifest cards** (`AssetReturnCard.vue`) carry a **Held→Requested→In-transit→Recovered** stepper (type-mapped lucide icon, status spine, 3D tilt). Master-detail: `AssetRecoveryConsole` (lenses active/recovered/in-transit/at-risk/units-out/fleet + a **fleet recovery ribbon** stacked bar) → `AssetCaseChip` roster → stage. Signature **at-risk bridge**: unreturned units as LWD nears surface "charged as recoveries in F&F" → cross-links to **Clearance + Settlement** (the previously-invisible recovery→F&F connection). Parallel `Promise.allSettled(fetchExitAssets)` over the queue (closed the bare-dropdown loophole). Page is correctly a **monitor + flag** surface — return *processing* stays in the Asset module (`/admin/hr/assets/returns` deep-links), only `flag-returns` writes. No backend change. Distinct from the Asset module's own re-entry-horizon/transit-relay and from clearance's authorization-lattice — this is the only **magnetic-flux + vault-door** motif. |
| Exit — Clearance | "The Gatehouse — Authorization Lattice" (concourse rebuilt 2026-06-24; user rejected the v1 portcullis-panel concourse as "so so shitty") — `ClearanceGatehouse.vue`'s scene is now a futuristic **department authorization lattice**: each department is a **conic-ring authorization node** (`--ex-p` arc = its sign-off %, dept icon in the hole) that lights by state — **cleared** = emerald glow + check seal (icon fades out), **partial** = amber ring + box-shadow pulse, **blocked** = red lock + periodic shudder, **idle** = dim; each node drops a **conduit** to a horizontal **charging energy spine** (fill = overall %, flowing dash `background-position` energy; cleared/partial conduits flow, blocked conduits go red-severed); a **record token** (employee medallion, conic ring) rides the spine by progress to a **threshold reactor core** (conic charge orb + breathing face + emit-rings) that flips emerald/"OPEN" with a burst when all-mandatory clear. HUD count-ups + a **recoveries reservoir** (flowing pipe → settlement). **`--ex-p` gotcha (hit again here):** it's `@property … inherits:false`, so the node/token/core conic rings each set `--ex-p` on the gradient-owning element itself (`.nd-disc`/`.tok-ring`/`.core-orb`), NOT a parent — a parent-set `--ex-p` reads as the 0deg initial on the child. No repetitive sweep-band (the v1 `gh-sweep` was part of the "shitty" feel); motion is node pulses + conduit/spine energy flow + token glide + core breathe. Master-detail section: `ClearanceConsole` (lenses: in-clearance/blocked/not-started/near/ready/recoveries→F&F + a **dept gate-keepers aggregate ribbon**) → `ClearanceCaseChip` roster strip → stage (context header + gatehouse + `ClearanceLaneCard` tilt board). Modals: `ClearanceSignoffModal` (live **morphing stamp seal** preview + 5-segment status + **assignee** ExSelect + recovery + remarks) & `ClearanceReopenModal` (required reason → audited `reopen`). Distinct from dashboard's single archway (one gate) and resignation's dusk-horizon motes — this is a CONCOURSE of mechanical portcullis gates. Loopholes closed: assignee field (backend-supported, was hidden), audited reopen (was a silent status flip), org-wide overview + blocked/recovery telemetry (was a bare dropdown; now parallel `Promise.allSettled` clearance load), cross-links to notice/asset-return/settlement. **Sign-off redesign (2026-06-23):** `ClearanceSignoffModal` rebuilt as a 3-step **Authority Console** (Verify → Disposition → Authorize + ceremonial seal-press) driven by a per-`item_key` **playbook** (`clearanceItemPlaybook` in `useExit.js`) so *every* obligation signs off uniquely — component icon + an animated **`ClearanceMotif.vue`** signature instrument (`access-revoke`/`erp-revoke`/`device`/`ledger`/`badge`/`biometric`/`handover`/`seal`/`generic`, each a distinct SVG scene reacting to status tone), a brief, a system-effect notice, a verification **checklist** (gates the CLEARED disposition → closes the careless-one-click-clear loophole), remark presets, recovery relevance + hint, and a live no-dues **certificate** card. BLOCKED requires an audited reason (parsed back out of `remarks` on re-edit, like `[Reopened]`). Backend: added IT `it_erp_login` ("ERP login / credentials revoked") to `DEFAULT_CLEARANCE_TEMPLATE` + idempotent **`backfill_clearance_items`** self-heal in `get_clearance` (surfaces template items added after a case was seeded) — plus a one-off migration of stale policy templates + open cases (the user's case pointed at a policy whose 16-item template predated the new row, so DEFAULT alone didn't reach it). **Cross-module auto-sync (2026-06-23):** `get_clearance` now calls `sync_clearance_from_systems(db, case)` — reads authoritative sibling modules and (a) attaches a live `system_signal` to each derivable item (`{source,state,auto,label,detail}`; rendered as a status chip on the lane card + a banner in the modal's Verify step) and (b) AUTO-CLEARS provably-done items with `[Auto] …` provenance: `it_laptop_return` (AssetAllocation: 0 outstanding & 0 lost/damaged), `it_erp_login` (User.is_active False), `fin_reimbursement` (0 in-flight claims), `hr_exit_interview` (interview COMPLETED). Signal-only (no auto-clear): `fin_loan_advance` (travel advances), and laptop-with-shortfall. Guards: only untouched PENDING items, never overrides BLOCKED/NA, skips items carrying `[Reopened]`, idempotent. `hr_ff_ack` deliberately stays manual (F&F approval needs clearance done first → would be circular). **ERP revoke action:** `POST /clearance-items/{id}/revoke-erp` actually de-provisions (reuses Account Provisioning semantics: `user.is_active=False`+`is_activated=False`, flips ERP `AccountProvisioning` rows to REVOKED) then signs off the gate — surfaced in the sign-off modal as a red "Revoke ERP login now" → inline-confirm → "Revoke & clear" (corporate IT-offboarding: the action does the work, not a checkbox). **HR-gate apply actions (2026-06-24):** the two HR gates `hr_records` + `hr_ff_ack` were the last cosmetic-checkbox gates — now each marked task WRITES to the real record. Backend `POST /clearance-items/{id}/apply-hr-records` (HRIS/docs/statutory → `EmployeeHistory` rows; lifecycle task runs the REAL exit via `_sync_employee_lifecycle` — note that handler commits internally, so flush the item first; idempotent via `item.submission.applied_keys`) + `apply-ff-ack` (stamps NEW `ExitSettlement.ff_*` cols, requires a drafted settlement). Both return `ClearanceApplyResponse{item,effects[]}` → `announceEffects()` toasts what changed. New **`ClearanceApplyConsole.vue`** (cinematic "Live records console": record-tiles that seal + per-task task→record cards with a commit cascade `:committing="sealing"`) renders in the modal's Verify step for apply gates; playbook gained `apply:{kind,verb,tasks[]}` aligned by index with `steps`. Modal backdrop upgraded blender-level (drifting `exm-mesh` + masked grid + seal-burst shockwave + stepper-beam shimmer). Pattern = **"the marked checklist task does the work + reports back"**; reuse it for any future gate that should mutate a real record on seal rather than just flip a status. |
| Exit — Interviews | "The Resonance Chamber" (2026-06-24; user rejected v1's linear equalizer-with-sweeping-scanline as "repetitive… shitty") — `InterviewResonance.vue` rebuilt into a **radial voiceprint**: a pulsing **eNPS core orb** (tone-colored emerald/amber/red via `--tone`) ringed by an **organic polar equalizer** (44 bars rotated around center, scaleY-pulsing with **seeded per-bar phase so there's NO marching band**; real completed-interview overall-scores drive bar height + sentiment color, the rest idle), **expanding sound-ripples**, a spinning dashed reticle, faint concentric scaffold; the core reads `±eNPS` + a det/pas/pro spectrum. Right half = a **live dimension-channel mixer** (Overall/Mgmt/Culture/Growth/Comp horizontal meters, gradient fill by score + traveling `ex-sheen-pass` shimmer + tick grid, width draw-on gated on `useInView`). Floating glass HUD (candour / would-recommend / voices-heard). Fills the whole card via a `286px + 1fr` grid; continuous + reduced-motion guarded. Distinct from siblings — the only **radial-voiceprint + polar-equalizer** motif (the linear scanline is gone). |
| Exit — Resignation | "Departure Vectors — Holographic Horizon" (2026-06-24, v2 — user rejected v1's "dusk-gateway scene" as "shitty animation") — `DepartureHorizon.vue` rebuilt into a **synthwave perspective grid** receding to a central **portal of light** at the vanishing point: each separation is a **comet (glowing head + flowing dash-trail + aura)** placed on its vector by status fraction and **sized by perspective** (bigger near the foreground, shrinking toward the portal), `rotate()`-aimed at the portal, with a gentle forward glide; a **forward data-sweep** (a widening light-bar `translateY`+`scaleX` from horizon→foreground), portal emit-rings + spinning dashed reticle + breathe, drifting scanline overlay, and a **right-edge depth-gauge** of milestone reticles fed by org-wide `by_status` (click→`@pick`). Comet click→`@focus` opens the drawer; frosted floating HUD count-ups kept. **Bug fixed in the rebuild:** v1 put `transform: translateY()` (a `dh-bob` keyframe) on the SAME `<g>` that carried the `transform="translate(x y)"` placement attribute — **CSS transform overrides the SVG attribute**, so every mote collapsed to the origin and bobbed there (THE "shitty" symptom). Fix = nest: outer `<g transform=translate>` (placement, no CSS transform) › mid `<g transform=rotate>` › inner `.cmt-glide` (CSS transform only). General rule already in §7 — never put attribute-transform + CSS-transform on one node. Distinct from the dashboard's flowing-ribbon current AND from sibling instruments — the only **synthwave-grid + perspective-comet-vectors** motif. Card uses a slim **JourneyMeter** rail (5 nodes) — deliberately NOT the gateway gate-posts of `ExitProcession` (kept for the drawer). The "New separation" modal renders a live **Separation Pass** (DRAFT→READY stamp, dusk route mini-svg). Distinct from the dashboard's flowing-current hero and Notice's countdown arc. |
| Exit — Settlement | "The Reckoning — Final F&F" — a continuously-alive treasury **Settlement Reactor** (`ReckoningReactor.vue`, replaced the earlier static `ReckoningBalance.vue`): a central **dual-arc balance ring** (conic `@property --seam` donut where an emerald Earnings arc meets a red Recoveries arc — the seam *is* the balance reading — + a rotating white sheen sweep), **converging energy streams** (gold particles flow left→core, red right→core via `left/top`+opacity keyframes with seeded per-dot delay/dur/size; stream dims when its side is 0), **orbiting bodies** (3 concentric rings, different speeds/directions, satellite dots), periodic **pulse rings**, a rolling-digit **`ReckonOdometer.vue`** at the core (per-digit reels translateY in `em`, spring slide), an animated **aurora + faint grid + drifting motes** backdrop, and **pointer-reactive 3D parallax** (`usePointerSpotlight` → `--mx/--my` rotateX/Y on `.rkr-scene` + radial glare). Two side readouts (Earnings/Recoveries) with count-up + shimmer-swept fill bars. The Adjust modal carries a **live mini-reactor preview** (ReckonOdometer net + emerald/red balance-meter that re-proportions as you type). `SettlementLedger` bars got a continuous shimmer; `.ctx`/`.flow` cards got a continuous edge-sweep. **All looping motion is the point** — entrance-only animation read as "static" to the user; ambient continuous motion is what sells "blender-level." Everything `prefers-reduced-motion` guarded. Section composition: console hero with **direction lenses** (All / Payable / Recoverable / Balanced + Σ payable/recoverable from `case.settlement_net_amount`), a horizontal **roster strip** (master), and a detail **stage** = context header (employee + status pill + clearance/asset-return cross-links) → `SettlementPipeline.vue` (5-node DRAFT→VERIFIED→APPROVED→PAID→CLOSED rail w/ traveling pulse + REVERSED branch) + contextual action row → `ReckoningBalance` + `SettlementLedger.vue` (diverging gold/red per-line magnitude bars + a **provenance chip strip** that surfaces `computation_snapshot`: "Salary · N/M paid days (attendance-gated)", leave days, gratuity years, notice shortfall, travel-advance count). 6 modals via shared `ExModalShell.vue` (teleport + Presence + tone amber/success/danger): **Adjust & recompute** (per-line override inputs, dirty-only → `recalcSettlement(id, overrides)`, live net preview, "Reset to auto"), Verify/Approve (pre-flight summary + notes), **Disburse** (segmented method + month/year `ExSelect` + ARREAR/RECOVERY posting preview), Close, Reverse (required reason). Distinct from siblings' gatehouse/recovery-bay/dusk-horizon — this is the only **weighing-scale + reactor** motif. Backend fix shipped alongside: `_pending_salary` is now attendance-gated (paid-day weights from `hr_attendance`, future-day + already-released-payslip guards) and `advance_recovery` auto-pulls outstanding travel advances. **Close redesign (2026-06-25):** the one-click Close was rebuilt into `ExCloseModal.vue` — a 3-step "Sealing the Ledger" ceremony (Reconcile attestation checklist → Closure category+remarks → Authorize) whose signature instrument is a **charging conic vault-seal** (`@property --seal-p`, a UNIQUE name not the shared `--ex-p`/`--seam`; ring fills with checklist %, padlock flips `LockOpen`→`Lock` when reconciled, snaps emerald on confirm — distinct from the reactor's dual-arc balance). Backend `close_settlement` now takes an optional `SettlementCloseBody {category,notes}` folded into `write_exit_audit(note=...)` — the reason lives in the audit trail, **NO DB migration** (safe on the shared prod DB). |
| Exit — Experience Letter | "The Credential Atelier — Mint" — a ceremonial credential mint (`ExLetterSection.vue`; the relieving tab was DECOUPLED into its own section on 2026-06-23, so the `letter-type` prop now only ever carries `experience-letter` here — dead `isRelieving` branches in the shared components are harmless). Signature instrument **`CredentialSeal.vue`**: a guilloché authentication die — counter-rotating sine-**rosette** engravings + a conic `@property --ex-p` mintage ring + a central hexagonal **crest that EMBOSSES** (scale-in) as the doc drafts + a gold-foil sweep + press-pulse rings & a verification **beam on ISSUE** + a struck repeating-stripe overlay on REVOKE. **`CinematicCertificate.vue`** = a pointer-tilted/spotlit document that **builds itself** (line-by-line ink-write via motion-v, a drawn guilloché security border, a pressing wax seal, foil mast sweep, an assembling QR matrix, a holographic ISSUED chip, DRAFT/VOID watermark; the sheet stays paper-cream in BOTH themes — only the frame/glow theme-shift). Composition: console `LetterAtelierConsole` (ambient engraving-hall backdrop: slow rosette spin + gold motes + foil sweep, + Eligible/Drafted/Issued/Revoked/Awaiting telemetry lenses) → `LetterCaseChip` roster (wax-seal dossier mini-cards, 3D-tilt) → mint stage: seal + **Eligible→Drafted→Issued** lifecycle stepper (REVOKED branch) + action rail + blocked-gate cross-link + a **Delivery-trace rail** that makes the corporate workflow visible (HR archive / self-service "My letters" / document vault / public QR — each lit active/pending/withdrawn by status). Distinct from gatehouse / recovery-vault / reckoning-reactor / dusk-horizon / countdown. **Backend shipped alongside**: the WeasyPrint PDF (`app/utils/hr/exit_documents/__init__.py` `_builtin_html`) rebuilt ultra-modern + on-brand amber (was stale `#9333ea` purple) — guilloché rosette watermark, gradient masthead, fact chips, segno QR + visible code, embossed `_seal_svg`, microprint strip, gold ribbon; **and a loophole fix** — the unified-vault `EmployeeDocument` mirror is now minted on **ISSUE** (`publish_letter`) not generate, and soft-deleted on **REVOKE** (`withdraw_letter`), so an employee never sees an un-issued/revoked draft in `/user/self-service/documents`. |
| Exit — Relieving Letter | "The Passage — Liberation Gate" (2026-06-23) — decoupled from the Experience atelier into its own `ExRelievingSection.vue` (wired in `HrExitWorkspacePage.vue`) so the two letter tabs no longer share a layout/metaphor (the user's complaint was "relieving looks the same as experience"). Signature instrument **`RelievingGate.vue`**: a mechanical **IRIS APERTURE** that OPENS with release-readiness — 8 retracting blades (nested `.rg-arm` rotate-distribute + `.rg-leaf` `transform-origin:50% 0` `scaleY(var(--rg-close))` retracting toward the rim, `--rg-close = 1 - openLevel`), a conic `--ex-p` rim gauge, a sunburst core that brightens through the widening hole, **outward-streaming motes on ISSUE** (`rotate(var(--a)) translateY` keyframe), passage pulses, slow-spin dashed orbit. openLevel: blocked 0.05 → eligible 0.42 → drafted 0.72 → ISSUED 1 (blooms emerald, "Passage granted") → REVOKED 0.18 (re-shut red). Hero **`RelievingConsole.vue`** = a "horizon of opening light-gates" ambient scene (expanding threshold arches + central beam + outward motes) — NOT the engraving-hall rosette — with relabelled lenses (Cleared-to-release / Released / Held-at-gate). Roster **`RelievingTokenChip.vue`** = a gate-arch glyph whose **portcullis raises** with clearance+F&F (`rect scaleY(1 - raise)`) + a dual readiness meter (distinct from the wax-medallion `LetterCaseChip`). Distinct page LAYOUT too: full-width **release-pipeline ribbon** (Clearance→Settlement→Drafted→Released, 4 stations) above a **gate-LEFT (1fr) / cert-RIGHT (≤460px)** grid, delivery-trace as a full-width footer band (vs experience's seal-left/cert-right + inline vertical trace). **Text-cutoff FIX**: `RelievingCertificate.vue` replaces the shared `CinematicCertificate` for relieving and uses **AUTO height** (`min-height:540px`, content grows) — the bug was `CinematicCertificate`'s `aspect-ratio:400/520` + `overflow:hidden` clipping the relieving variant's longer body+attestation+QR. Distinct gate-arch letterhead + left release-ribbon margin + horizontal three-seal attestation strip + angled RELIEVED stamp; builds itself line-by-line, paper stays cream both themes. Reuses the proven `ExLetterSection` data layer (queue fetch → enrich → generate/issue/revoke; blocked = clearance<100% OR F&F not PAID/CLOSED). Frontend-only, no backend change, build green. Distinct from siblings' mint-die / gatehouse / recovery-vault / reckoning-reactor / dusk-horizon / blueprint-caliper — the only **opening-iris/aperture** motif. |
| Exit — Policies | "The Separation Charter — Governance Atelier" (2026-06-23) — the signature instrument is a **self-drawing Governance Blueprint** (`PolicyConsole.vue`): a 5-station exit-journey rail (Resignation → Notice → Clearance → Settlement → Exit) on a drafting-blueprint panel (corner ticks, faint cross-grid, technical-drawing aesthetic) whose connector **draws itself** (scaleX) with a continuously **traveling survey pulse**; each station is annotated by the *in-force* policy's parameters — an animated **measuring caliper** on Notice (jaws + readout), **gate ticks** on Clearance, **gratuity + chain-depth chips** on Settlement — and every station is a **cross-link** to its sibling tab (closes the "is it connected" gap). Console hero = gradient-accent title + 6 telemetry lenses (Policies/Active/Inactive/Org-default/Grade-scoped/Avg-notice) that filter the grid. Cards = `PolicyCharterCard.vue` **statute plates**: corner-ribbon (In force / Grade override / Default / Inactive), a **wax-seal medallion** (conic `--ex-p` ring, emerald pulse when in force), bento dials (notice/probation calipers, buyout, gratuity), an **approval ladder** (nodes rise + links draw), gate/question counts, 3D pointer-tilt + glare; clicking a card **focuses** the blueprint on it. "In force" = the resolved org default (active wildcard `grade_id=null`, earliest `created_at`) computed client-side, mirroring backend `resolve_policy`. Modal `ExPolicyModal.vue` rebuilt as a **Drafting Console**: live **charter preview** (DRAFT→READY stamp), staggered sections, **custom +/− steppers replacing every number input** (no spinner arrows anywhere — the user's recurring ask), modern toggle switches, and three builders — approval chain, clearance gates, **and interview questions** (the last two loopholes: `description` and `interview_questions` were backend-supported but the old modal never exposed them). Delete uses a dedicated cinematic **`PolicyRetireModal.vue`**: a **wax-seal-break ceremony** (two clip-path半 halves rotate apart + crack-flash + white-flash on confirm, idle breathe), a consequence panel (kept-for-audit / stops-resolving / org-default warning), a **required reason** (preset chips + textarea) **persisted to the audit log** (backend `delete_policy` gained an optional `reason` query param → audit note), and a reversible **"Deactivate instead"** secondary path (flips `is_active` via `updatePolicy`) — the Apple/Google "offer the non-destructive option" nudge. **Scroll-clip gotcha fixed:** a modal whose `.exm-body` is `display:flex; flex-direction:column` AND height-constrained will *shrink* its children (default `flex-shrink:1`) instead of scrolling — the live-preview got clipped. Fix: `body { flex:1 1 auto; min-height:0 }` + `body > * { flex-shrink:0 }` + `flex-shrink:0` on header/footer. Backend touch is tiny (delete reason) + restart; rest frontend-only; build green. Distinct from siblings' gatehouse / reckoning-reactor / recovery-bay / dusk-horizon / countdown-arc — this is the only **technical-blueprint + caliper + wax-seal** motif. |
| Exit — Audit Logs | "Chain of Custody — The Forge" (2026-06-23) — signature instrument **`AuditChainForge.vue`**: workflow events are struck into an immutable **hash-sealed chain of linked blocks** at a glowing **forge anvil** (newest = forge w/ ember-ring + strike-pulse; older blocks trail left, joined by self-drawing **knit links** w/ travelling flux); a **tamper-evidence verification beam** sweeps the track; an **integrity console** side-panel = "Chain intact" lock + all-time count + a **family-composition donut** (conic, 5 action families genesis/transition/completion/reversal/config) + legend + today/last-sealed. Pointer-parallax 3D scene + drifting embers; all continuous + reduced-motion guarded. Section: console hero (complete **33-action** searchable filter + 6 **entity quick-filter lenses** w/ recent counts + Live + Refresh) → `AuditChainForge` (unfiltered recent-120 window + server all-time total) → filtered server-paged **`AuditEventRow.vue`** ledger (tilt+glare, status spine, action-family glyph + green **seal tick**, entity chip, **#hash** badge, from→to **morph pill** w/ flowing arrow, actor/System, note; expands via the CSS grid-rows `0fr→1fr` trick → timestamp/actor/entity-ref/case-ref/payload + cross-link CTA) → `ExPager`. **Loopholes closed (frontend):** action filter was 16/33 (now complete via `auditMeta.js` single-source map); the page was a DEAD read-only log w/ NO cross-links — every row now jumps to its sibling tab via `auditTargetTab` (CASE→resignation, NOTICE_*→notice, ASSET_RETURN_FLAGGED→asset-return, CLEARANCE→clearance, INTERVIEW→interviews, SETTLEMENT→settlement, DOCUMENT→experience/relieving by note, POLICY→policies). Reusable: **`auditMeta.js`** (families/icons/labels, cross-link resolver, `shortHash`/`relTime`). Distinct from siblings (gatehouse/reckoning-reactor/recovery-vault/dusk-horizon/voiceprint/iris-aperture/blueprint) AND from the **Asset** module's equalizer-ribbon Forensic Ledger — the only **forge + hash-chain** motif. **Backend audit-write gaps FLAGGED, not closed:** employee self-edit of draft (`PATCH /me/exit/{id}`), settlement draft `PATCH` overrides, and `SETTLEMENT_DRAFTED` (enum defined, never written) record no audit row. Frontend-only this pass; build green; dark+light. |
| Exit — Self-service (`/user/self-service/exit`) | "My Passage — The Departure Sequence" (2026-06-23) — the EMPLOYEE's own exit POV. Signature instrument **`SsExitJourney.vue`**: the 11 `EXIT_STAGES` rendered as a horizontal **boarding rail** the employee travels — done nodes light emerald (check), the **current node breathes with a "You are here" comet + pulse rings**, future dim; connectors are a track with a **traveling light flux** (active=amber draw, done=emerald sheen); ends at a **gateway-of-light** that seals gold when relieved; auto-scrolls the current node into view; progress readout + drifting motes + blueprint grid. Page (`SelfServiceExitPage.vue`) = **Departure console hero** (gradient title + status pill + fact chips + a **conic LWD countdown medallion** — days-to-last-day odometer via `ExCountUp`, or a status emblem pre-notice) → journey rail → **telemetry strip** (Days-to-LWD / Clearance% / Interview / Letters, count-ups) → cinematic cards (resignation w/ manager-review banner + **Edit & Withdraw**, notice `NoticeCountdownArc`, **F&F settlement** net/earnings/recoveries, clearance read-only ring, mode-aware interview, sealed-letter downloads). Advanced **`SsResignModal.vue`** = 4-step wizard (Type segmented cards → Reason chips+note → Timing `HrDatePicker`+policy explainer → Review w/ a live **"Separation Pass"** boarding-pass preview, DRAFT→READY stamp, confirm gate) + **edit mode** (type locked, matches backend PATCH which only takes reason/detail/requested-LWD). **`SsInterviewModal.vue`** renders the ACTUAL backend `questions[]` (rating→stars, text→textarea, `*reason*`→`primary_reason_category` ExSelect) + would-recommend switch + seal-flash. **Loopholes closed:** old page had NO edit (backend PATCH supported) → added; interview modal hardcoded 5 ratings ignoring policy `questions[]` + the 2 text Qs → now dynamic; NO F&F visibility though `settlement{}` is returned → added read-only card; manager review/decision not surfaced → banner; closed cases (REJECTED/WITHDRAWN/CANCELLED) vanished → closed-outcome summary + re-file; begin-CTA now **eligibility-gated** (blocks ON_NOTICE/EXITED/ARCHIVED/SUSPENDED/… ; backend ACTIVE/ON_PROBATION stays the hard gate). Self-allowed resignation types only (no TERMINATION/MUTUAL — backend-blocked). No backend change; build green; dark+light. Distinct from admin siblings AND from sibling self-service pages (Flight-Deck travel / Gear-Locker assets / Minted-Note payslips). **Component-motion + live reverse-timer pass (2026-06-24):** added **`SsDepartureClock.vue`** — a LIVE-ticking "Final Approach" chronograph (the user asked for a "highly animated reverse timer"): a real `setInterval(1s)` countdown to the END of the last working day (LWD 23:59:59 local so HH:MM:SS keeps running through the final day) rendered as rolling **DD:HH:MM:SS digit reels** (translateY-em strips like `ReckonOdometer`, seconds reel gradient-tinted + blinking colons), a **draining notice meter** (remaining-fraction width + shimmer), a rotating **radar conic** + sweeping **scanline** + drifting motes + pointer parallax (`usePointerSpotlight` on `.sdc-inner`), and **tone escalation** (calm→urgent≤6d→crit≤1d→overdue counts UP red, +provisional steel when only `requested_last_working_date` exists). Gated to countdown-relevant statuses only (`SUBMITTED/MANAGER_REVIEW/ACCEPTED/NOTICE_PERIOD`) so relieved/clearance/settlement don't show a misplaced "last day passed" alarm. Also added reusable **`SsTiltCard.vue`** (entrance + hover-lift + 3D pointer-tilt + spotlight glare + accent spine via slot, so page-scoped card styles stay intact) and wrapped all 6 dashboard cards in it; hero medallion repurposed from a duplicate day-count → a **% notice-served** progress ring (complements the clock instead of duplicating it); letter download rows → motion-v hover/tap (CSS transform removed to avoid the motion-v/CSS transform conflict). Frontend-only, build green. The clock is the only **live-ticking digit-reel chronograph** motif (distinct from `NoticeCountdownArc`'s static draw-on half-arc and the reckoning odometer). **Consistency + authoritativeness fix (2026-06-24):** the meter "% left" and the medallion "% served" were computed on different bases (integer calendar days vs fractional-to-end-of-LWD) → didn't sum to 100 (7%+97%=104). Fixed by making BOTH derive from one authoritative `servedPct` (`left = 100 − served`). Backend now surfaces it: `exit_self._self_detail` adds `notice_metrics` (the same `notice_serving.notice_metrics` the admin board uses — served/remaining/progress/required/short_notice, anchored on the REAL notice start) + a compact `notice_serving` (attendance buckets via `_attendance_during_notice`). The clock takes `:served-pct`, medallion `heroPct = notice_metrics.progress_pct` (fallback mirrors the backend formula). **Workflow clarification:** the day countdown is **calendar-based** (`remaining_days` = LWD − today) and is correctly *independent of attendance* — not punching during notice does NOT shorten/extend the last working day; it lands as **loss-of-pay / notice-shortfall in the F&F** (settlement engine). The notice card now shows an attendance-reality strip (attended / on-leave / not-recorded chips + an LOP warning) so the employee sees the monetary consequence. **Empty-state ("Your Passage") redesign 2026-06-27** (user: make the no-resignation state ultra-modern / high-graphics): the flat `.sse-rest` card was replaced by **`SsPassagePortal.vue`** — a serene cinematic **"Threshold of Light"**: a luminous CSS doorway/arch (light-spilling interior + faint slats + diagonal shimmer + a glowing threshold bar) pours a downward light **beam** onto a breathing **pool**, behind it a slow-rotating conic **ray fan** (NOT screen-blend — washes out on cream), a pulsing **sun** + 3 breathing **halo** rings, 16 deterministic **rising embers**; two-depth **pointer-parallax** (far `.sp-atmos` vs near `.sp-scene`). Below: eyebrow + heading (copy switches on `closed` prop) + sub + primary CTA (`@begin`) or gate note + a 3-chip reassurance row. Props `eligible`/`gateNote`/`closed`/`reduced`. **Gotcha re-confirmed: abspos children of a `display:grid; place-items:center` parent do NOT reliably center — every scene layer needs explicit `left:50%` + translate-centering baked into BOTH the rule and its keyframes.** Own `.reduced` + `prefers-reduced-motion` guards (sp-* classes escape exit-theme's `[class*="ex-"]` blanket rule). The only **doorway-of-light threshold** empty-state in the module (distinct from the dashboard's flowing-current threshold and the relieving iris-aperture). |
| Exit — Dashboard (`/admin/hr/exit/dashboard`) | "The Current of Departure" (2026-06-24, v2) — the hero is a **full-bleed flowing-current scene** (`ThresholdGate.vue`), NOT a centered object — the user explicitly rejected the first pass (a centered archway/portal medallion: "I don't like this gate animation, not in the middle but for the whole card"). v2 fills the entire card edge-to-edge: a dusk field (drifting `ex-tide-flow` gradients + twinkling specks + vignette), **5 aurora-silk light ribbons** spanning full width (blurred `feGaussianBlur` strokes, `mix-blend-mode:screen`, continuous `stroke-dashoffset` FLOW left→right — seamless because `--flow` = the dash period), a **dense particle current** (~30 motes riding the ribbons via `offset-path`, fading in/out, streaming toward the exit), a **threshold of light** at the RIGHT horizon (radial sun glow whose intensity binds to completion `--glow` + a pulsing seam + leftward-expanding emit-rings — particles dissolve into it = "relieved"), and a **full-width journey meridian** (progress rail + comet head + shimmer + count-up) replacing the rejected center medallion. 3-tier pointer-parallax (`usePointerSpotlight` → `--mx/--my`, ribbons/particles/threshold translate by depth). SVG scale uses `transform-box: fill-box; transform-origin: center` (module idiom, matches `DepartureHorizon`/`InterviewResonance`) — NOT `0 0` (resolves to viewBox origin). **Lesson: "for the whole card" means edge-to-edge ambient motion, not a hero prop in the middle — and a centered conic medallion reads as "the middle."** Distinct from resignation's `DepartureHorizon` (plotted status-motes on a dashed route to a spoked sun) — this is the only **flowing-ribbon-current + right-edge threshold** motif. Section (`ExDashboardSection.vue`) keeps the workflow-complete navigation — procession stage-pick map + 7 hero lenses + 11-tile module deck, **every exit tab reachable, no loophole** — but every panel is now alive: `ExitProcession` (cohort) rebuilt continuously-alive (baseline light-runner via `offset-path` + `ex-sheen-pass` sweep + **draw-on posts** via `y`/`height` transition gated on `useInView`+`grown` ref + lamp glow; **both cohort AND single modes preserved** — the case drawer uses single), **animated reason bars** (draw-on width + per-bar shimmer + `ExCountUp`), a **separation-type donut** (SVG arcs, hover-segment widen + dim-others + center count-up that re-counts on hover + orbiting conic sheen + clickable legend), and a `GatewayTile.vue` deck (3D pointer-tilt + glare + status spine + count-up badge + alert ping + `ex-deal` stagger). **Real distributions only — no fabricated time-series** (data = kpis/by_status/by_reason/by_type). Frontend-only, build green, dark+light. |
| Recruitment — Rehire (`recruitment/rehire`) | "Boomerang Return" (2026-06-25) — signature instrument **`ReturnArc.vue`**: alumni plotted as **comets riding a boomerang arc that loops back home** — an outbound dashed low path + a glowing **return arc** sweeping up and into a breathing **org "home" core** (emit-rings + house glyph); comet count = eligible-alumni count, each via SMIL `<animateMotion :path rotate="auto">` with staggered negative `begin` (no lockstep), gated to a static pose under `prefersReduced`; departure gate at left, pointer-parallax stage, faint blueprint grid + drifting motes. Console hero (gradient title + search/refresh + 4 telemetry lenses All/Boomerangs/First-timers/Avg-prior-tenure) → cinematic **`RehireAlumCard.vue`** (3D tilt + spotlight, spinning avatar readiness ring, **service ledger** prior-tenure→away with a hover-flowing mini return arc, boomerang ×N badge, sweep CTA). Modal **`RehireModal.vue`** rebuilt as a **2-pane Reinstatement Console**: a live **Welcome-Back Pass** (left — avatar, Exited→Rejoins route, placement chips, probation/Return-#N pills, DRAFT→READY stamp, flowing arc) + a **4-step wizard** (Tenure `HrDatePicker`+probation `+/−` stepper → Placement 4×`HrSelect` "Keep (current)" → Reason preset-chips+note → Review animated **process checklist** of exactly what happens on confirm). **Light-theme border bug fixed**: cards used `var(--hr-border-soft, …)` but `--hr-border-soft` is UNDEFINED → fell back to faint gold (invisible on cream); switched to real `--hr-border`/`--hr-border-strong` tokens. Arc-gradient light swap done via `useTheme().isLight` (CSS can't reference the dynamic-uid gradient id). `theme:light` `mix-blend`/pale-stroke pitfalls avoided (deeper `#ea580c`/`#c2410c` arc + solid flow stroke). Distinct from recruitment's orbiting "Talent Stream" hero and every exit/asset motif — the only **boomerang-return-arc + comets-coming-home** instrument. Backend unchanged (`/hr/employees/rehire-eligible` + `lifecycle/rehire`); build green. |
| HR Settings — Approval Workflows (`settings/approval-workflows`) | "The Routing Switchboard — Signal Relay" (2026-06-26) — approval routing as a **live signal relay**: a request-signal travels Employee → relay **gates** → Approved verdict. Signature instrument is the editable relay itself: an origin node, `ApprovalGate.vue` station cards (per stage — `SetSelect` approver picker, label, **+/− amount-gate stepper** for travel/reimbursement, reorder/remove, 3D tilt+glare, status spine, glyph-ring pulse) joined by **continuously-flowing conduits** (moving-gradient energy + idle packets), and a sealing terminus. A **"Dispatch test"** button fires a packet that walks the conduits and **lights each gate in sequence** then seals the verdict (`dispatchPos` timer drives `:lit`/`:active`/`.surge`/`.done`). Hero carries a **3-channel "routing bus"** (leave/travel/reimbursement, ambient flowing tracks, doubles as the channel selector); a horizontal **policy roster** strip selects the policy; a right **Routing-Intelligence** panel shows stage count + approver-mix bars + amount-gate list + a **connectivity map** (the honest wiring: leave/travel/reimbursement = Live-configurable, **training = Fixed-in-code**, exit = No-chain) + a "Pair with Notification Rules" cross-link. Delete = **`ApprovalResetModal.vue`** "Revert to default routing" — a **current-chain → default-chain morph** (two mini-rails, conflict→ok), a live-consumption banner ("in-flight requests keep their snapshot"), reason presets→audit. Backend: thin editor over the existing `approval_chain` JSONB on Leave/Travel/Claim policies — confirmed every consumer treats an **empty chain as "use default"** (`if not chain: return default`), so revert = PATCH `[]` (safe, never auto-approves); added optional `reason` to `ApprovalChainUpdate` folded into the settings-audit note (no migration). Shared `approverMeta.js` (icon/color/label, brand palette only) keeps gate/modal/panel in sync. Distinct from every sibling — the only **signal-relay / flowing-conduit switchboard** motif. |
| HR Settings — Recruitment (`settings/recruitment`) | "The Talent Forge — Hiring Governance Console" (2026-06-26) — replaced the dead-end `SetModuleHubSection` link-hub (5 deep-link cards, ZERO animation) with a bespoke `SetRecruitmentSection.vue`. Signature instrument **`HiringFunnel.vue`**: a vertical perspective **talent cascade** — 7 narrowing gate-bands (Applied→Screening→Shortlisted→Interview→Selected→Offer→Joined, fixed geometric `width:100−i*7.5%` so it reads as a funnel even on an empty DB) fed by the **live recruitment dashboard `funnel[]`**, each gate = icon + label + throughput meter + count-up + sheen sweep, draining into a glowing emerald **"Hired" basin** (emit-rings when joined>0); 30 **data-independent cascading motes** (seeded delays/x/drift, never reads empty), a topographic **flow-field** SVG, a floating glass **HUD** (in-funnel / joined / conversion count-ups), 3-tier pointer-parallax via `usePointerSpotlight`. Composition: console hero (`SetSectionHead`) + **6 live telemetry lenses** (open positions / in-pipeline / interviews / offers-out / hires-MTD / acceptance% — each deep-links into the recruitment module) → command grid (funnel + a **Source-of-hire** animated-bar panel from `sources_distribution` + a **Top-departments** list from `department_hiring`, both real data) → **taxonomy blueprint** (`RecTaxonomyCard` tilt/spotlight spec-cards over `recruitmentVocab.js` — the 9-dimension governing vocabulary mirrored from the backend enums, **honestly badged "System · fixed in the engine"**) → editable **Interview Panels** (`PanelForgeCard` tilt cards + `PanelModal` SetModal-shell create/edit with live-preview aside + chip-expertise + member builder + `PanelDeleteModal` with a non-destructive "Deactivate instead" path) → **connectivity bus** (wired-settings `@go` cross-links to Numbering/Designations/Employment-Types/Approval-Workflows/Notification-Rules + module-surface jumps). **Workflow truth surfaced (closes the "loophole" ask):** recruitment is **enum-driven** — the ONLY DB-backed, org-configurable entity is the Interview Panel (`/hr/recruitment/panels`, reused via `usePanels`); every stage/status/source/type/round/recommendation is a fixed Python enum, and **requisition & offer approval is a MANUAL decision (POST /decision), NOT a configurable chain** — flagged on the Approval-Workflows wire-card (`flagged:true`, dashed, "Manual" pill) rather than implying a false connection. `recruitmentVocab.js` is the single source of truth that ends the stage/source/type duplication scattered across 8+ recruitment Vue files. New `recruitment` is an explicit workspace branch (dropped from `HUB_SLUGS`). Frontend-only, no backend change, build green, dark+light (no `mix-blend:screen`, all `--set-*` tokens). Distinct from every sibling — the only **descending talent-funnel / cascade** motif (vs approval-workflows' signal-relay conduits, payroll's turbine, compliance's shield, numbering's stamping-press). |
| HR Settings — Appraisal Templates (`settings/appraisal-templates`) | "The Weighting Wheel — Rubric Calibration" (2026-06-26) — rebuilt the plain master-detail editor (left list + form, no signature motion) into a cinematic console+workbench. Signature instrument **`AppraisalWeightWheel.vue`**: a **live calibration dial** — each weighted section is an SVG arc (donut technique: one `<circle>` per section, `stroke-dasharray`=weight share of C=2πr, `stroke-dashoffset`=cumulative, group `rotate(-90)`, smooth transition on dasharray/offset); the ring only **CLOSES into a full circle at exactly 100%** — under-weight shows a hatched amber gap, over goes red, at 100% it snaps emerald + glows + fires emit-rings + the needle locks. Center read-out = total% + state ("Calibrated"/"N% to go") + cycle/rating/section chips; needle points to total; rotating sheen + breathing aura + pointer parallax. It IS the editor's live preview AND cross-highlights with section rows (`@hover`↔`:active-index`). Distinct from employment-types' fixed-enum DONUT — interactive weight-balance gauge; "close the circle to calibrate" is the signature. Composition: console hero + **6 telemetry lenses** (Templates/Active/Balanced/Scoped filter the roster; Avg-sections/Cycles info) → horizontal **roster strip** (`AppraisalTemplateCard` tilt/glare cards w/ a conic balance-ring **on `::before`** — masking the ring element itself clips its centre content, so the ring MUST be a pseudo-element — + section-type dots + scope chips + "New" card) → **2-col editor** (sticky wheel-left + form-right): identity, cycle segmented, **rating-scale stepper + named-rung labels builder**, **applies-to grade/department toggle-chip scope**, weighted **`AppraisalSectionRow`** rows (type `SetSelect`, ±5 weight stepper, expandable **per-section criteria** chip builder via `Presence` height-auto, reorder/remove, stable `_k` key so reorder doesn't remount) + auto-balance → **workflow/connectivity** band. **Loopholes closed (all frontend-only — fields existed, UI never exposed them):** `is_active` toggle + Deactivate-instead (`AppraisalDeleteModal`), `description`, `rating_scale.labels` (was max-only), `applies_to_json` {grade_ids, department_ids} scope (stored but never surfaced AND never consumed), per-section `criteria_json`. **The honest-connectivity answer (user asked "how is the promote module connected?"):** it ISN'T — appraisal templates are a **dead config master**, no Performance-review module consumes them, and `POST /hr/employees/{id}/lifecycle/promote` reads designation/grade/pay only with **zero appraisal/performance data** (the `governs:['employees','training']` in connectivity.js is aspirational). Surfaced via a "Consumed downstream" group with honest flags — Performance reviews `Planned` (static, no module), **Promotion `Not wired`** (→ employees module), Training `Planned` (→ skill-matrix) — plus "Scoped with sibling settings" (Grades/Designations/Departments/Audit-Logs real `@go`) and a one-line workflow note. No backend change (model already stores rating_scale/applies_to/criteria as JSONB `Any`; delete has no reason param so no audit-reason here). `appraisalVocab.js` mirrors backend enums (6 SECTION_TYPES, 6 CYCLES, default rating labels). Build green, dark+light (wheel sheen `mix-blend:overlay`→normal in light). Distinct from every sibling — the only **weight-balance calibration-wheel** motif. |
| HR Settings — Training (`settings/training`) | "The Capability Canopy — Learning Foundation" (2026-06-26) — replaced the **last** dead-end `SetModuleHubSection` link-hub (zero animation) with a bespoke `SetTrainingSection.vue`; `training` dropped from `HUB_SLUGS` (now empty → the hub branch + `SetModuleHubSection` import removed from `HrSettingsWorkspacePage`, the component is now an orphan). Signature instrument **`CapabilityCanopy.vue`**: a **living tree of capability** — a gradient trunk (rust→ember, grows via `scaleY` from `transform-origin:50% 100%`) rising to a fan of **7 branches** (one per `TrainingType`, drawn via `pathLength="1"` + `stroke-dashoffset` 1→0 staggered by `--bi`), each tipped with a **breathing bloom** (SVG circle, radius+count from the live `by_type[]` distribution, count in `<text>`, seeded `--phase` so blooms never pulse in lockstep), under a **"sun of mastery"** whose glow/scale binds to `completion_rate` (emit-rings), with a **rising sap spark** up the trunk (translate keyframe — NOT `offset-path`, which is unreliable on SVG `<circle>`), ~18 seeded **rising knowledge motes** (data-independent → a near-empty DB still reads lush), a blueprint floor, a floating glass HUD (programs/trained/completion count-ups), a color→family **legend**, and 3-tier `usePointerSpotlight` parallax. SVG is `viewBox 0 0 1000 480` + container `aspect-ratio:1000/480` + `xMidYMid meet`. Composition = the proven Settings grammar but a unique identity: console hero (`SetSectionHead`) + **6 live telemetry lenses** (programs / trained / completion% / compliance% / certs-active / skill-gaps — each deep-links into the Training module) → command grid (canopy + a **Delivery-momentum** spark from real `monthly[]` completions + a **Certification-horizon** stacked bar from `cert_status[]` with ≤30/60/90 windows + skill-gap callout) → **editable Skill catalog** (`SetSkillCard` tilt/glare cards with a **proficiency-ladder** rail + category filter chips; `SetSkillModal` SetModal-split with a live skill-card preview + category `SetSelect` + max-level `+/−` stepper; `SetSkillDeleteModal` with requirement-orphan warning + **Deactivate-instead** path — `deleteSkill` has NO reason param, so no audit reason here) → **taxonomy blueprint** (reuses `RecTaxonomyCard` over `trainingVocab.js` — 9 fixed-in-code dimensions: types/delivery/skill-cats/sources/cert-lifecycle/compliance-cadence/enrollment+status/request-lifecycle/people+resources, honestly badged "System · fixed") → **connectivity bus** (`@go` cross-links). **Workflow truth surfaced (closes the "loophole" ask):** the genuinely org-configurable, settings-appropriate Training entity is the **Skill catalog** (`/hr/training/skills`, CRUD via `useTraining`) — the other masters (certs/trainers/programs/compliance/budgets) are operational and managed in the module (deep-linked, not duplicated). **Real wires:** notification-rules (`TRAINING_ASSIGNED`+`CERTIFICATION_EXPIRY` in the catalog), designations + grades (skill-**requirements** pin a required level per role — fetched live to show real per-skill req counts), departments (skill/budget/compliance scoping). **Flagged honest:** approval-workflows (training-request sign-off is a **fixed Manager→HR chain** in `chain.py`, not the configurable `approval_chain` editor) + numbering-series (requests use a **built-in `TR-YY` SystemSetting counter**, not the NumberingSeries table). `trainingVocab.js` is the single source mirroring the backend enums. Frontend-only, no backend change, build green, dark+light (token-based; bloom count text stays `#fff` on saturated discs). Distinct from every sibling — the only **growth-tree / canopy** motif (vs recruitment's descending funnel, onboarding's rocket-ascent, approval's signal-relay, payroll's turbine, compliance's shield). |
| HR Settings — Onboarding (`settings/onboarding`) | "The Launch Sequence — Joining Control" (2026-06-26) — replaced the dead-end `SetModuleHubSection` link-hub with a bespoke `SetOnboardingSection.vue`. Signature instrument **`LaunchSequence.vue`**: an **ascending launch trajectory** — a quadratic-bezier arc (computed in JS, sampled in a 1000×440 viewBox; container `aspect-ratio:1000/440` + `preserveAspectRatio=xMidYMid meet` so node `left/top %` = `coord/viewBox*100` aligns the SVG path with the absolutely-positioned DOM nodes) from a **launch pad** (bottom-left) to a **liftoff orbit** (top-right, emit-rings when ACTIVE>0); the **7 journey stages** (Pre-Join→Approvals→Documents→Identity→Assets→Training→Active) are **gantry rings** along the arc (SVG `stroke-dashoffset` fill by count-share, count badge, pulse when lit), and a **rocket rides the arc to the cohort's centre-of-mass** (`Σ count·i / 6·total`) with tangent-aligned rotation (`atan2` of the bezier derivative +90°), a flickering exhaust plume (nested bob node so the bob keyframe never fights the rotate transform) and a drifting **starfield** (data-independent → never empty) + telemetry sweep + pointer parallax. Fed by the live `/hr/onboarding/journey-state` per-stage counts; HUD count-ups (in-journey / reached-active / cohort progress). Composition: console hero (6 live lenses from `dashboard/stats`: in-progress / joining-today / docs-pending / assets-pending / training-due / on-probation — each deep-links into the module; training-due → the Training module, honouring "training owned by Training") → LaunchSequence → **editable Checklist Templates** (`OnbBlueprintCard` tilt cards w/ category lane spine + mandatory/offset/owner chips + activate-toggle, lane filter chips, `ChecklistTemplateModal` SetModal create/edit w/ live preview + category segments + due-offset `+/−` stepper + Mandatory/Active switches) → **editable Welcome Kits** (`WelcomeKitCard` + `WelcomeKitModal` item-builder w/ qty steppers + live kit-box preview) → **taxonomy blueprint** (reuses `RecTaxonomyCard` over `onboardingVocab.js` — journey/lanes/documents/accounts/roles/statuses, honestly badged "System · fixed") → **connectivity bus** (`@go` to Recruitment/Numbering/Employment-Types/Training/Approval-Workflows/Notification-Rules + module-surface jumps). **Workflow truth surfaced:** the ONLY DB-backed, org-configurable onboarding settings are **Checklist Templates** (`/hr/onboarding/checklist-templates` — bootstrap stamps every ACTIVE template onto each new hire; `is_active` gates seeding; create+edit+toggle, NO hard delete) and **Welcome-Kit Templates** (`/hr/welcome-kit/templates` — create+edit only, no delete, no is_active in upsert) — neither had a settings UI before (real loophole closed). The 7-stage journey, the **9 document slots** + **default accounts** (ERP/Email/Attendance) are HARDCODED in `onboarding_bootstrap.py` (look configurable, aren't — surfaced as fixed), and **joining approvals are added per-hire (manual), not a configurable chain** (flagged `Manual` on the Approval-Workflows wire-card). New `onboarding` is an explicit workspace branch (dropped from `HUB_SLUGS`; only `training` remains a hub). Frontend-only, no backend change, build green, dark+light. Distinct from every sibling AND from the onboarding module's own `OnbJourneyPath` (a flat undulating sine-path with waypoints) — this is the only **launch-trajectory + rocket-ascent + gantry-rings** motif (vs recruitment's descending talent-funnel). |
| HR Settings — Asset Categories (`settings/asset-categories`) | "The Lifespan Cellar" (2026-06-26) — replaced the GENERIC `MasterManager` render (asset-categories was the last "Modules" master still on the shared engine, which is exactly why it felt repetitive) with a bespoke `SetAssetCategoriesSection.vue`. Signature instrument **`LifespanColumns.vue`**: a cellar of luminous **glass lifespan columns**, one per asset class — **fluid fill height = asset population** (`asset_count`, count-up), a **metering stream + splash drips at a cadence ∝ the depreciation clock** (short useful-life → fast ~1.3s, long → slow ~7s; rate `life/9` clamped), and `depreciation_method: NONE` (or no life) renders a **frozen emerald frost ∞ "permanent" cap** with NO drip — the depreciation dimension made literally visible. Inactive columns drain/desaturate/hatch; active sink to the floor biggest-first. Population depth-rail + drifting motes (data-independent → never empty) + ambient sweep + `usePointerSpotlight` parallax; hover lifts a column and updates a header readout. Cards (`AssetCategoryCard.vue`) carry a **mini lifespan meter** echoing the instrument + the previously-ignored `asset_count` badge + life/method/type chips + lineage breadcrumb (orphan flag) + guard-aware delete. Modal (`AssetCategoryModal.vue`) = `SetModal` side-rail console with a **live mini-column preview** that fills/drips as you type, custom +/− life stepper (no spinner arrows), segmented method/status, `SetSelect` parent/type. **Loopholes closed (the deliverable):** the old `masterDomains.js` config exposed only name/code/parent/useful-life/active/description — it OMITTED **`default_asset_type`** and **`depreciation_method`** which the backend supports AND the depreciation workflow consumes (you literally couldn't set how a class depreciates) → both now editable; **`asset_count`** (returned per row, ignored by the generic engine) is now surfaced everywhere — lenses, cards, instrument, and a real **delete-guard preview** (`AssetCategoryDeleteModal.vue` shows "N assets filed here — protected" mirroring the backend 409 *before* the click, with a "View N assets" deep-link + a non-destructive "Deactivate instead" path); a **Depreciation-Intelligence aside** surfaces config-gaps (straight-line classes missing a useful-life), orphan sub-classes (parent inactive/removed), permanent classes, and delete-protected classes. **Connectivity:** cross-links to the Assets dashboard / **full Taxonomy Atlas** (`/admin/hr/assets/categories`) / fleet inventory; the per-class "View N assets" deep-links `/admin/hr/assets/inventory?category_id=…&categoryLabel=…` — which required a **tiny safe seed in `HrAssetsWorkspacePage.vue`** (read `route.query.category_id` into `invFilter` synchronously in setup, before the inventory child mounts) since the assets workspace previously only accepted its in-workspace `@go` filter (no external route could deep-link inventory filtered by category — itself a small gap). Wired as an explicit workspace branch **before** `v-else-if="masterDomain"` (masterDomain stays truthy, so order matters). **ROOT-CAUSE follow-up:** the page showed 0 assets per class because the **Register-asset modal (`AssetUpsertModal.vue`) never had a category picker** — it only set the `asset_type` enum, never `category_id`, so no asset was ever filed into the taxonomy. Added a **Category `AsSelect`** (active classes via `fetchCategories`) → `form.category_id` in the create/update payload; picking a class also inherits its `default_asset_type` + `depreciation_method`. Backend already accepted `category_id` (`_AssetExtras` in `app/schemas/hr/asset.py`) — pure frontend gap. **Asset TYPE (enum) ≠ CATEGORY (taxonomy)** — the register form now exposes both. **Category-owns-its-types (backend change, by user request — the ServiceNow/Snipe-IT model):** added **`AssetCategory.allowed_asset_types` (JSONB, opt-in allow-list; empty = any type)** — settings modal shows a multi-select chip set, the register modal's Type dropdown filters to the class's types with a **"show all types" escape hatch** (`showAllTypes` ref; current value always kept visible), `default_asset_type` auto-derived = first allowed. Decision was **Guided + opt-in, NOT strict** (empty list = all types; never hard-blocks entry — the enterprise discipline of guide-don't-wall). Migration = idempotent `add_asset_category_allowed_types.py` (`ALTER … ADD COLUMN IF NOT EXISTS … JSONB`, reads `.env` itself, ran on the LIVE remote DB) + a backend restart (run_server.py PID kill → relaunch detached → `/health` 200 → openapi shows the field ×3). Distinct from every sibling settings motif AND from the Asset-module's own Taxonomy Atlas (spectrum-bar + coverage conic ring + decay sparkline + fleet-share rings) — the only **glass-lifespan-column / depreciation-metered-fluid** motif. Frontend-only, no backend change, build green, dark+light (light overrides on every cylinder; no `mix-blend:screen`). NB: the `Infinity` lucide import shadows the JS global per-file — fine, none of these files use the numeric `Infinity`. |
| HR Settings — Asset Types (`settings/asset-types`) | "The Specimen Table" (2026-06-26) — a NEW settings page born from the asset-categories thread (user wanted to create their own asset *types*, not just categories). Signature instrument **`TypeSpecimenTable.vue`**: a **periodic-table grid of asset-kind tiles** — each tile = index + lucide icon + a 3-letter symbol (from code) + count-up + a share meter (gradient fill) + built-in(lock)/custom(spark) flag; a **diagonal energize scan** sweeps the grid, drifting motes (data-independent), pointer-parallax, hover lift+glare; click→edit. Unique vs the Lifespan Cellar (cylinders) and every sibling. Composition = console hero (5 lenses All/Active/Inactive/Built-in/Custom + Σ typed) + Specimen Table + a **Catalog-intelligence aside** (most-used / custom count / unused / deactivated-but-in-use ghost) + `AssetTypeCard` grid + `AssetTypeModal` (icon-picker grid from `ASSET_TYPE_ICON_CHOICES`, live specimen-tile preview, auto-code-from-name) + `AssetTypeDeleteModal` (system-protect + in-use 409 guard + deactivate-instead). **Backend (real, by user request):** new `AssetTypeDef` table (`hr_asset_type_defs`, 13 built-ins seeded `is_system`), router `/hr/asset-types`, AND the key change — **`Asset.asset_type` + `AssetCategory.default_asset_type` converted from PG enum `hr_asset_type` → varchar(40)** so custom codes are storable (models→`String(40)`; schemas→`str`/`List[str]`; `AssetType` str-enum kept for built-in branching since `"LAPTOP"==AssetType.LAPTOP`). Migration `migrate_asset_types.py` (idempotent ALTER+CREATE+seed; ran on live remote DB — table is `hr_assets` PLURAL; guard checks `information_schema` data_type). Frontend: `useAssets` gained catalog CRUD + `iconForTypeName`/`ASSET_TYPE_ICON_CHOICES` (lucide registry); the **register modal (`AssetUpsertModal`) + category allowed-types chips now read the LIVE catalog** so custom types appear with icons (`ALL_TYPE_OPTIONS` is a computed with a hardcoded-enum FALLBACK until load; preview accent uses `var(${cssVar}, var(--as-amber))` so custom codes don't break the medallion color). Domain added to connectivity.js (icon `Shapes`) + route-tab constraint + workspace branch. **TYPE (physical-kind catalog) vs CATEGORY (depreciation/grouping) stay two intentionally-separate axes** — surfaced in copy on both pages. Build green, dark+light, reduced-motion guarded. Distinct **periodic-table / specimen-grid** motif. |
| Travel — manager Team-Approvals (`travel/team/`) | "Authorization Bridge" — **side-profile ILS GlideScope** (`GlideScope`: distance×altitude elevation, glide-slope beam + PAPI + plotted aircraft by aging/cost + approach scan-sweep) — deliberately a SIDE view to stay distinct from the admin's top-down radar. `TeamTravelStrip` cards (flight-progress strip w/ inline expandable detail built from the queue payload — managers can't hit the admin `GET /{id}`, so never fetch detail there) + enhanced "Clearance control" `TravelDecisionModal` (tone-morph aura, cost micro-bar, live `ApprovalRunway` chain preview, ink-press stamp + cleared-wash + taxiing-plane flourish). |
| Performance — Dashboard (`/admin/hr/performance/dashboard`) | "The Performance Observatory" (2026-06-26) — the Arena's `dashboard` was a static page (the user: "not a single animation"). Signature instrument **`PerfObservatory.vue`**: a living celestial **orrery** — a luminous **core** = workforce avg score (colour via `scoreTone`, breathing corona + emit-rings + a conic `--perf-p` completion-rate ring around it), wrapped by **4 concentric orbital tracks** = the review lifecycle (Self→Manager→Completed→Acknowledged), each carrying **orbiting bodies** (one glowing mote per in-flight review in that stage, capped at 9 + a `+N` overflow marker). **Orbit nesting that survives reduced-motion:** `.obs-arm` (static `rotate(var(--a0))` start offset, `height:50%` of the ring = radius, `transform-origin:bottom center`) › `.obs-spin` (continuous `@keyframes obs-orbit{to{rotate(360deg)}}`, same origin) › `.obs-body` (`top:0;left:50%;translate(-50%,-50%)`) → body orbits at radius starting at a0; under `prefers-reduced-motion` the spin animation is killed and the body rests at its distributed a0 (NOT collapsed to 0°). Plus a flowing **topographic wave-field** (`stroke-dashoffset` contour lines) + **data-independent starfield** (26 seeded twinklers so an empty DB still reads rich) + 3-tier pointer parallax (`usePointerSpotlight` → `--mx/--my`; wave/stars/system/HUD translate by depth) + a 3D `rotateX/Y` tilt on the system + 4 floating glass **HUD count-ups** (in-orbit/signed-off/in-flight/overdue, overdue pulses red). Per-ring count **tags** stack up the vertical centre-line (rings differ in diameter → tops at different y, so they don't collide). Section (`PerfDashboardSection.vue`) = console hero (6 animated lenses) → Observatory → bento command grid (**relay rail** w/ traveling pulse + **segmented tick-bar status distribution** à la the "Activity Summary" reference | `PerfScoreRidge`) → (recent activity w/ live-dot first row | by-cycle shimmer bars) → **module launchpad** (`v-tilt` tiles to ALL 7 sibling tabs — insights/reviews/cycles/calibration/goals/feedback/pips — the connectivity deliverable: every Performance surface reachable from Command) → overdue banner. **New review modal rebuilt** (`PerfReviewModal.vue`): 2-pane — a live **"Review Pass"** preview (DRAFT→READY stamp, a mini orbit motif echoing the Observatory w/ a conic weight-total ring, period/due/target rows, weight-balance chip) + a staggered form with a **segmented Single/Cohort** mode toggle (sliding gold thumb; switching inside the modal — a new capability), `PerfSelect` dropdowns (NEW perf-scoped AsSelect clone — teleported z-5200, searchable, keyboard-nav, `--perf-*`/`--hr-input-*` tokens), the **exact `HrDatePicker`** the Assets register modal uses for purchase-date (z-5000 teleport, month-slide), and a cinematic employee picker. Dashboard mounts the modal too, so **New review / Launch cycle fire straight from Command** (createReview/bulkCreateReviews → refresh stats). Frontend-only, no backend change, build green, dark+light (all `--perf-*` tokens, no `mix-blend:screen`), reduced-motion guarded. Distinct from the **Assets** dashboard "Status Reactor" (static nested arcs) and the **Exit** dashboard "flowing current" — the only **orbiting-orrery** motif. |
| HR Settings — Merit Policy (`settings/merit-policy`) | "The Merit Curve — Performance→Pay Transfer Function" (2026-06-26) — rebuilt the plain hero-bar + flat `MeritLadder` page (which also had a **silent bug**: it coloured bands via `usePerformance.bandTone` → `var(--perf-*)`, and **`--perf-*` does NOT resolve in the settings scope**, so every band rendered colourless). Signature instrument **`MeritCurve.vue`**: the policy drawn as its literal **transfer function** — X = performance score (% of rating max), Y = hike % — where each band is a **step ZONE** (score range → a hike clamp-window rect) forming an ascending **staircase** (draw-on via `stroke-dashoffset`), with a continuous **oscilloscope PROBE** (JS rAF ping-pong sweep, reduced-motion-gated) that reads out "score N% → band → hike range" live, and **red HATCHED gap zones** wherever the bands don't cover 0–100% (a governance defect made visible). Distinct from every sibling settings motif — the only **transfer-curve / oscilloscope** instrument (vs grades' band-spectrum, payroll's turbine, appraisal's calibration-wheel, etc.). Section (`SetMeritPolicySection.vue`) = `SetSectionHead` console (6 telemetry lenses incl. a clickable **Integrity** lens) → command grid (big MeritCurve + a conic **budget gauge** (`@property --mp-deg`) + a **Policy-integrity checklist**: coverage gaps / overlaps / monotonicity / top-hike-vs-budget — the corporate governance check) → a **"From rating to raise" workflow ribbon** (7 stations, traveling pulse, each cross-links its real surface: Appraisal score→appraisal-templates, HR approval→`/performance/merit`, Comp revision/Payroll→payroll) → tilt+glare policy roster cards (compact MeritCurve + sound/gap chip) → **connectivity bus** (appraisal-templates / grades / approval-workflows[flagged: hike sign-off is a direct HR decision, not a chain] / audit-logs + module surfaces). **Loopholes closed:** the section was mounted with **no `@go`** (zero cross-links) — now wired (`@go="selectTab"` in `HrSettingsWorkspacePage`); colourless bands fixed via a local `--set-*` `bandColor`; coverage/overlap/monotonic validation surfaced (was none). **Modal** = `SetModal` split: live MeritCurve preview + form (name/desc + **`MpStepper`** budget + status toggles) + per-band editor rows (a **`SetSelect` tier dropdown** EXCEPTIONAL→BELOW+Custom that sets key/colour/label, label input, `MpStepper` score-% & hike-% ranges — no native spinners, the recurring ask, auto-PIP toggle, animated add/remove via `Presence`) + live validation warnings; aside = a **"Policy Pass"** (DRAFT→READY stamp, mini budget gauge, band/top-hike stats, integrity checklist). Separate `SetModal` delete (mode=delete) with default-policy caution. Reusable: **`meritBands.js`** (`bandColor` on `--set-*`, `TIER_OPTIONS`, `analyzeBands` gap/overlap/monotonic, `bandAtFrac`), **`MpStepper.vue`** (typeable +/− stepper, spinners stripped), **`MeritCurve.vue`** (full + `compact`). **`frac_max` gotcha:** a band reaching 100% must SAVE as `1.01` (not 1.0) so a perfect appraisal score still resolves (backend `band_for_score`: `frac < hi OR hi > 1.0`); the UI displays 100% but `payload()` re-inflates to 1.01. Backend unchanged, build green, dark+light (all `--set-*`, no `mix-blend:screen`), reduced-motion guarded. See [[merit_hike_pipeline_shipped]] for the data/workflow. |
| Performance — Reviews (`/admin/hr/performance/reviews`) | "The Verdict Line — Review Flux Pipeline" (2026-06-27) — the Reviews tab was a plain filter-chips + card-grid with ZERO animation and NO signature instrument. Rebuilt around **`ReviewFluxPipeline.vue`**: reviews flow **left→right** along the 4-stage lifecycle (Reflection → Manager review → Completed → Signed-off) as **score-tinted capsules riding flowing conduits** between **luminous stage-reactor nodes** (conic `--perf-p` ring = that stage's share of total, icon in hole, count-up badge, in-flight nodes pulse emit-rings); a **merit SPUR** drops off the Completed node into a glowing **pay-uplift coil** (the literal Reviews→Merit→Pay link made visible, clickable → `@go merit`); an **overdue ember HUD** + avg/completion/in-flight HUDs (reuse `SetCountUp`); data-independent drifting motes + scanline + 3-tier `usePointerSpotlight` parallax + a draft feeder badge + a voided side-chip. Stage nodes are clickable filters (`@pick` → status); the picked node lights via an `:active-status` prop. Capsules ALWAYS run (≥2 per segment) so an empty DB still reads alive. Unique within Performance — vs the dashboard's **orbiting orrery**, insights' **bell-curve spectrograph**, merit-policy's **oscilloscope transfer-curve** — the only **horizontal stage-flux-pipeline + merit-spur** motif. Section (`PerfReviewsSection`) = console hero with **6 telemetry lenses** (All + 4 lifecycle + Cancelled, filter the list) → the pipeline → filter bar (client-side name search over the loaded page + cycle `PerfSelect`) → enhanced `PerfReviewCard` grid (now shows a **hike chip** when `hike_status≠NONE`) → pager. **Loopholes closed (the deliverable — all were real gaps, mostly frontend-only):** (1) the drawer NEVER surfaced the **merit→hike pipeline** though the backend (`/{id}/merit`, `/recommend`, `/approve-hike`, `/reject-hike`) + composable (`fetchReviewMerit`/`recommendHikeAdmin`/`approveHike`/`rejectHike`/`hikeStatusMeta`) + every hike field on the review response already existed → new **`PerfMeritPanel.vue`** (drawer "Merit & Pay" tab) resolves the band from the active Merit Policy, draws the band's hike-window rail, lets HR **recommend (clamped to the band) → approve & apply (effective-dated salary revision to payroll, idempotent) → reject**, shows prev→new CTC once APPLIED, and **cross-links** to Settings → Appraisal-Templates (the rubric) + Merit-Policy (the bands) + the in-module Merit/Calibration tabs — closing the "is it connected to appraisal-templates/merit-policy" question; (2) the workspace mounted Reviews with **no `@go`** (zero cross-links) → wired `@go="selectTab"`; (3) **delete had no backend guard and silently soft-deletes even an APPLIED-hike review without reversing the compensation revision** → new cinematic **`PerfReviewDeleteModal.vue`** (VOID-stamp ceremony) computes status-aware consequences, shows a **critical warning quoting the live CTC change** that a delete does NOT reverse, requires a reason for high-impact (COMPLETED/ACKNOWLEDGED/APPLIED) deletes, and offers a reversible **"Cancel instead"** path (transition→CANCELLED, only valid for in-flight statuses per the state machine — the Apple "offer the non-destructive option" nudge). The drawer (`PerfReviewDrawer`) rebuilt: avatar header, dual self/manager + overall **score gauges**, flowing **workflow stepper** (sheen-swept rail fills to current stage), segmented **Scorecard / Merit & Pay / Timeline** tabs (timeline built from the review's date fields), rubric/overdue/hike facts, delete routed through the new modal via `request-delete`. `PerfReviewModal` enhanced: a **lifecycle ribbon** in the live "Review Pass" (shows it opens at Manager review → … → Merit) + a process note + a drifting-mesh backdrop. **Backend contract notes (verified, no change needed):** list `GET /hr/performance/` accepts only `status`/`cycle`/`employee_id`/`page`/`limit≤100` (NO search/department — hence client-side name search); transition state machine is `DRAFT→{SELF,CANCELLED}`, `SELF→{MGR,CANCELLED}`, `MGR→{SELF,COMPLETED,CANCELLED}`, `COMPLETED→{MGR,ACK}`, `ACK→{COMPLETED}`, `CANCELLED→{SELF,MGR}` (so cancel-instead only offered for DRAFT/SELF/MGR); recommend requires COMPLETED + within band; approve creates an idempotent `APPRAISAL_{id}` comp revision. Added a missing `.perf-btn-steel` definition to `performance-theme.css` (was used module-wide, never styled). `SetCountUp` reused (supports `suffix`/`decimals`). Frontend-only, build green, dark+light (all `--perf-*` tokens, no `mix-blend:screen`), reduced-motion guarded. |
| Performance — Cycles (`/admin/hr/performance/cycles`) | "The Cadence Deck" (2026-06-27) — the Cycles tab was a static card grid (progress ring + bar, hover-lift only, NO signature instrument, NO cross-links). Rebuilt around **`CycleCadence.vue`**: each review cycle is a **LANE on a shared 0→100% maturation track** — a progress **comet** rides each lane (`left` transition), the filled run flows **amber→gold→emerald** as the cohort matures (gradient + `cc-flow` sheen), **per-review tick segments** light up by completion (staggered), and a **maturation seal** (emerald pop) fires at 100%; a vertical **NOW playhead** sweeps the deck, drifting data-independent motes + time-grid floor + 3-tier `usePointerSpotlight` parallax + floating HUD count-ups (cycles / in-progress / matured / avg-done via `SetCountUp`). Lanes are clickable (`@pick` → highlights the matching card). Distinct within Performance — vs the dashboard orrery, insights bell-curve, merit oscilloscope, **reviews flux-pipeline** (a single 4-stage funnel) — the only **multi-lane horizontal maturation-timeline** motif. Section (`PerfCyclesSection`) = console hero with **6 lenses** (All / In-progress / Matured filter + Reviews / Avg-done / Avg-score info) → CycleCadence → cycle-type `PerfSelect` filter → **`CycleCard`** grid (3D tilt + spotlight glare, conic progress ring + maturation seal, status pill, flowing bar, rubric chip → settings, action row: **Open reviews / Merit / Retire**) → a **"cadence pipeline" connectivity bus** (Appraisal-Templates⚙ → Reviews → Calibration → Merit → Merit-Policy⚙). **Workflow truth + loopholes closed (the deliverable):** cycles are **aggregations** of reviews (grouped by `cycle`+`period_label`; the `/cycles` endpoint returns `{cycle,period_label,template_name,total,completed,progress,avg,started_at}` — **no cycle id, no cycle-level mutation**). (1) "View reviews" ignored the cycle → now **deep-links** `router.push({tab:'reviews', query:{cycle}})` and `PerfReviewsSection` reads `route.query.cycle` on mount to pre-filter (the reviews list only filters by `cycle` type server-side, not `period_label` — honest partial filter); (2) zero cross-links → added the pipeline bus + per-card Merit/rubric jumps; (3) **no cycle delete/close existed** → new **`CycleRetireModal.vue`**: on open it **fetches the cycle's reviews** (paged `?cycle=` then client-filters by `period_label`) to compute a real impact breakdown, then offers a **safe "Close" path** (bulk-cancel only the in-flight DRAFT/SELF/MANAGER reviews → CANCELLED, keeps finalised + audit) and a **destructive "Delete" path** (soft-delete ALL) with a **critical applied-hike warning** (quotes that N live salary increments are NOT reversed), reason-required for high-impact, sequential bulk op with a live progress bar (no backend bulk endpoint — done client-side via `deleteReview`/`transitionReview`). Launch reuses the already-cinematic `PerfReviewModal` (mode=launch, lifecycle ribbon). Frontend-only, no backend change, build green, dark+light, reduced-motion guarded. |
| Performance — Insights (`/admin/hr/performance/insights`) | "The Performance Spectrograph" (2026-06-27) — score distribution as a luminous **bell curve riding a poor→outstanding prism**. Signature instrument **`PerfSpectrum.vue`**: 5 band counts → rising peaks (SVG rect `y`/`height` transition) feeding a smooth **Catmull-Rom bell curve** (draw-on via `pathLength="1"` + `stroke-dashoffset` 1→0) over faint prism color-zones (red→emerald via `scoreTone`), an animated **org-average plumb line + HTML flag**, a one-shot **scan sweep**, seeded drifting motes (data-independent), gridlines, HUD (scored/avg) + `usePointerSpotlight` parallax. Distinct from the dashboard's orbiting **PerfObservatory** orrery and merit-policy's **step** transfer-curve — the only **smooth-Gaussian + prism** motif. Section composition: PerfHero + 5 lenses → spectrograph → **Completion ring + review-lifecycle funnel** (segmented `by_status` bar) | **Goal-health conic** (`goals.by_status` donut) → Department **ranking** (rank medals, lead row) | cycle-trend spark → Top-performers **podium** (#1 crown + gold/amber/ember medals) | Needs-attention → **"From rubric to raise" connectivity ribbon** (traveling pulse; 5 stations cross-link Appraisal-templates⚙ → Reviews → Calibration → Merit → Merit-policy⚙) → clickable footer signals. **Loopholes closed (frontend-only):** the **PIPs lens emitted `go:'pip'`** but the tab key is **`pips`** → dead link, fixed; `reviews.by_status` + `goals.by_status` were returned but never visualized → now the pipeline funnel + goal-health donut; footer signals were non-clickable → now `@go`; no cross-links to calibration/merit or to the settings that DRIVE the numbers (appraisal-templates rubric, merit-policy bands) → the connectivity ribbon (in-module via `@go`, settings via `router.push`). Build green, dark+light, reduced-motion guarded. |
| Performance — Merit & Increments (`/admin/hr/performance/merit`) | "The Increment Mint — Merit Treasury" (2026-06-27) — the Merit tab was a flat 3-section table (budget bar + Awaiting/NeedsRec/Applied lists) with ZERO animation and NO signature instrument. Rebuilt around **`MeritMint.vue`**: performance struck into pay. A central **molten BUDGET TREASURY reactor** — a conic `--perf-p` budget ring around a circular **vessel of molten gold** whose liquid FILL = committed/budget (surface waves + rising bubbles), that **OVERFLOWS RED** (liquid → conflict, overflow-lip drip, red emit-rings, red aura) when `over_budget` — the governance limit made visceral; under a **MINTING PRESS** (die strikes on a 3.4s loop → flash + 6-spark radial burst, "armed" glow when pending>0) feeding a **strike line**: raw **ingots** (`needsRec`) → glowing **blanks** (`recommended`, bobbing) → **coins** dropping into a **vault stack** (`applied`, `BadgeIndianRupee` faces, +N overflow) — the three group-clusters are clickable filters (`@pick`). Data-independent drifting motes + vault-wall stripes + 3-tier `usePointerSpotlight` parallax + 4 floating glass HUDs (committed/budget-pool/recommended/over-or-remaining, ember-pulse when over). Unique within Performance — vs the dashboard **orrery**, insights **bell-curve**, reviews **flux-pipeline**, cycles **maturation-lanes**, calibration **9-box**, merit-policy **oscilloscope** — the only **mint / coinage / molten-treasury** motif. Section (`PerfMeritSection`) = `PerfHero` ("Increment **Mint**") + **6 lenses** (In-the-mint / Awaiting-approval / To-recommend / Minted filter the roster; Committed ₹ / Budget-used% info, danger-tinted when over) → cinematic **cycle chips** → MeritMint → state-aware **`MintRow.vue`** roster (3D tilt+glare, score-gauge medallion that morphs to a gold coin when applied, band chip w/ hike-window, status spine ingot→blank→coin, state-aware action) → **"From rating to raise"** connectivity bus (Appraisal-Rubric⚙ → Reviews → Calibration → Merit·here → Merit-Policy⚙). **Loopholes closed (the deliverable):** (1) section was mounted with **no `@go`** (zero cross-links) → wired `@go="selectTab"` in `HrPerformanceWorkspacePage`; (2) the old page could only **nudge to the employee profile** to recommend — now the whole pipeline runs in-Arena via **`MeritStrikeModal.vue`** (resolves the band authoritatively via `fetchReviewMerit`, clamps to the band window, RECOMMEND / APPROVE & APPLY / DECLINE) with a live **"Mint Pass"** coin preview (spinning band-coloured ring, TO-RECOMMEND/TO-MINT/DECLINED stamp); (3) the old quick-approve had **no %/effective-date controls and no downward-override justification** → full `+/−` stepper (clamped, min/mid/max + recommended-↺ chip), `HrDatePicker` effective date, and a **required justification when approving below the recommendation**; (4) bare reject → **reasoned decline** (required note); (5) **ACKNOWLEDGED-with-no-hike dead-end** (recommend 409s on ACK) → modal offers **direct approve** for ACK rows (backend approve-hike accepts COMPLETED *or* ACKNOWLEDGED with an explicit %); (6) **over-budget was invisible** → the reactor overflows red AND the approve modal shows an over-budget exception warning (advisory, never hard-blocks — mirrors the backend, which doesn't). Reuses `bandAtFrac` (meritBands.js, pure math) + `bandTone` (perf colours) to resolve each row's band client-side from the active default Merit Policy for display. **`@property --perf-p` gotcha (hit again):** it's `inherits:false`, so the conic angle MUST be set on the gradient-owning element (`.mm-ring`/`.mr-medal-ring`), NOT a parent — a parent-set value reads as the 0deg initial on the child. Modal teleports to body with `class="…-overlay perf-scope"` so `.perf-scope .perf-btn` + `--perf-*` tokens resolve outside the workspace (the PerfSelect precedent). Frontend-only, no backend change, build green, dark+light (deep warm tones in light, no `mix-blend:screen`), reduced-motion guarded. |
| Performance — Goals & OKRs (`/admin/hr/performance/goals`) | "The North-Star Ascent — Objective Summit" (2026-06-27) — the Goals tab's old hero instrument was an **"Alignment Constellation" (radial orrery)** that REPEATED the dashboard's PerfObservatory orrery, plus native `<select>` filters and a `window.confirm` delete. Rebuilt around **`GoalAscent.vue`**: OKRs as a mountaineering ascent — a topographic massif rises to a glowing **NORTH-STAR beacon** (its shine binds to avg progress), each objective is a roped **CLIMBER MARKER** placed on the slope by its progress altitude (0% base camp → 100% summit, fanned off the rope so they don't stack), tinted by `goalTone`; **ACHIEVED** objectives plant a flag at the peak. A left **ALTITUDE GAUGE** reads avg progress; flowing contour lines, drifting stardust + snow (data-independent → alive when empty), 3-tier `usePointerSpotlight` parallax, hover tooltips, marker-click `@focus` → highlights + scrolls the matching card. Unique within Performance — vs the dashboard **orrery**, insights **bell-curve**, reviews **flux-pipeline**, cycles **maturation-lanes**, calibration **9-box**, merit **mint** — the only **mountain-ascent / north-star-summit / roped-climbers** motif. **Marker↔SVG alignment:** container `aspect-ratio: 1000/380` + SVG `xMidYMid meet` so DOM markers positioned by `left/top %` (= viewBox coord/viewBox ÷ size) sit on the slope (the onboarding LaunchSequence rule — never letterbox-mismatch). Section (`PerfGoalsSection`) = `PerfHero` ("Goals & **OKRs**") + **6 lenses** (Objectives/KRs/On-track/At-risk/Achieved filter; Avg-progress info) → GoalAscent → toolbar (status chips + **`PerfSelect`** cycle, replacing the native `<select>`) → enhanced **`GoalObjectiveCard`** grid (now 3D tilt+glare via `usePointerSpotlight`, KR-track draw-on, trophy at 100%, `active` highlight paired with the ascent) → **"From objective to outcome"** connectivity bus (Appraisal-Rubric⚙ "Goals section type" → Goals·here → Reviews → Insights "goal-health" → Merit). **The New-objective modal rebuilt** (`PerfObjectiveModal.vue`) into a 2-pane **Objective Composer wizard**: a live **"Objective Pass"** preview (mini summit-peak SVG + north-star, owner chip, KR list, DRAFT→READY stamp) + a **4-step flow** (Aim: cinematic employee picker [userinfo] + title + category `PerfSelect` + "Why this matters" rationale [reason] → Key results: builder rows with metric `PerfSelect` + start/target/unit + a `+/−` weight stepper → Schedule: **the exact assets-register `HrDatePicker`** for start & due dates + alignment note → Review: summary + on-create process checklist) with an animated stepper (progress rail + done-ticks) and slide transitions; "Add key result" collapses to a single compact pane. **Loopholes closed:** (1) section mounted with **no `@go`** → wired `@go="selectTab"`; (2) native `<select>` cycle/category/metric → teleported **`PerfSelect`**; (3) native `<input type=date>` → **`HrDatePicker`** (the calendar the user keeps asking for); (4) `window.confirm` delete → cinematic **`GoalDeleteModal.vue`** that surfaces the **KR cascade** (deleting an objective soft-deletes its N key results + check-in history — a consequence the confirm hid); (5) the constellation-repeats-dashboard problem → the unique ascent motif. **Honest connectivity:** the backend supports `review_id` on a goal AND appraisal rubrics have a **GOAL section type**, so goals can be scored as a review's Goals section and feed Insights goal-health — surfaced in the bus + the modal's Schedule step (not auto-linked; the link is set when a review references the goal). Backend `performance_goals.py` unchanged — objectives roll up the weighted mean of their KRs, KRs derive progress from start/current/target, check-ins journal to `check_ins_json`; objective check-in 409s (roll-up only). Frontend-only, build green, dark+light (warm tokens, no `mix-blend:screen`; snow flakes are tiny ambient), reduced-motion guarded. |
| Performance — Improvement Plans / PIPs (`/admin/hr/performance/pips`) | "The Recovery Monitor — Vital-Signs Cardiograph" (2026-06-27) — the PIP tab had a thin avatar-rail "recovery track" (read as "not a single animation") and was an island (no `@go`). Rebuilt around **`PipRecoveryMonitor.vue`**: PIPs as patients on a clinical monitor — a continuously **flowing ECG cardiograph** (procedural PQRST path via `buildEcg()`, `stroke-dasharray` flow + a sweeping vertical **read-head** with pulse dot) over a drifting **topographic contour field**, where every active plan is a glowing **VITAL NODE** plotted by *time-elapsed (x) × objectives-recovered (y)* climbing toward a dashed **recovery threshold** before a **deadline gate**; the trace + read-head **tint amber→red as plans go overdue** (`tone` calm/live/crit), floating glass **vitals HUD** (active/recovered/overdue-alarm/success-rate count-ups), drifting parallax (3-tier `usePointerSpotlight`), data-independent ambient (trace runs idle at 0 plans). Unique within Performance — vs orrery / bell-curve / flux-pipeline / maturation-lanes / 9-box / mint / mountain-ascent / convergence — the only **medical vital-signs / ECG-cardiograph** motif (combines the reference decks' flowing line-field + activity tick-bar + mood-sentiment + heatmap into a monitor). **motion-v node gotcha:** vital nodes are `<Motion>` animating `scale`, so they're centred with **negative margin (`-16px`), NOT `transform: translate(-50%,-50%)`** — motion-v overrides transform and would drop the offset (the documented entrance↔transform conflict). Section (`PerfPipSection`) = `PerfHero` ("Improvement **Plans**") + 5 lenses → monitor → **command strip** (Recovery-sentiment segmented mood bar from latest check-in rating per active plan incl. an **"Unmonitored / going dark"** governance segment | Outcomes conic success-rate ring + recovered/unsuccessful/cancelled) → status chips → cinematic **`PipCard.vue`** grid (3D tilt+glare, status spine, a per-plan **vital sparkline** [ECG of check-in sentiment, seeded fallback], objectives-met + recovery-window meters, **"From review" provenance chip**, manager chip) → **"From rating to recovery"** connectivity bus. **The New-plan modal rebuilt** (`PipModal.vue`) into a 2-pane console: a live **"Recovery Brief"** aside (DRAFT/ACTIVE/outcome stamp, employee, recovery-pulse sparkline, objectives meter, window countdown, manage-mode provenance+manager) + a **4-step CREATE wizard** (Who: cinematic picker + title + **optional "Triggered by review" `PerfSelect`** populated from `fetchReviews({employee_id})` → Why: reason/expectations/support → Objectives: shared **`PipObjectivesEditor.vue`** builder → Schedule: **`HrDatePicker`** start/end + Draft/Active segmented + on-create process checklist) and a polished MANAGE workbench (lifecycle stepper + objective status pills + check-in timeline w/ sentiment-tinted ratings + outcome + transitions; two-stage delete). **Connectivity (the deliverable — surfaced, all real):** the genuine live wire is **Merit-Policy → PIP**: when a manager recommends a hike (`recommend_hike` in `performance_self.py`), a band with **`auto_pip:true`** (Settings → Merit-Policy) **auto-spawns a DRAFT PIP linked to the triggering review** (`review_id`, `PIP — {period}`) — the page now surfaces that **provenance** (card + modal "From review" chip → `@go reviews`) and the full chain in the bus: **Appraisal-Rubric⚙ → Reviews → Merit band⚙ (auto-PIP) → Plans·here → Outcome** (Outcome flagged honest: an UNSUCCESSFUL PIP is the documented basis for further action but is **NOT auto-wired to the Exit module**). Loopholes closed (frontend-only): `@go="selectTab"` wired; `review_id`/`manager_id` were `PipCreate`-supported but never exposed → review-link picker + manager auto-note; provenance was hidden. Backend `performance_pip.py` unchanged (DRAFT→ACTIVE→(EXTENDED)→SUCCESSFUL|UNSUCCESSFUL+CANCELLED; objectives OPEN/MET/MISSED; check_ins; soft delete). Build green, dark+light (`--perf-*` tokens, no `mix-blend:screen`), reduced-motion guarded. |
| Performance — My Performance (self-service, `/user/self-service/performance`) | "The Growth Atrium — Standing & Ascent" (2026-06-27) — the employee's own performance page (distinct from the manager's [[team_performance_squad_deck]] SquadReactor surface). The old page was a flat hero + generic cards with no per-component motion. Rebuilt around a **bento command-deck hero** pairing TWO instruments: (a) the kept **`GrowthTrajectory`** ascent line, and (b) a NEW signature **`StandingHalo.vue`** — a **semicircular merit-band gauge**: the company merit spectrum (Below→Partial→Meets→Exceeds→Exceptional, `DEFAULT_MERIT_BANDS` arcs drawn on a half-circle via an `arc(f0,f1)` SVG-arc helper, sweep=1 over the top) with an **animated needle** (`transform-box: view-box; transform-origin:140px 140px`, armed-after-mount so it sweeps from rest, `needleDeg=(f-0.5)*180`) pointing to exactly where the employee's latest *scored* review stands; rotating conic glow + breathing aura + drifting motes + sweeping read-head shimmer + count-up score core + active-band highlight + band legend. This **makes the merit-policy connection visible to the employee** (previously invisible). Unique within Performance — vs dashboard orrery, insights bell-curve, reviews flux-pipeline, cycles maturation-lanes, calibration 9-box, merit mint, goals ascent, PIP cardiograph, team SquadReactor — the only **semicircular banded needle-gauge**. Page (`SelfServicePerformancePage`) = command-deck hero (mesh+orbs+grid+scan parallax, gradient-accent greeting w/ name from `/auth/me`, whoami chip, StandingHalo+GrowthTrajectory bento, 4 count-up tiles incl. an "Awaiting you" alert) → manager CTA → **action banners** (reflections-in-progress + ack-needed) → 360°-feedback-to-give (motion-v stagger) → **split** (reviews main / goals side with conic progress rings) → new cinematic **`SsReviewCard.vue`** (3D tilt+glare, conic score gauge, status spine w/ live pulse, ack-needed bell flag, band/score-word chip, **reporting-manager avatar footer**, hike chip honouring the APPLIED-only %-disclosure policy). **Bug fixed (the deliverable):** the employee's drawer surfaced only their *own* self-reflection — the reporting manager + their assessment were buried in a tiny conditional block. `SsPerfReviewDrawer.vue` rebuilt so the **reporting manager is first-class**: a dedicated **reviewer identity** card ("Owns your score") + a prominent **Manager's-assessment verdict** card (reviewer avatar + summary, or an explicit "awaiting" state when not yet finalized) — the employee now always sees who reviews them and what they said. (Manager→sees-employee-self-reflection was already correct in `TeamPerfConsole` zone 1 "Employee reflection".) **Connectivity (no loophole):** ↔ appraisal-templates (reviews carry snapshotted sections/scale, read-only) · ↔ merit-policy (band/% derive from the policy bound at launch; now VISUALIZED by StandingHalo) · ↔ admin/hr/performance (same `PerformanceReview` rows; reflection + ack write back). Employee never self-rates; ack gated to COMPLETED; reflection blocked on closed reviews. Frontend-only, no backend change, build green, dark+light (all `--perf-*` tokens), reduced-motion guarded. **HERO PIVOTED 2026-06-27 (user: hero components all showed appraisal data, wanted Improvement-plan / Feedback-to-give / Goals-&-OKRs instead):** StandingHalo + GrowthTrajectory + the 4 appraisal tiles were pulled OUT of the hero (components still in `performance/components/` but unused on this page; appraisal "My reviews" stays in the body). New signature **`GrowthRings.vue`** — three concentric **Apple-Watch-style activity rings** (the only ring-trio in Performance; outer=Goals avg-progress · middle=Feedback given/total% · inner=Improvement where "no PIP"=full emerald on-standing, active=objectives-met%, ack-needed=red ping); animated stroke-dashoffset draw armed-after-mount, glowing leading tip, center count of `growthActions` (feedbackPending+pipToAck+goalsAtRisk) else "All clear", legend rows cross-highlight arcs + `@go` smooth-scroll. Right of bento = three color-keyed (`--lc`) **`.ssp-lens` domain cards** each with a DISTINCT mini-visual (Goals=per-goal vertical bars · Feedback=avatar dots filled/hollow-pulse · Improvement=objective pips/clear) → click `goTo()` scrolls to `[data-ssp-anchor]` body sections. Lead `cyclePill`→`focusPill` (growth-actions summary). Activity-ring trio is now a CLAIMED motif — don't repeat for sibling pages. |

Fresh ideas to mine from libraries (§ `21st-dev-libraries.md`): aurora/beams background, sonar ping grid, ticker odometer, bento mosaic, marquee ribbon, orbiting circles, depth parallax stack, waveform/equalizer, ismetric stack, flow-field particles.

## 5. Reusable components & helpers (import, don't rebuild)
- `components/AssetModal.vue` — teleported glass modal (aura + sheen edge).
- `components/AsSelect.vue` — the module's modern dropdown (teleported popover, dot/icon options).
- `components/AssetField.vue` — labeled input that delegates `type="select"` → AsSelect.
- `components/AssetCountUp.vue` — eased count-up, in-view gated.
- `components/AssetEmptyState.vue`, `AsStamp.vue` (status pills), `AssetStatusStamp` / `AssetTypeBadge`.
- `@/components/hr/forms/HrDatePicker.vue` — the calendar; `HrSelect.vue` for HR-scoped forms.
- Reusable instruments worth generalizing: `MaintGear.vue` (procedural cog), `AuditScanGrid.vue` (muster matrix), `AuditReconRing.vue` / `DisposalCrucible` (conic gauges), `VendorConstellation` (orbital map).
- `@/composables/useShiftMotion.js` → `prefersReduced()`, `useInView(elRef)`, `usePointerSpotlight(elRef)`, `seededWave()`.
- Workspace wiring: sections emit `@go` (`selectTab`, accepts `{tab, filter}`) + `@detail` (`openHistory(assetId)`); deep-link filters flow `selectTab {tab,filter}` → `:initial-filter` → `@consumed` one-shot.
- **Pagination**: per-module reusable animated pagers — `TrvPager.vue` (travel), `LedgerPager.vue` (assets), `ExPager.vue` (exit). All share the shape: windowed page pills (1 … current±1 … last) with spring-popped active pill + pulse ring, prev/next + jump-to-edge, a live "Showing a–b of N" count-up readout, and a progress rail. Props `page/totalPages(or pageCount)/total/limit`, emits `update:page`. **10 records/page** is the module default. For server-paged lists use the module's `useXxxCases({limit:10})` factory; for "load-all-then-slice" sections use `useClientPage(sourceRef, 10)` from `useExit.js` (returns `{page,total,totalPages,paged,reset}`, auto-clamps page when a filter shrinks the source — reset `page=1` in a `watch` on the filter/lens). Horizontal master-detail roster strips paginate too (don't let them grow into an endless scroll).

## 6. Tokens (warm gold/amber brand)
Assets `--as-*` (see `src/styles/asset-theme.css`): `--as-canvas/dome`, `--as-surface[-elevated]`, `--as-panel`, `--as-border-soft/strong`, `--as-amber/-bright/-strong`, `--as-ember[-deep]`, `--as-steel[-dim/-dark/-hi]`, status `--as-st-{available|allocated|reserved|maintenance|retired}[-soft]`, condition `--as-cond-*`, `--as-grad-hero/-rail`, `--as-card-shadow[-hover]`, `--as-glass[-deep]`, `--as-spring`/`--as-ease`, `.as-mono`. HR uses `--hr-*` (`hr-theme.css`).
Semantics: **warm gold/amber = brand**, emerald = success, red = danger, steel/grey = neutral. **No blue/purple/indigo/teal accents.**

## 7. Gotchas (all learned the hard way)
- **`motion-v`** animates transform **shorthands** (`scaleX`, `y`, `scale`, `rotate`) — NOT `transform:'scaleX(0)'` strings. Import `{ Motion, AnimatePresence as Presence }` from `'motion-v'` (it's `AnimatePresence`, not `Presence`). Never import from bare `'motion'`.
- **`@property --p { syntax:'<angle>'; inherits:false; initial-value:0deg }`** is required for a conic-gradient angle to *transition* smoothly; declare it in any component whose ring tweens, and set `--p` inline on the owning element.
- **3D tilt + entrance conflict:** entrance transform on an OUTER shell, tilt transform on the INNER element — never both on one node (CSS `animation` fill / `motion-v` inline transform will fight `:hover`).
- **Teleported popovers** (AsSelect/HrDatePicker) sit above the modal (z 5000–5200 vs overlay ~1440) — good; if a popover lives inside a `v-click-outside` parent add `@mousedown.stop`.
- **lucide-vue-next** in this repo: some names differ — `FilePen` (not `FileEdit`), `FilterX` works as a named export even though no `filter-x.js` file, `ChartPie` (not `PieChart`). Verify with `ls node_modules/lucide-vue-next/dist/esm/icons/<kebab>.js` before importing exotic icons.
- **Light theme is not optional:** every hardcoded color needs a `[data-theme="light"]` path; prefer tokens so it's automatic. Test the toggle.
- **`mix-blend-mode: screen` + pale gradient stops = INVISIBLE on the cream light theme** (hit across every exit hero — dashboard ribbons/particles, asset-bay flux, etc.). Screen *lightens*, so a glow built for a dark stage vanishes on `#f6f2ec`. Likewise near-white strokes (`rgba(255,247,230,…)`) and bright-yellow stops (`#fcd34d`/`#fbbf24`) read on charcoal but disappear on cream. Fix per element in light: `[data-theme="light"] .x { mix-blend-mode: normal; }` + swap to a **deeper gradient** (`#ea580c`/`#c2410c`/`#b45309`, emerald `#059669`/`#047857`) — add a parallel `…Light` `<linearGradient>` in `<defs>` and override `stroke`/`fill` (CSS `stroke`/`fill` beat the presentation attribute; use `!important` only if the value is set inline via `:style`). Always toggle the theme on any glow/flux/particle instrument before calling it done.
- **Reduced motion:** wrap every keyframe/loop in `@media (prefers-reduced-motion: reduce)`; `prefersReduced()` gates JS-driven motion.
- **Verify build:** `node ./node_modules/vite/bin/vite.js build --logLevel error` (no `lint`/`test` scripts exist). Backend is Vue-agnostic; most redesigns need **no backend change** — confirm before editing Python.

## Support Desk — My Tickets (`/{admin/support-desk,user/support}/tickets/my`) (2026-06-29)
"The Resolution Orbit" — the personal queue as a living orrery. Signature instrument **`SdResolutionOrbit.vue`**: my tickets orbit a central **SLA-health core** (conic `--sd-p` ring = % resolved + avatar/readout + on-track/at-risk badge) on three concentric **status rings** (inner = Open/In-progress, mid = Pending/Hold/Escalated, outer = Resolved), each droplet **priority-tinted** (`priorityColor`), breaching/escalated ones pulse a halo; pointer-parallax on aura/rings; data-independent ambient motes so it's never empty; click a droplet → focus the ticket. Pure-CSS orbit (NOT SVG): `.orbiter` = margin-centered rotating wrapper (`sd-spin-slow var(--dur)`, negative `animation-delay` distributes phase), `.drop` pinned at `top:0`. **Gotcha caught:** the rotating wrapper must be `pointer-events:none` with only the small `.drop` button `pointer-events:auto` — else each ring-sized wrapper is a giant invisible click target stacked over the whole orbit. **`--sd-p` is @property inherits:false** → set it on the gradient-owning element (`.core-ring`/`.sla-ring-fill`) itself, never a parent. Composition: **`SdMyDeskConsole.vue`** hero (greeting + gradient accent + Raise-a-ticket/Refresh CTAs + **panel-aware telemetry lenses** that filter client-side) over the orbit → search/sort toolbar → cinematic **`SdMyTicketCard.vue`** rows (status spine, live conic **SLA-countdown ring** driven by a 1s `now` tick, 3D pointer-tilt + spotlight glare, type glyph, "awaiting you"/"unassigned"/"breached" attention badge, `sd-deal` stagger) → animated dot-pager → drawer. **Panel split (admin gets more control):** employee → conversational `SdMyTicketDrawer` + lenses All/Open/Pending/Resolved + reply/rate; admin → full agent console `SdTicketDrawer` + lenses All/Open/Pending/At-risk/Escalated/Resolved + inline quick actions (assign-me/escalate) + raised-by shown. Both panels share `listMyTickets` (`/me/tickets` = raised-by-me OR assigned-to-me, agent-enriched), fetched once (limit 100) and filtered/sorted/paged 100% client-side for instant lens switching. The section detects panel via `route.path.startsWith('/user')` (no prop plumbing — kind 'self' passes `{}`). Raise-a-ticket wizard **`SdTicketCreateModal.vue`** rebuilt panel-aware: 4-step admin (Classify type-chip-grid + priority severity bar + category → Describe subject/reason/tag-chips → Context org/contact/source/SLA/assign-me → Review) / 3-step employee (no Context — backend forces source/contact); each step a keyed `<Motion>` slide; live **"Support Pass"** preview aside (DRAFT→READY stamp, priority pill, barcode foot, shine sweep) + requester/agent VERIFIED card; branches endpoint `createTicket` (admin) vs `createMyTicket` (self). `SdSelect` upgraded to support `{value,label,icon?,dot?,desc?}` + flip-up + AnimatePresence. Distinct from siblings — the only **orrery/orbiting-droplets** motif (≠ ops command-deck reactor, calm self-console, the Zendesk-grade `SdTicketListSection` table, the basin theme).

**Reusable gotcha — async section + `<transition mode="out-in">` = blank until reload.** `SupportModuleWorkspace.vue` put the `defineAsyncComponent` `<component :is>` DIRECTLY as the out-in transition's child → the transition animated the async loading-placeholder (a comment node) and the resolved section never entered until a forced re-render (the user's "clicking the in-page menu doesn't load the page unless I reload" bug). **Fix = wrap the dynamic component in a STABLE keyed element** (`<div class="sw-canvas" :key="tabKeyId">…<component/></div>`), exactly like the working `HrLeaveWorkspacePage` pattern (`<section :key="activeTab">`). The transition then animates a real element and the async resolves inside it. Frontend-only, no backend change, build green, dark+light.

## Support Desk — My Tickets v2: the AGENT WORKBENCH (2026-06-29, SUPERSEDES the Resolution Orbit)
User rejected v1 (Console + orbiting-droplets) as "basic, repetitive, HR-like, not advanced" and supplied a full ServiceNow agent-workbench spec. **Lesson: for a daily-execution surface (an agent lives in it all shift), a decorative hero reads as "basic" — they want a DENSE, PRODUCTIVE grid workbench with cinematic polish, not a pretty centerpiece.** New layout (their spec): Header(rich action bar) → KPI bento → Smart-Insights bar → search+quick-pills toolbar → data grid → floating bulk bar. Signature instrument = the **Workload Reactor** (`SdWorkbenchKpis.vue`): a 0-100 conic `--sd-p` gauge (tone calm→busy→heavy→overloaded, breathing core, dashed orbit ring) as the hero KPI tile, surrounded by animated count-up bento (Open/Pending/SLA-Risk/SLA-Breached/Resolved-Today, click→filter, breach tile pulses). Distinct from the orbit (deleted) AND from sibling reactors — it's a single *load* gauge, not orbiting tickets. Other new pieces: `SdInsightTicker.vue` (auto-rotating heuristic "Smart insight" bar, severity rail + action button, AnimatePresence mode=wait — pauses on hover), `SdTicketGrid.vue` (the centerpiece: localStorage-persisted configurable columns + customize-cols popover, header-click sort, multi-select w/ select-all, **hover-reveal row quick-actions**, a **live conic SLA-countdown cell** driven by a 1s `now` tick, **critical-row left-spine pulse**, **row-flash keyframe when a row's `updated_at` changes** after an action, density toggle, horizontal scroll), `SdAdvancedFilterDrawer.vue` (480px right drawer: status/priority chips + impact/urgency/SLA/category/org/date/unassigned/escalated, live active-count), `SdWorkbenchActionDrawer.vue` (ONE mode-driven 440px drawer doing resolve/escalate/note/bulk-assign/bulk-status/bulk-priority — consolidates 6 drawers; resolve = ITIL code+root-cause+summary+time-pills+notify+close). Orchestrator `SdMyTicketsSection.vue`: a floating **bulk bar** (slides up via AnimatePresence when rows selected; assign/status/priority/escalate/resolve/**merge**[folds dups into earliest-created via a `mergeTicket` loop]/export), **saved views** (localStorage + 5 default views), CSV export built client-side from the filtered set (matches what's shown). **Role-adaptive (the key product decision):** ONE workbench on BOTH panels; `caps = admin?{agent,manage}:{agent:detectSupportAgent()}` gates row/bulk actions; agent → full `SdTicketDrawer` console on open (+ fire-and-forget `markTicketViewed`), non-agent employee → `SdMyTicketDrawer` chat. Both fetch a 100-row working set + a server `WorkbenchStats` aggregate, then filter/sort/select 100% client-side (instant). **Backend depth shipped alongside** (the "incomplete backend" complaint): migration `add_ticket_workbench_columns.py` (sub_status/impact/urgency/resolution_*/time_spent_minutes/merged_into_id/last_viewed_at) + `resolve`/`merge`/`time`/`viewed`/`workbench` endpoints + `app/utils/support_desk/workbench.py` heuristic engine whose `_insights()` is an explicit **Claude-ready seam** (swap rules for an LLM, same `WorkbenchInsight` contract). **`GET /workbench` MUST be declared before `GET /{ticket_id}`** (a UUID path-param would 422 on "workbench"; literal-first wins, like `/calendar`+`/export`). **`--sd-p` @property inherits:false** again — set it on each conic gauge element itself (`.rx-arc`, `.sla-ring`), never a parent. Heuristic insights chosen over LLM (Q&A'd): instant, free, deterministic. Distinct from every sibling — the only **agent-workbench / data-grid + workload-reactor** surface. Build green, dark+light, reduced-motion guarded, backend JWT-probed 200.

## Support Desk — Create Ticket: "The Intelligent Intake Engine / Crystallization" (2026-06-30)
Rebuilt `sections/SdNewTicketSection.vue` (`/{user/support,admin/support-desk}/tickets/new`) from a bare 4-field form (zero animation — the user's exact complaint) into a cinematic **3-panel enterprise intake console** (ServiceNow-grade). Signature metaphor = **CRYSTALLIZATION** — a raw support *signal* forms into a structured, SLA-bound ticket (no Support Desk sibling uses it). Three NEW instruments: **`SdIntakeCrystal.vue`** (left panel) = a forming faceted **SVG gem credential** — 6 brilliant-cut facets ignite progressively as fields fill, an SVG charge-ring (`stroke-dashoffset`) fills to completion %, a formation **ledger** checks off, a READY **seal-burst** + sheen on submit-ready, 3D pointer-tilt + spotlight glare; the gem fill is the **priority token** via `color-mix(in srgb, var(--gem) …)` so it's light-theme-correct and re-tints with priority. **`SdPriorityMatrix.vue`** (classify section) = the charged **4×4 ITIL Impact×Urgency grid** — crosshair beams positioned by `--sel-r`/`--sel-c` (CSS `transition` as they move) sweep to the computed P-cell which ignites + pings, one-shot scanline, live derived **P1–P4** readout; controlled (`:impact`/`:urgency` props, emits `pick({impact,urgency,priority})`); mirrors the backend `IMPACT_URGENCY_MATRIX`. **`SdIntakeCopilot.vue`** (right panel) = live **"Intake Intelligence"** (echoes the attached workflow-builder + knowledge-base reference shots) — **deterministic client-side heuristics** (classify type/priority/category with one-click **Apply** chips, a sentiment meter, **duplicate-match** vs the requester's recent `listMyTickets` by token-Jaccard → click-to-open) + **REAL KB** suggestions via `listMyKb({q})` (debounced; the backend `?q` title `ILIKE` — the ONLY backend call the copilot makes) + an SLA forecast. The heuristics live in `analyse()`-style blocks = the explicit **LLM-swap seam** (same heuristics-over-LLM call this module already made for the workbench `_insights`). Composition: sticky **header** (`top: var(--nav-h)` where `--nav-h:52px` clears the **fixed 52px TopNavBar** — `.top-nav{position:fixed;top:0;z-index:1000}`, the WINDOW scrolls under it; side panels sticky at `+head-h+12`, `head-h` measured via ResizeObserver) carrying a draft-# chip + a **charged conduit progress** (nodes + flowing `--sd-grad-rail`, **scroll-spy** via IntersectionObserver `rootMargin:'-30% 0 -55%'`, click-to-scroll with `scroll-margin-top`) + Save-draft/Discard/Create → 3-col body [sticky **crystal** + verified-requester card + "what happens next" automation card + recent-tickets · scrolling numbered **section cards** (`sd-deal` stagger via `--i`) · sticky **copilot**]. **Panel-aware:** `/user`→employee (`createMyTicket`, sections Requester(read-only identity)/Describe/Classify/Attach/Review) / else agent (`createTicket`, adds a Routing&SLA section: org/contact/source/sla/assign). **Connectivity — loophole closed (one canonical create):** the rail "New Ticket" (`onNew`), My-Tickets "+ New" (`@new="goNew"`), and the `?new=` deep-link (`fireCreate`) ALL now `router.push('${base}/tickets/new')`; the legacy quick-create `SdTicketCreateModal` is left mounted but **inert**. Success → confirmation modal (real `ticket_number`/priority/SLA from `response_due_at`/`resolution_due_at`) → "View in My Tickets". **Save Draft = localStorage** (`sd.intake.draft.<panel>` + restore banner on mount; there is no backend draft endpoint — honest). **Backend gotchas to remember:** `TicketCreate` is Pydantic `extra='ignore'` (send unknown fields freely, NO 422); `TicketPriority` has **5** members incl. `urgent`, `TicketType` has **9** (a sub-agent misread them as 4/4 — always confirm enums in `app/models/support_desk/constants.py`); self-service create FORCES source=portal + contact/raised_by from the user and IGNORES org/assignment, so the employee page only persists subject/description/ticket_type/priority/impact/urgency/category_id/subcategory_id/tags/attachments. Distinct from every sibling — the only **forming-credential (crystallization) + charged priority-matrix + AI-copilot intake** surface (≠ basin / ops-reactor / workbench workload-reactor / orbit / globe / ribbon / triage-field). Build EXIT=0, dark+light, reduced-motion guarded (`html:not([data-cinematic="on"])`).

## Support Desk — Create Ticket v2: STEPPER WIZARD + tall hero + capability mode (2026-06-30, SUPERSEDES the scroll-spy crystallization layout)
User rejected v1 as "one big scroll" and wanted **discrete on-page steps without window scroll**, a **tall dramatic animated hero**, **client clarity** ("which client am I raising for?"), and **assignment rules**. **Key correction they gave: `/user/support/tickets/new` is the AGENT portal** (not a generic employee form), `/admin/support-desk/tickets/new` is admin, `/support/portal` is the end-client. So the page now branches on **capabilities, NOT path**: `fetchCapabilities()` → `mode = is_agent ? 'agent'(createTicket) : 'self'(createMyTicket)` (the old `isAgent = path==='/admin'` was wrong — an agent on `/user` got the slim form). New layout: **tall `SdIntakeHero.vue`** (signature = the **INTAKE STREAM** — flowing SVG conduits converge into a forming ticket-hex glyph that locks emerald when submit-ready + drifting motes; hosts title/draft-chip/actions + the stepper as its spine; fresh motif, ≠ crystal/basin/reactor/orbit/globe/ribbon/triage-field/matrix) → **fixed-height 3-col body** `[SdIntakeCrystal (kept) | one-step stage | SdIntakeCopilot (kept)]` where the **center shows ONE step at a time** via `<Presence mode="wait">` + keyed `<Motion>` slide (dir-aware), Back/Next footer gates forward on subject-validity, the stepper (`SdIntakeStepper.vue` — charged conduit, igniting/active-pulse nodes, click-to-jump ≤ reached) navigates; the **window never scrolls** (`.itk-body height: calc(100vh - nav - hero-h - 60px)`, hero-h via ResizeObserver, each column `overflow-y:auto` internally). **Canvas tint removed** — dropped the old `.itk-header var(--sd-canvas)` band + heavy aura so content sits directly on app `--bg-color` (DashboardLayout main was already transparent). **Client clarity:** agent step-1 has an org `SdSelect` + an animated **"ON BEHALF OF [Client]"** banner (support_plan/code chips); self mode = read-only "Internal request" identity card. **Assignment (`SdRoutingPreview.vue`):** calls new `GET /me/tickets/routing-preview` (debounced) → shows the team the ticket WOULD route to + **real SLA forecast** + `you_are_on_team`/`can_self_assign`; surfaces **"You're on the [team] team that handles this" + Assign-to-me** toggle (gated to agent/handling-team-member); **team + agent routing pickers reveal ONLY for `isManager||isAdmin`** (the rule: only reporting-manager/admin route to a different team). Agent feature-expansion step (`context`): business_impact / affected_users / revenue_impact / vendor_name / linked_change / linked_problem. **Backend (Phase 1, JWT-probed 200):** `match_route(db,t)` read-only matcher extracted from `route_and_assign` (no writes) powers the preview; `routing-preview` endpoint declared **before `/{ticket_id}`** (literal-first); `TicketCreate` gained `assign_me` + team_id/queue_id/collaborators/business_impact/affected_users/revenue_impact/vendor_name/linked_change_id/linked_problem_id/links (all already on the model/`TicketUpdate`); `create_my_ticket` honors `assign_me` **only when `_can_work`** (agent/handling-team-member); `create_ticket` persists the new fields + a **team-routing guard** (non-superuser/non-manager/non-team-member who supplies team_id has it dropped → auto-route decides). New components: `SdIntakeHero` / `SdIntakeStepper` / `SdRoutingPreview`. Build EXIT=0; backend health 200 + capabilities/routing-preview authenticated 200 (matched team "Tier 2" + "Standard SLA" ETAs). Reduced-motion + dark/light guarded.

## Support Desk — Ticket Drawer: ONE adaptive "Resolution Console" (2026-06-30, MERGES the two drawers)
User flagged a "very big bug": on the **agent portal** (`/user/support/tickets/my`) the drawer opened as the *client/requester* CSAT chat instead of an agent drawer. Root cause was a **two-drawer capability split** repeated on every surface (`v-if caps.agent → SdTicketDrawer (agent, /support-desk/tickets/*) v-else SdMyTicketDrawer (requester, /me/tickets/*)`) — a viewer whose `is_agent` was false on the agent portal fell to the client drawer. Backend reality: `is_agent = is_superuser OR is_support_agent`; `GET /support-desk/tickets/{id}` (`get_support_agent`) **403s for non-agents**, so the agent drawer can't blanket-replace the requester one. User chose (AskUserQuestion): **"one adaptive rich drawer."** **`SdTicketDrawer.vue` rebuilt as a SINGLE capability-adaptive drawer used everywhere** — resolves `isAgent` from `props.caps.agent` (when passed) ELSE `route.path.startsWith('/admin') || useCapabilities().isAgent` (self-detects via cached `fetchCapabilities()` when mounted with no caps, e.g. the `?ticket=` deep-link); **branches its data source** `isAgent ? getTicket : getMyTicket` (+ fire-and-forget `markTicketViewed` for agents) and every mutation by persona — **agent** (full console + edit/vendor + `/support-desk/*`), **worker** (`viewer_can_work`: route via `/assignees`→managerAssignTicket, collaborators, internal-note reply, resolve, reopen via `/me/*`), **requester** (public reply, edit-if-open, withdraw, reopen, **CSAT rate**). Resolve/Close/Withdraw/Archive reuse `SdResolveModal`/`SdCloseModal`/`SdWithdrawModal`(`:agent=isAgent`)/`SdTicketDeleteModal`. **Design (matched the user's cited HR `LeaveBalanceAdjustDrawer` + `TravelDetailDrawer` bar):** Teleport+Presence slide-in (`x:'100%'`, 0.46s `[.16,1,.3,1]`); a **status-reactive left spine** (`spineColor` by status, red when breached) + two drifting ambient **orbs** (`tkd-drift-a/b`, Leave-style); compact **sticky header** (number/flags/**role-badge**[agent·worker·requester]/subject/pills/animated banner); **signature instrument = the "Resolution Console"** (NEW motif, ≠ basin/triage-field/globe/ribbon/workload-reactor/orbit and ≠ SdMyTicketDrawer's dual conic ORBS): a **focal live time-to-breach countdown** (dashed spin-ring, tone ok/warn/breach/met) + **dual SLA frontier bars** (flowing fill + 80% warn-tick + breach-pulsing comet, off a 1s `now` tick) + a horizontal **lifecycle filament** (Logged→1st Reply→Working→Resolved→Closed nodes on a flowing `--sd-grad-rail` rail, last-reached node beacon-pulses); then a Travel-style **vitals deck** (Age/Logged/Replies + contextual CSAT|Reopens|Esc|People count tiles); staggered `sT(n)` Motion sections; Conversation/Details/Activity tabs (translateX ink); **sticky action dock** (capability-gated motion-v `whileHover/whileTap` buttons) with **slide-up action sheets** (hold/reopen/remind/incident/rca/route/collab via `<Presence>`) + an agent "More" menu. Surfaces extra fields the old one hid: `sub_status` pill, `last_viewed_at`, sla-policy line, `merged_into_id` chip, priority **P-code**, links (task/problem/change). **Wired into all 4 surfaces** — `SdMyTicketsSection` (`:caps :me`), `SdTicketListSection` (per-scope tabs, `:caps="{agent}"`), `SdSelfDashboard` (self-detect), `SupportModuleWorkspace` `?ticket=` (self-detect). **`SdMyTicketDrawer.vue` is now fully ORPHANED** (no importers; left in place, safe to delete). **3-portal terminology (the user's):** `/user/support` = AGENT portal · `/admin/support-desk` = ADMIN portal · `/support/portal` = CLIENT portal **(deferred — build last)**. **Init-order gotcha:** the drawer's `immediate` `watch(ticketId)` calls `resetTransient()` which touches refs declared lower in `<script setup>` → declare the watch **last** (after all refs) or it TDZ-throws if ever mounted with a non-null id. Build EXIT=0 (4735 modules), dark+light, reduced-motion `html:not([data-cinematic="on"])` guarded.

**Drawer v2 (same day, after live screenshots — IMPORTANT corrections):** (a) **Don't adapt the drawer mode by the viewer's `is_agent`** — the user's model is `/user/support` AND `/admin/support-desk` are BOTH agent portals, so **force the agent console on both** (`onAgentPortal` route check → `wantsAgent=true`), and on a `getTicket` 403/404 fall back to `getMyTicket` + `agentDenied` (graceful degrade). The agent console on /user still needs superuser/`is_support_agent` (backend enforces) — flag the test user. (b) **Action forms must be MODALS, not inline slide-up sheets** — user called the sheets "shitty"; built `modals/SdAgentActionModal.vue` (one corporate mode-driven modal for hold/reopen/remind/incident/rca/route/collab — morphing seal + eyebrow + ticket chip + workflow strip + reason chips + toggle + people pickers, SdResolveModal-class). (c) **Define EVERY button class** — `.tk-btn` referenced by the composer Reply + CSAT Submit was never defined → unstyled buttons (user noticed instantly). (d) Gate CSAT `!isAgent` (never in the agent console). (e) A translucent sticky drawer header lets scrolled content "peek/cut" → make it **opaque** (`surface` over `--sd-basin`). **VERIFIED LIVE headless** (minted superuser JWT, `NODE_PATH=<proj>/node_modules node script.cjs` so the scratchpad script resolves `playwright-core`, deep-link `?ticket=<id>` to open the drawer): AGENT CONSOLE badge + full dock + modal opens + no CSAT on resolved + Reply styled + zero console errors; admin parity confirmed.

**v2.1 refinement pass (2026-06-30, same day — user feedback on the live page):** (1) **Source** gained `whatsapp` + `monitoring` (added to `TicketSource` enum → `_SOURCES` auto-updates; frontend `SOURCES` list). (2) **Full Requester Information always shown** (the self/agent field-hiding was wrong — user saw a bare identity card): step 1 now renders Organization*/Source/Contact*/Email/Phone/Department/Location + a "Raised by you" identity strip for EVERYONE, and `create_my_ticket` was extended to persist organization_id/customer_id/contact*/department/location/source/business-impact/related (was forcing internal + dropping org). (3) **Team-membership gate (the hard rule):** if the classification routes to a real team and the requester isn't on it (and isn't an admin), they're BLOCKED — `SdRoutingPreview` shows an ultra-modern red "You're not on the [team] team" lock, the section's `teamBlocked` computed disables Continue/Create, AND `create_my_ticket` enforces it server-side (`match_route` + `_can_work`, 403 with a guiding message) so the API can't be bypassed. Tickets matching NO team go to triage (allowed); agents/admins bypass. (4) **sd-mx live-sync bug fixed:** applying a priority (copilot Apply OR the priority bar) left the Impact×Urgency matrix blank because it's impact/urgency-driven — added `PRIORITY_TO_MATRIX` + `setPriority()` that lights the canonical cell; `onMatrix` stays the source-of-truth so there's no feedback loop. (5) **Hero overlap fixed + bigger/more cinematic:** `SdIntakeHero` restructured into 3 non-overlapping zones (top controls / a dedicated **signature lane** where conduits flow into a forming reactor core / the stepper) — the forming-hex no longer collides with the action buttons; taller hero; richer motion (rotating dashed rings, travelling `offset-path` packets, breathing aura) + a dramatic **ready LOCK** (hex scales emerald, tick draws, ring-burst + sheen sweep). (6) **Ultra-modern scrollbars** scoped to the page's scroll surfaces (`scrollbar-width:thin` + ember→amber `::-webkit-scrollbar-thumb`). (7) **Discard → ultra-modern modal** (`SdModalShell`: pulsing danger seal + reason chips + "what happens" workflow strip + danger confirm; silently resets if nothing entered). (8) **Dynamic categories per request type confirmed + data-fixed:** the frontend already filters `category.request_types.includes(ticket_type)`; tagged 5 legacy catch-all categories (Account&Access/Billing/Bug Report/Feature Request/Technical Support) on the live DB so each type now shows its own clean set (General Inquiry left a true catch-all). Build EXIT=0; backend restarted + JWT-probed 200.

**v2.2 — SIGNAL FORGE motif + body declutter (2026-06-30, user: "sh-lane is repetitive at application level, want unique blender/game-level graphics; itk-body clustered & messy; cry-grain content cut").** (1) **Replaced the hero's SVG conduit→hex lane with `SdSignalForge.vue`** — a **2D canvas particle system** (the repo's first; ≠ the WebGL basin/globe and ≠ all SVG/CSS sibling instruments): raw "signal" particles stream in from the left, are caught by a forge core's gravity, **spiral into a tightening vortex** (tighter+brighter as `completion` rises) and are absorbed in a spark; **motion-blur streaks** via per-frame low-alpha trail-fill; on `ready` the core **locks emerald + fires a shockwave**. Game/blender-grade yet cheap. **Canvas+theme gotchas baked in:** additive `globalCompositeOperation='lighter'` ONLY on dark (it *lightens* → particles VANISH on cream, same family as the screen-blend gotcha) — light theme uses `source-over` + deeper ember palette; trails = fill the canvas each frame with a low-alpha **theme bg color** (NOT clearRect); a too-bright/large core + additive accumulation reads as a "blown-out white sun" → keep the core small (r≈8 idle/11 ready), gradient `r*2.8`, inner disc `r*0.42` at alpha 0.9, and clear trails faster (fade alpha 0.24); palette + bg-fade re-picked via a `MutationObserver` on `data-theme`/`data-cinematic`; **paused via IntersectionObserver** + reduced-motion (`data-cinematic!=='on'` && prefers-reduced) draws a single static frame. Edge-fade `.sh-lane-cap` gradients dissolve particles into the hero instead of hard-clipping. (2) **Body declutter:** removed the left "what happens on submit" card (it was the clutter) so the crystal is the single elegant left credential; added staggered `itk-rise` column entrances; widened copilot col to 332px. (3) **Crystal cut FIX (`SdIntakeCrystal`):** the viewport-capped aside was shorter than the crystal (worsened by the taller hero) → **compacted** the gem (132×165→110×138), tightened margins/ledger, **dropped the decorative barcode**, trimmed hero lane 132→116px — the full credential (gem + subject + pills + ledger + foot) now fits with no scroll-cut. Build EXIT=0; headless-driven (playwright-core + system Chrome, token injected to clear the auth guard) — renders with **zero console/page errors**, forge amber idle → emerald on ready confirmed, crystal uncut.

**v2.3 — WebGL forge + FULL-WIDTH stage (2026-06-30; user: the 2D particle forge was still "more bad — want VERY high-end blender/game-level"; and "itk-stage should come full width, arrange Incident + Intake Intelligence at best positions").** (1) **`SdSignalForge.vue` rebuilt as a real three.js (WebGL) scene** — an **accretion forge**: a depth-sorted particle stream sweeps in from the left, gravity + tangential swirl spiral it into a luminous core through **3 tilted orbiting rings** (TorusGeometry), absorbed + respawned; additive `PointsMaterial` w/ a canvas radial-glow sprite, **perspective depth + pointer parallax**, core pulse scales with `completion`, **tint()** lerps amber→emerald + a **shock ring** on `ready`. Full lifecycle (IO pause, ResizeObserver, reduced-motion=one static frame, `dispose()` geo/mat/textures/renderer — mirrors SdBasinCanvas/SdCommandGlobe). **Solves additive-on-cream for good:** the forge lives in a **dark inset "display panel"** (`.sh-lane` obsidian gradient + inset shadow + `SIGNAL FORGE` mono tag, BOTH themes) so additive is always brilliant. **Headless WebGL gotcha:** launch Chrome with `--enable-unsafe-swiftshader --use-gl=angle --use-angle=swiftshader` or the canvas is blank. (2) **Full-width stage:** `.itk-body` → flex-column: full-width `.itk-stage` (step content `max-width:1040px` centered) on top, centered `.itk-deck` `[crystal 380 | copilot 520]` below. (3) **Viewport-lock gotcha (important):** the Support shell (`DashboardLayout`→`.content-area`) is effectively viewport-locked — content taller than the viewport is CLIPPED not scrolled (a flex-column body silently clipped the deck; `document.scrollHeight` stayed == viewport, deck rendered off-screen at top:1039). **Fix: `.itk { height: calc(100vh - nav - 60px); overflow-y:auto }`** so the page owns its own scroll inside the locked shell. Build EXIT=0; headless-confirmed (top = WebGL forge + full-width stage; scrolled = uncut crystal + copilot deck), zero console errors.

**v2.4 — REVERTED to 3-column + original animation (2026-06-30; user: "incorporate Incident + Intake Intelligence WITH the Requester info — what's the fun of them at the bottom when you can't see them without scrolling", and "keep the FIRST animation, you're not capable of good motion graphics").** Lesson: **a live-preview credential + a live AI panel MUST stay in-view beside the form — a below-the-fold deck defeats their purpose; and don't keep re-rolling a signature animation the user has already accepted.** Reverted BOTH iterations: (1) layout back to the **3-column `[SdIntakeCrystal | step stage | SdIntakeCopilot]` viewport-fit grid** (`.itk-body` grid 304/1fr/336, `height: calc(100vh - nav - hero-h - 48px)`, columns `overflow-y:auto` so all three stay visible with NO page scroll; `--hero-h` via the heroEl ResizeObserver); (2) hero animation back to the **original SVG INTAKE-STREAM lane** (conduits → forming hex that locks emerald on ready). **`SdSignalForge.vue` (the WebGL/2D forge) is now ORPHANED** (no importer; left on disk). The compacted crystal (v2.2) is kept so the left column doesn't cut. Net: this page's stable design = original SVG hero lane + 3-col all-visible body. Build EXIT=0, headless-confirmed (crystal | form | copilot side-by-side at one top, no scroll), zero console errors. **Takeaway for future SD work: stop swapping the hero motif — the SVG conduit lane is the accepted one.**

**v2.5 — Incident credential moved INTO a bigger hero; stage stretched (2026-06-30; user: "put the Incident card in the hero header, make it big, no overlap — then we have space, stretch itk-step-wrap on the left").** Hero is now a **2-col body** (`.sh-body` grid `[1fr 320px]`): LEFT `.sh-anim` = SVG forge lane + stepper (stacked, vertically centred); RIGHT `.sh-cred` = a `<slot name="credential">` holding the Incident crystal. `SdIntakeCrystal` gained a **`compact` prop** (gem 96×120, **ledger hidden** since the hero stepper shows progress, 2-line subject clamp, foot pinned bottom) → ~250px, fits the hero with no overlap. Page **body dropped to 2-col `[stage 1fr | copilot 340]`** — the step stage stretches into the freed left space (~760px wide) so the form breathes; copilot stays visible right; viewport-fit + internal scroll preserved (`--hero-h` ResizeObserver auto-adjusts for the taller hero). **Pattern worth reusing: a live-preview instrument can live in the hero via a named slot** — section still owns the data (passes the crystal into `#credential`), hero owns layout. Headless-confirmed: crystal in hero (top:226, h249), wide stage (w754), copilot beside, doc==viewport (no page scroll), zero console errors. Build EXIT=0.

## Support Desk — Advanced Filters drawer redesign + SdDatePicker + resolved-today data fix (2026-07-01)
Three fixes on `/{user/support,admin/support-desk}/tickets/my`. (1) **`SdAdvancedFilterDrawer.vue` ultra-modern rebuild** — 500px right drawer with grain + rail spine + drifting aura; header carries a **live match-meter ring** (SVG `stroke-dashoffset` conic, `SdCountUp` core) showing `matchCount/total` (total = `previewCount(blank())`, no new prop) + a "N of M tickets match" subline; a **removable active-filter token strip** (`activeTokens` computed → per-status/priority/sla/category/org/assignee/date/toggle chips, each `tk.clear()`s, animated via nested `<Presence>`); sections gained **icon-tile headers + selected-count badges** + staggered `secT(i)` reveal; **quick date presets** (Today/7d/30d/This month — toggle off on re-click, `activeDatePreset` highlights); switch cards upgraded (icon + sub-label). Footer Apply shows the live match count badge. **`clearAll` is now LOCAL-only** (resets the working draft; live preview updates; commit on Apply — drawer stays open) instead of the old emit-blank-and-close. (2) **`SdDatePicker.vue` (NEW, `views/support-desk/components/`)** — the calendar in the drawer "wasn't opening": it used `HrDatePicker` (anchored z-5000 teleport). Replaced with an **SD-native date-only picker using the centered-layer pattern** (copied from `SdDateTimePicker`: `.sdd-layer position:fixed inset:0 z-6000` dim+blur backdrop, centered calendar, `@mousedown.self` to close) — bulletproof inside drawers (no anchor math, always above the z-2300 overlay + z-5200 SdSelect popovers), SD `--sd-*` tokens, month-slide, min/max, Today/Clear/Done. **Rule: in the Support Desk module use `SdDatePicker`/`SdDateTimePicker`, NOT `HrDatePicker`** — the centered-layer pattern is the robust one for drawer/modal contexts. (3) **Filter audit:** all `matchAdvWith` fields verified against `TicketResponse` (impact/urgency/category_id/organization_id/assigned_agent_id all exist); fixed the **created-between boundary** — `created_from` used `new Date('yyyy-mm-dd')`=UTC-midnight while `created_to` used local end-of-day, so an IST same-day ticket could be wrongly excluded → both now anchored to the LOCAL day (`+'T00:00:00'` .. `+'T23:59:59.999'`). (4) **Backend "4 resolved but only 2 were resolved" fix** — `app/utils/support_desk/workbench.py` `resolved_today` (+`avg_resolution_minutes`) and `routers/support_desk/dashboard.py` `resolved_today`/`closed_today`/`avg_reso` now filter `SdTicket.merged_into_id.is_(None)`. Root cause: **merging duplicates calls `_transition_status(...,CLOSED)` which stamps `resolved_at`/`closed_at` on the duplicate** and sets `merged_into_id`; the frontend hides those tombstones (`!t.merged_into_id`), so they inflated the workbench "resolved today" above the visible "Resolved" lens. Reopen *does* clear `resolved_at`, so reopen was NOT the cause. **Backend change needs a restart** (run_server.py runs uvicorn without --reload). Build EXIT=0, dark+light, reduced-motion guarded.

## Support Desk — All Tickets "Team Operations Command Center" cinematic pass (2026-07-01)
Frontend-only redesign of `sections/SdAllTicketsSection.vue` + its 4 leaf components (no backend/contract change). User asks: hero too small, sonar basic + overlapping, squad/mid basic, SLA + Workload views "shitty", filters odd. **Three deliberately NON-repeating instruments** so the page reads varied: **radial** Triage Scope (hero) vs **linear** SLA-frontier rail (SLA view) vs **vertical** capacity-equalizer (Workload view). (1) **Hero enlarged to the My-Tickets-hero scale** — adopt the `SdMyTicketsHero` shell pattern (ambient orbs + SVG mesh + grain, pointer-parallax on orbs, `clamp(30,3.8vw,46px)` gradient title, CTA row, scan-line + danger/warn/amber "pulse chips") in a 2-col `[lead 1.04fr | scope 1.16fr]` grid; the squad board moved OUT of the hero into its own full-width strip (the old 3-col hero was the "cramped/overlapping" complaint). (2) **`SdTriageSonar` → framed "Triage Scope"** (the fix for "sonar overlaps neighbours"): give the canvas its OWN obsidian panel (`.scope` — corner ticks + warm rim + `TRIAGE SCOPE` mono tag, **dark in BOTH themes** so additive glow always reads, like the create-ticket forge's display panel) so nothing bleeds into siblings. Canvas upgrades: `ctx.createConicGradient(sweep,…)` trailing-fan beam (guarded — fallback to manual arc-segments), **SLA-frontier ring labels** (NOW/12h/24h/48h+) drawn on-canvas, faint radial **priority sector wedges**, comet-trail ping blips, **breach reactor + expanding shockwave ring**, **ambient motes** (data-independent → never empty), centered circular reticle (CSS). **Key fix:** breached blips cluster at the centre, so push their radius ≥0.22 to ring AROUND the centred "N breaching" readout (which has a radial dark backing) instead of burying it. Legend on its own bottom rail (reserved band), core readout pinned centre — both OUTSIDE the busy ring zone (the "no overlap" requirement). (3) **`SdSquadLoad` → "Squad telemetry"** strip — per-agent tile = conic load-ring (`--sd-p` @property, avatar-initial core) + segmented tick meter (open vs busiest) + crit/breach pips + a team-balance pill; `repeat(auto-fill,minmax(232px,1fr))`. (4) **`cmd-mid` de-clustered** — KPI bento is now a clean `repeat(4,1fr)` grid of `SdKpiTile` (already cinematic: tilt+spine+sheen+count-up; pass `:live` to pulse the breaching tile) + the `SdInsightTicker` as a full-width strip BELOW it (was squeezed into a 2-col side column = the "clustered" look). (5) **Filters "properly structured"** — lenses + view-tabs + tools consolidated into ONE `.cmd-deck` `.sd-card` (lenses row, divider, control bar) instead of three loose rows. (6) **`SdSlaPulse` → "SLA Frontier"**: compliance **reactor** (conic `--sd-p` + masked rotating `g-sweep` highlight + ok/warn/bad tone) + 3 **segmented state meters** (VORTEX "weapons-range" tick bars: Within/Approaching/Breached, click-to-filter) + an animated **linear frontier rail** (`z-over | NOW gate | z-soon | z-safe` zones, flowing gradient line, each at-risk ticket a priority-tinted blip positioned by time-to-breach, breached ones pulse) + redesigned **countdown ledger** (per-row priority spine + live conic mini-ring of consumption + tone-coded remaining). (7) **`SdWorkloadMonitor` → "Load Balancer"**: team-utilization **reactor** + a **capacity equalizer** (one vertical load-bar per agent, `wl-rise` scaleY grow, dashed balanced/overload guide-lines — the VORTEX energy-system motif) + ring-gauge roster cards (conic load + in-progress/pending/resolved mini stat-bars). **Verification recipe used (reusable):** mint a superuser JWT by reading `C:\Projects\FourConnectService\.env` directly with psycopg2+jose (the ORM trips on the unresolved `AssetCategory` relationship if you `db.query(User)` without the full model registry — so go raw SQL), then a `playwright-core` + system-Chrome script injects it as localStorage `admin_token`+`user_token` and screenshots `/admin/support-desk/tickets/all` per view per theme. **Headless gotcha:** `elementHandle.screenshot()` of a tall `.cmd` (>viewport) inside the DashboardLayout scroll container paints the lower portion BLACK on the first capture — NOT a bug (boundingBox confirms deck/stage are laid out with real dims); screenshot the inner section (`.cmd-stage`) or wait longer + click a view to confirm. All `--sd-*` tokens → light theme automatic (scope panel stays dark by design); every loop/keyframe `@media (prefers-reduced-motion) html:not([data-cinematic="on"])` guarded; build EXIT=0; dark+light headless-confirmed, zero console errors.

**v2.1 THEME-ADAPTIVE correction (same day):** "light in both themes" was WRONG — the user's actual intent was *adapt to the theme* ("it should come light on light theme and dark on dark theme"). The literal ink-on-light interior was refactored into a **`--dk-*` component-scoped variable set** on `.dok` (dark = night-atelier defaults: warm-graphite #211b13→#120e09 stage, smoked amber-glass crates, gunmetal rail, lamp-glow skylight, faint light-beam sweep; `[data-theme="light"] .dok` = the bright product-table overrides) — same geometry + motion in both. **Lesson: when a user says "dark background → light", read it as "wrong for my current theme", not "light always"; build instruments theme-adaptive from the start (the Awaiting-Reply vitrine rule), and route every interior color through component vars so the flip is one override block.** Dark+light headless re-verified, zero console errors.

## Support Desk — On Hold v2 "ATELIER DOCK" (2026-07-02, same day — LIGHT-STAGE instrument)
User rejected the obsidian dock panel ("ohh-instrument has dark background i want it on light background") + asked for next-level launch-ad motion via **7-8 artifacts**. Repeated the proven **Artifact motion-study gallery** flow (pending-customer/vendor precedent): ONE artifact page with **8 LIVE-ANIMATED light-stage studies** (Atelier Dock / Levitation Field / Pendulum Wave / Tag Line / Amber Reliquary / Zen Garden / Orbital Carousel / Kinetic Mobile), each looping its release choreography with real hold data → **user picked STUDY 01 "Atelier Dock"**. `SdSuspensionDock.vue` rebuilt: the crane story re-lit as a **bright product table in BOTH themes** — interior colors are LITERAL ink-on-light (never `--sd-text`, which would flip) so in dark mode it reads as a lit display case against the dark hero (the inverse of the display-panel trick; note the panel gets a HEAVIER drop shadow in dark). Kept: slot-mirror departure choreography, trolley travel, props/emits, reduced-motion guards. Added the two launch-ad tells from the study: **breathing CONTACT SHADOWS** on the studio floor (separate non-rotating element per slot, same duration/negative-delay as its crate's sway via the shared `swayStyle(i)` inline style, counter-phased translateX) + a periodic **SPECULAR SWEEP** (blurred white gradient bar, `translateX(-320px)→1300px rotate(7deg)`, 9s). Layout lesson: **5 bays @20% clipped the manifest labels — 4 bays @25% is the right density** for a ~600px instrument (gallery showed 4; match what was approved); ETA labels shortened (`RELEASE 12d`, `NO DATE`) + `.cr-foot flex-wrap` so tight crates wrap instead of clipping. Gallery-verification gotcha: the raw artifact HTML has no doctype — wrap a LOCAL copy for headless preview, and PowerShell 5.1 `Get-Content -Raw` re-encodes UTF-8-no-BOM as ANSI (mojibake em-dashes in the preview ONLY; the published artifact is fine). Build EXIT=0; dark+light headless-verified, zero console errors.

## Support Desk — On Hold "The Suspension Dock" (2026-07-02)
`/{user/support,admin/support-desk}/tickets/on-hold` — the LAST work-state tab off the generic `SdTicketListSection`. User picked the motif from a 4-option AskUserQuestion gallery (Amber Vault / Chronostasis / Cryo Bay / **Suspension Bridge Dock** ← chosen) + "Full ServiceNow-grade" backend. Signature instrument = **`SdSuspensionDock.vue`** (DOM/SVG + motion-v, NOT canvas — text-bearing crates render crisper as DOM, the Awaiting-Reply-deck lesson): held tickets hang as **cargo crates on swaying suspension cables** (per-slot `--dur` + negative delay pendulum, `transform-origin: top center`) from a riveted **gantry rail** with blinking beacons and a **patrolling crane trolley** (CSS `left` keyframe patrol; on resume it switches to a JS-set `left` + transition, travels to the bay, the crate lowers away via Presence `:exit` + emerald "Released" pop, queue backfills from a **local slot-mirror** — fixed `slots` array + watch-diff, the choreography trick). Crates carry priority edge, reason-code tag (`holdReasonShort`), release manifest (`RELEASE 2d 4h` / `NO RELEASE DATE` / `OVERDUE` strobe), STALE dashed badge, hover-reveal emerald **RELEASE** chip; obsidian **dock panel** in BOTH themes (display-panel trick) w/ lamp shafts + dust motes + `+N more` tag + "Dock clear" plate; ambient runs at 0 data (hard rule). Hero **`SdOnHoldHero.vue`** = the `aoh` shell re-identified (stone/ember `--sd-dock-*` duotone in support-desk-theme.css, hanging-cable mesh lines, CLOCKS FROZEN eyebrow, title "Held, not forgotten.", emerald **Resume next** + **Review stale** CTAs, banked-SLA scan chip) + 8 lenses (In suspension / Scheduled / Releasing today / Overdue / Stale / No date / **Blocked by CHG-PRB** (`linked_change_id||linked_problem_id`) / banked-time `stat`). Advanced panel **`SdReleaseTimeline.vue`** = a NEW axis (≠ AgingLadder created-age, ≠ CadenceDeck silence): **release runway** — fixed-flex zone bands `OVERDUE | NOW-gate | TODAY | THIS WEEK | LATER | NO-DATE siding`, priority-tinted blips positioned by time-within-band, click-to-filter — + a proportional **hold-reason spectrum** bar + chips (count + avg-held). Backend shipped alongside (probe-verified E2E): `HoldReason` taxonomy + `STALE_HOLD_DAYS=7` in constants; cols `hold_reason_code`/`last_hold_review_at`/`hold_review_count` (+`ix_support_tickets_hold_until`) via `add_ticket_hold_columns.py` + `ensure_ticket_hold_columns` startup hook; **`_transition_status` hold bookkeeping** (the loophole fix, mirrors the vendor block: ANY path into ON_HOLD stamps `held_from_status`/`held_at` w/ `or`-guards, ANY path out clears all hold fields — bare set-status/bulk/board-drop no longer leave stale metadata or mis-resume); `TicketResume{reason}` + `POST /hold-extend` (review stamps; hold itself 409s when already held — extend is the update path); bulk `hold`/`resume` actions w/ skip reasons; **`auto_resume_expired_holds`** (hold_until passed → resume to `held_from_status` else owner?in_progress:open, SLA un-freezes via shared `resume_held_ticket` in `_common.py`) called on on-hold **list-load** (admin + command-center) AND in `tasks_cron.py` + **`remind_stale_holds`** (no release date, unreviewed ≥7d → once-daily `hold_review_due` activity + notify, deduped on activity NOT review_count); enrichment `time_on_hold_ms`/`auto_resume_at`/`hold_stale`; dashboard `on_hold` count. Frontend governance: `SdAgentActionModal` hold mode = taxonomy chip grid + release date ("auto-resumes"), resume mode = note + "RETURNS TO {held_from_status}" chip, NEW `hold-extend` mode (prefilled from ticket); `SdFlowMoveModal` **routes move-to-on_hold through `holdTicket`** (code chips + date) instead of bare set-status + fixed its stale "hold does NOT stop SLA" consequence copy; `SdTicketTable` gained `holdReason`/`release`/`heldFor` columns (vendor-column pattern); drawer gained Extend action + enriched hold banner/KvRow. Distinct from every sibling — the only **hanging-cargo / crane-choreography** motif (≠ radar, conveyor-belt, pipeline, chat capsules, dish, orbit, gauge; the departure is a short apron exit, deliberately NOT a belt — Unassigned owns the belt). Build EXIT=0; headless dark+light+admin verified (4 crates, 8 lenses, runway, manifest cols, extend modal) w/ **zero console errors**; seeds archived after.

## Support Desk — Open / In-Progress "Live Operations Floor" (2026-07-01)
`/{admin/support-desk,user/support}/tickets/open` was the ONLY active-work tab still on the generic `SdTicketListSection` (flat table, zero animation) — every sibling had a bespoke section, so it read "repetitive." Gave it its own **`sections/SdOpenTicketsSection.vue`** + wired the `open` tab to it in `supportModules.js` (explicit `{kind:'ticket-list', comp}` object, exactly how All/Unassigned are wired; `SdTicketListSection` stays the engine for the other 12 scopes). **Scope stays STRICT open+in_progress** (backend `_apply_scope('open')` = `status IN(open,in_progress)`; user chose strict, NO backend change) — adjacent stages (Pending/Hold/Escalated) are surfaced but **cross-link** to sibling tabs. Signature = **THE MOMENTUM PIPELINE** (`components/SdFlowPipeline.vue`): a horizontal aggregate **stage-flow** `Open → In-Progress → Pending → Hold → RESOLUTION GATE` in a **dark inset display panel** (both themes, so additive glow reads — same trick as the create-forge / triage-scope), with node conic mini-rings (share of active) + **flowing rails carrying traveling packet particles** (CSS `left` keyframe, breaching packets flare red) + a pulsing green **Resolution Gate** (beam + throughput/velocity readout from `avg_resolution_minutes`) + an **escalated spur alert**. Clickable nodes: open/in_progress filter locally, pending/hold/escalated `emit('pick',{action:'nav',navTab})` → cross-navigate. Fresh motif — linear/directional/throughput-encoded, ≠ All's radial sonar, My's single load-gauge, Unassigned's centripetal gravity-well, Create's converge-to-one intake stream, the drawer's single-ticket lifecycle filament. Hero **`components/SdActiveOpsHero.vue`** mirrors the `smh`/`cmd` shell (parallax orbs+grain+mesh, gradient title "Active work, in motion", CTA row w/ ⌘K, 8 telemetry lenses) but hosts the pipeline via a **`#instrument` named slot** (section owns data, hero owns layout — reusable pattern). NEW advanced feature **`components/SdAgingLadder.vue`**: aging/SLA burn-down **equalizer** (Fresh<1h/Today/1–3d/3–7d/>7d bars, click-to-filter) + a **stall/idle detector** (`updated_at`/`last_viewed_at` older than 8h — ServiceNow "idle" concept the flat table never had). **Data pattern = My-Tickets workbench:** fetch ONE 100-row team-scoped window via `listScoped({agent,scope:'open'})` (agent→team-sealed admin route, employee→`/me`; consistent with All/Unassigned) + stats via `fetchCommandCenterStats()`(agent)/`fetchMyWorkbench()`(employee) — then **filter/sort/lens/age/paginate 100% client-side** for instant switching. Default view = **Flow board** (`SdTicketsBoard` gained an optional `columns` prop → active-work lanes + a **Resolve drop-gate**; dropping →Resolve opens `SdResolveModal`, →In-Progress on an unowned ticket opens `SdBulkActionModal` mode=assign since the backend gates in_progress on an owner, →Pending/Hold = `changeTicketStatus`); other views Swimlanes(`SdSquadBoard`)/Table/SLA/Workload reused. **Every board drop opens an animated confirmation** (`modals/SdFlowMoveModal.vue` — morphing status seal + FROM→TO process pills + requester/owner people row + contextual reason chips → note persisted to the timeline via `/status`'s `note` field + workflow-consequences strip; requires an owner INLINE when the target is In Progress, since the backend gates it) — so no silent status jumps; resolve/close still route to `SdResolveModal`, escalate to `SdBulkActionModal`. Bulk + single actions all route through the rich **`SdBulkActionModal`** (assign/status/priority/escalate/resolve/merge, per-ticket eligibility mirrors the guards). Loopholes closed: KPI counts match the strict list (no more OPEN_SET-vs-scope drift), escalated surfaced via `is_escalated`/stats not just status, on-hold-still-burning shown via insights, merged tombstones hidden (`!merged_into_id`), cross-links via `router.push(\`${base}/tickets/${tab}\`)` (workspace `@go` is module-level, not tab-level — navigate directly). Build EXIT=0; headless-verified (minted superuser JWT, Chrome via playwright-core, tokens injected) — hero+pipeline(4 nodes+gate)+8 lenses+aging+board+table all render, **zero console errors**, dark+light confirmed, insight ticker rotates. Reduced-motion guarded (`.reduced` prop + `html:not([data-cinematic="on"])`).

## Support Desk — Escalated "The Thermal Updraft" (2026-07-03)
`/{admin/support-desk,user/support}/tickets/escalated` — rebuilt off the generic list into **`sections/SdEscalatedSection.vue`** wired via the explicit `{kind:'ticket-list', comp, scope:'escalated'}` registry entry. Identity = **`--sd-esc-*` ember/heat** (#f97316 ramp + `--sd-esc-deep-bg` obsidian both themes; ≠ war-room crimson). **⚑ SIGNATURE = "THE THERMAL UPDRAFT"** (user picked study 06 of the 8-study Artifact gallery): `SdEscalationInstrument.vue` = Canvas2D full-bleed hero backdrop — a flickering **fire-bed** feeds a rising heat column (ambient embers + shimmer filaments, never empty); every ACTIVE escalation rides it as a labelled ember — **ALTITUDE = log-dwell (10m→48h) at tier**, size/glow = tier, color = state (amber flicker unacked / red halo+ring ack-overdue / steady ember acked / AUTO tag = sweep-raised); ignition burst on arrival, emerald cool-off on departure; chips are canvas hit-tested buttons with a **de-collision pass** (sort by y, push down, clamp above the lens dock) + leader lines. Hero `SdEscalatedHero` = seismograph shell contract (full-bleed `#instrument`, glass console left "What rises, burns.", eMTTA·avg-dwell·ack-coverage readouts, breach-candidates CTA → Breached tab, 9 docked lenses). Second instrument **`SdTierLedger.vue`** = horizontal **tier strata** (L1/L2/L3+ lanes, blips on the shared log-dwell axis, lane→tier lens, blip→drawer) + proportional **escalation-reason spectrum** (click→filter; mirrors hold-reason spectrum mechanic, new tier axis). `SdDeEscalateModal` (reasoned stand-down, single+bulk, L→L-1 pills, consequences strip). SdTicketTable gained cols `tier/escTarget/escAck/escDue/dwell` + `esc-ack` emit. Backend shipped alongside (probe-verified 30/30): structured escalation record (type/reason_code taxonomy/escalated_by/escalated_to_team FK), **escalation ACK → eMTTA** (`escalation_acknowledged_at/by`, cleared on every level bump), **per-priority response clock** (`escalation_response_due_at` + day-throttled overdue sweep), **auto-escalation on SLA-resolution breach** (once-only `auto_escalated_at` stamp, open/in_progress+owned only — user-approved), reasoned+notified de-escalate (level-0 clears live state, keeps history), `GET /{id}/escalation-history` (derived from activities + dwell_ms), team-sealed `GET /me/tickets/escalated/stats` (by_level/by_type/by_reason_code/eMTTA/dwell/candidates/squad), bulk `escalation_ack`/`de_escalate`, vendor sweep refactored through `apply_escalation` (fixed its missing `escalated` activity), **stop-the-clock fix**: escalating a paused ticket now runs `apply_pause_transition` (the old inline path froze the SLA forever). SdEscalateConsole.run() now sends ONE structured escalate (reason_code guessed from text; no comment-smuggling); SdAgentActionModal deescalate mode requires the reason. Headless-verified admin+agent × dark+light (9 lenses/instrument/ledger/table, zero console errors), esc-ack DB-confirmed, guided sweep + de-esc modal smoked. Distinct from every sibling — the only **rising-ember heat-column / dwell-altitude** motif. GOTCHA: a demo agent may be member of one team AND lead of another — "agent rows < admin rows" seal heuristics can false-alarm; verify membership before suspecting a leak.

## Support Desk — Critical "The War Room" (2026-07-03)
`/{admin/support-desk,user/support}/tickets/critical` — the LAST work-state tab off the generic list. **`sections/SdCriticalTicketsSection.vue`** wired via the explicit `{kind:'ticket-list', comp, scope:'critical'}` registry entry. **Scope = critical ∪ major incidents** (`include_major=1` widens the list; bare scope stays pure for other callers); working set keeps terminal rows so the **RCA-gap lens** works. Identity = **`--sd-crit-*` crimson-scarlet** (≠ ops-dash rose; `--sd-crit-deep-bg` obsidian in BOTH themes like the dock). Composition: `SdCriticalHero` ("Hold the line." + ACK-next / Declare-MI / Guided CTAs + MTTA·MTTR·ack-coverage mono readouts + 9-lens rail + `#instrument` slot) → SdSquadLoad → control deck (Situation board | Incident flow [Triage→Mitigating→War room→Resolve gate] | Swimlanes | SLA | Load) → SdTicketTable w/ NEW catalog cols `mi/ack/nextUpdate/impact` (+ emits `ack` one-click acknowledge, `war` MI-badge→console) → `SdWarRoomConsole` drawer (impact triage, war-room link, **stakeholder-update composer** w/ investigating→resolved template chips + cadence timer, responders, mission clock, PIR/RCA form, **presence avatars + collision warning**) + `SdMajorIncidentModal` (declare/stand-down + cadence arm). ServiceNow/Zendesk features shipped on real backend: **ACK→MTTA** (`acknowledged_at/by`, ≠ first_responded_at), **update cadence** (`next_update_due_at` + overdue sweep on list-load/cron), **presence heartbeats** (`support_ticket_viewers`, POST /{id}/presence, prune>120s/live<60s), `GET /me/tickets/critical/stats` (CriticalStats, `_command_center_filter` sealed), bulk `ack`, **bulk team-guard fix** (pre-filter payload ids through `_agent_scope` — was mutable-by-raw-id), `_panel_base()` panel-aware notification deep-links. **Guided mode** = rank (unacked→breached→soonest-due→oldest) served one-at-a-time on drawer close (Zendesk play-mode). **⚑ Signature instrument = "THE SEISMOGRAPH WALL"** (user picked study 01 of the 8-study gallery): Canvas2D FULL-BLEED strip-chart hero backdrop — pen draws a continuous baseline tremor, each active critical re-fires as a magnitude event (spike burst + flagged ticket chip riding the paper; mag from MI/breach/unacked), MI = traveling shockwave rings + band flash; flags are canvas hit-tested buttons (hover glow, click→open); 3-lane LRU picker prevents chip overlap; first load staggers in via the scheduler. Dual palette (night-ops additive glow / day-ops chart paper source-over); readVar + data-theme MutationObserver; ResizeObserver/IO/visibility pause; reduced-motion silently simulates ~45s then draws one static frame. Hero = ultra-wide banner per the study note: instrument fills the hero (`height=0` absolute-fill contract), glass-blur console over the calm left end above a theme-aware scrim, 9 lenses docked along the bottom ruling as compact glass chips. Contract `{tickets(rank-ordered), stats, now, reduced, height}` → `open`. **GOTCHA:** `watch(computed, …)` in `<script setup>` evaluates the getter immediately — referencing a `const ref` declared later = TDZ crash that blanks the whole section ("Cannot access 'drawerId' before initialization"); declare refs above eagerly-evaluated computeds/watches. Headless-verified admin+agent × dark+light, ACK/MI-modal/guided smoked live, build EXIT=0.

## Support Desk — Breached "The Time-Debt Meter" (2026-07-04)
`/{admin/support-desk,user/support}/tickets/breached` — the last major work tab off the generic list. **`sections/SdBreachedSection.vue`** wired via the explicit `{kind:'ticket-list', comp, scope:'breached'}` registry entry. Identity = **`--sd-brc-*` crimson debt-ink + tarnished-brass ledger** (#dc2626 core + #eab308 brass; `--sd-brc-deep-bg` vault-night in BOTH themes; repair=emerald, risk=amber — deliberately monetary vs war-room crimson / updraft ember). **⚑ SIGNATURE = "THE TIME-DEBT METER"** (user picked study 02 of the 8-artifact gallery: fracture pane / time-debt / containment / domino / seal wall / gauge cluster / light table / event horizon): `components/SdBreachInstrument.vue` = full-bleed hero backdrop (height=0 contract) — a giant 6-reel **rolling odometer of total breach-minutes computed PAUSE-AWARE from the working set every second** (real minute-per-minute accrual, no fake speed-up; spring count-up on mount, short reel-rolls as minutes land; never reads less than the sealed stats rollup), "+N min/min ACCRUING" burn-rate chip (= active unpaused resolution breaches), ❄ frozen count, and a **worst-debtors ledger** (top-5 by overage, share bars, "+1m" interest-tick pulses when a row's own minute lands, click→drawer); ledger-rule background + drifting debt motes + right-anchored stage so the glass console owns the calm left air. Hero `SdBreachedHero` = escalated-shell contract ("Every minute owed, on the meter.", CTAs Capture-next-root-cause [emerald primary] / Guided triage / N-at-risk → rail scroll, TOTAL-DEBT·AVG·WORST·RCA-COVERAGE readouts, 9 docked lenses: in-debt/response/resolution/both/unowned/not-escalated/RCA-missing/frozen/repaired-today). NEW surfaces: **`SdSlaAnatomy.vue`** (per-ticket SLA autopsy rail: opened→RESP met/missed→TARGET flag→striped crimson overage zone→NOW/REPAIRED/❄FROZEN pin, banked-pause chip — powers the first-class **Anatomy view** + the RCA console evidence header), **`modals/SdRcaConsole.vue`** (breach-reason taxonomy chips [BREACH_REASONS in useSupportDesk] + what-happened/corrective/preventive → existing `POST /{id}/rca`; UNEXAMINED→READY-TO-FILE stamp morph), **`SdAtRiskRail.vue`** ("prevent the NEXT breach": scope=due_soon chips w/ live countdown + burning-fuse bar, imminent ≤30m throbs). SdTicketTable gained catalog cols `overage` (pause-aware red mono, ❄ when frozen) / `breachedAt` (relative from stamps) / `rca` (Filed ✓ / Reason / pulsing RCA capture button, emit `rca`). Backend shipped alongside (probe_breached_desk.py **39/39**): **stale-flag loophole CLOSED** — `sla_response_breached_at`/`sla_resolution_breached_at` cols (migration `add_ticket_breach_timestamp_columns.py`, backfilled w/ due instant) + `app/utils/support_desk/breach.py::sweep_sla_breach_flags` (idle past-due non-terminal unpaused rows flip via `recompute_breach_flags` — which now stamps/clears the timestamps — + once-only `sla_breached` activity + first-ever `EVT_TICKET_SLA_BREACH` dispatch w/ 24h staleness throttle; runs on breached/overdue/due_soon list-load, inside the stats endpoint and as cron step 14 BEFORE the auto-escalation sweep so fresh breaches lift same-run), team-sealed `GET /me/tickets/breached/stats` (BreachedStats: by_kind/by_priority/by_age/total_debt_minutes [pause-aware SQL case], avg/max overage, at_risk+imminent, repaired_today, avg_repair_overrun, missing_rca+rca_coverage, squad), list params `breach_kind|missing_rca|active_only` + `due_soon` scope + `sla_*_breached_at` sort keys (both admin + /me lists). Data = the proven one-window pattern (100 rows `listScoped scope=sla_breached active_only=1` sorted breached_at asc + sealed stats + 8-row due_soon rail); guided = deepest-overage-first served on drawer close; "Include repaired" toggle widens to terminal rows so lens totals reconcile. Distinct from every sibling — the only **monetary/odometer-ledger** motif. Build EXIT=0.

## Support Desk — Overdue "The Gravity Well" (2026-07-04)
`/{admin/support-desk,user/support}/tickets/overdue` — the LAST operational ticket tab off the generic list. **`sections/SdOverdueSection.vue`** wired via the explicit `{kind:'ticket-list', comp, scope:'overdue'}` registry entry. Identity = **`--sd-ovd-*` rose event-horizon** (#e11d48 core + amber dust + emerald escape; `--sd-ovd-deep-bg` void-night in BOTH themes — deliberately RECOVERY ops vs the Breached ledger's monetary crimson: everything here is open, unpaused, still savable). **⚑ SIGNATURE = "THE GRAVITY WELL"** (user picked concept 01 of the 8-artifact gallery: gravity well / floodline / sandfall / pressure dam / time rift / overrun runway / clock wall / orbital decay): `components/SdGravityWell.vue` = Canvas2D full-bleed hero backdrop (height=0 contract) — every overdue ticket orbits a singularity as a burning body: **orbit radius = log(lateness) (later = deeper), angular speed = Keplerian (deeper = faster)**, color = priority, dashed halo = unowned, decay trails, rotating accretion arcs, infalling amber dust, twinkling starfield, worst-3 canvas labels, **emerald escape-burn** riding whenever `recovered_today > 0`; bodies keyed by ticket id (stable orbits across refetches, fade in/out on lens flips), hover = HTML telemetry card + dashed orbit ring, click→drawer; readVar palette + data-theme MutationObserver + ResizeObserver + visibilitychange pause; reduced = one static frame. Hero `SdOverdueHero` = breached-shell contract ("Past the deadline, still in orbit.", CTAs **Start recovery run** [emerald primary] / **Nudge owners** [rose] / N-at-tipping-point → strip scroll, TIME-OWED·AVG·DEEPEST·RECOVERED-TODAY·❄FROZEN-EXCLUDED readouts, 8 docked lenses: in-the-well/no-first-reply/past-resolution/unowned/not-escalated/critical/long-tail>3d/recovered-today). NEW surfaces: **`SdTippingPoint.vue`** (scope=due_soon capsules with live SVG **draining orbit-rings** + mm:ss countdowns, ≤30m throbs rose — deliberately ≠ SdAtRiskRail's fuse bars, which stay breached-only), **HORIZON view** (in-section log-scale past-due gantt: deadline rail + +1h/+4h/+24h/+3d gridlines, live-growing gradient bars, click→drawer), **RECOVERY RUN** (teleported overlay guided mode — queue ranked by `priority-weight × log₂(lateMin) + unowned-boost`, per-card actions Open console / Assign to me / Nudge owner / Resolve / Skip, progress dots, Esc ends, hides while drawer open). SdTicketTable gained catalog cols `lateBy` (live pause-aware governing-clock overage, rose mono + dashed "1st REPLY" chip when the response clock is the miss) / `dueAt` (absolute target). Backend shipped alongside (probe_overdue_desk.py **51/51**): team-sealed `GET /me/tickets/overdue/stats` (OverdueStats: kind split incl. response-overdue, lateness ladder <1h→>3d, total/avg/max late, oldest ref, at_risk/imminent, recovered_today, frozen_excluded, squad; runs sweep_sla_breach_flags first), **`overdue_kind=any|response|resolution` param on all 3 list endpoints** via shared `_common.apply_overdue_scope` (CLOSED the response-SLA blind spot — default stays legacy resolution-only for other callers; the section requests `any`), **`POST /tickets/{id}/nudge-owner`** (owner accountability ping ≠ /remind's requester nudge: `_agent_scope`-sealed 404, 409 on unowned/terminal/self, 24h day-throttle deduped on `owner_nudge` activity, notification + audit) + `nudgeTicketOwner`/`bulkNudgeOwners`/`fetchOverdueStats` in useSupportDesk (bulk nudge counts 409-skips separately so the toast stays honest). Data = the proven one-window pattern (100 rows `listScoped scope=overdue overdue_kind=any` sorted resolution_due_at asc + sealed stats + 10-row due_soon strip + 1s `now` tick for live cells). Distinct from every sibling — the only **orbital-mechanics/singularity** motif. Build EXIT=0; section API calls probed live with an agent token (team-sealed 200s).

## Support Desk — Reopened "The Möbius Loop" (2026-07-05)
`/{admin/support-desk,user/support}/tickets/reopened` — the returns desk (resolved once, back again). **`sections/SdReopenedSection.vue`** wired via the explicit `{kind:'ticket-list', comp, scope:'reopened'}` registry entry. Identity = **`--sd-rop-*` orchid-magenta one-sided band** (#e0509b core + amber `band` desk-crossing + emerald `offramp` re-resolution; `--sd-rop-deep-bg` night air in BOTH themes — deliberately RECURRENCE vs Overdue's rose deadlines: a reopened ticket carries its failed fix with it). **⚑ SIGNATURE = "THE MÖBIUS LOOP"** (user picked concept 03 of the 8-artifact gallery: echo chamber / boomerang / möbius / revolving door / palimpsest / tide / rewind reel / newton's cradle): `components/SdMobiusBand.vue` = Canvas2D full-bleed hero backdrop (height=0 contract) — a woven **lemniscate with a half-twist** (11 strands, `tw = sin(θ/2)·offset` flips side over one lap = ONE surface; counter-flowing dashes) between labelled **DESK** (amber) and **RESOLVE** (emerald) crossings; every active reopened ticket is a **rider**: speed grows with `reopened_count`, color = priority, glow ring = chronic ≥2, dashed halo = unowned, worst-3 canvas labels `×N`; a desk-crossing fires an **amber re-entry pulse**, an **emerald off-ramp comet** departs RESOLVE while `re_resolved_today > 0`; riders keyed by ticket id, hover = HTML telemetry card (cycles/source/cycle-age), click→drawer; readVar palette + data-theme MutationObserver + ResizeObserver + visibilitychange pause; reduced = static frame. Hero `SdReopenedHero` = overdue-shell contract ("Resolved once. Back again.", CTAs **Start loop run** [emerald primary] / **N chronic riders** [magenta → rail scroll] / N re-breached [risk → lens], REOPEN-RATE-30D·AVG-TIME-TO-REOPEN·AVG-CYCLE-AGE·RE-RESOLVED-TODAY·STUCK-RIDER readouts, 8 docked lenses: on-the-loop/chronic≥2×/customer-reply/requester/agent-reopen/re-breached/unowned/re-resolved-today). NEW surfaces: **`SdChronicRail.vue`** (repeat-offender capsules: cycle dots ×N, source chip, coded verdict, live cycle age — recurrence ≠ TippingPoint's countdown rings), **LOOP view `SdReopenLoop.vue`** (per-ticket cycle strip: struck-through FAILED FIX [prev_resolution_* + title=summary] ─dashed-flow→ KICK-BACK [source/verdict/"fix held Xh"] ─→ THIS CYCLE [cycle age vs fresh due; RE-BREACHED hot / RE-RESOLVED emerald]), **QUALITY LOOP RUN** (teleported guided overlay ranked `cycles×2.2 + priority + log₂(cycleAge) + unowned-boost`, failed-fix panel on each card). SdTicketTable gained catalog cols `reopenSource`/`reopenedAt`/`prevRes`; SdAgentActionModal reopen mode + SdMyTicketDrawer reopen panel gained **ReopenReason verdict chips**; SdTicketDrawer gained a reopen-context banner + rich flag tooltip. Backend shipped alongside (probe_reopened_desk.py **50/50**): `ensure_ticket_reopen_columns` guarded migration (reopen_reason_code/source, last_reopened_at/by, reopen_latency_ms, prev_resolution_* snapshot + **DDL-safety for the model-only reopened_count**, activity backfill), **single-writer `_common.apply_reopen` engine** (used by _transition_status + self-reopen + portal auto-reopen; snapshots the failed fix, stamps the cycle, **re-arms a FRESH resolution SLA from the package before recompute_breach_flags** — closed the "instantly overdue on reopen" loophole), **`auto_reopen_on_customer_reply`** (reply to RESOLVED within the 3-day window auto-reopens source='portal' — the silent-reply loophole; CLOSED still needs an agent), `EVT_TICKET_REOPENED` dispatches, ReopenSource/ReopenReason vocab + CHRONIC_REOPEN_THRESHOLD=2, team-sealed `GET /me/tickets/reopened/stats` (ReopenedStats: active/re-resolved/chronic/by_source/by_reason/reopen_rate_30d [reopens ÷ (surviving resolves + reopens)]/time-to-reopen/cycle-age/worst/squad), list params `reopen_source|chronic|reopened_from|reopened_to` + `last_reopened_at`/`reopened_count` sorts on both panels. Data = the proven one-window pattern (100 rows `listScoped scope=reopened active_only=1` sorted last_reopened_at desc + sealed stats + "Include re-resolved" reconcile toggle + 1s `now` tick). Distinct from every sibling — the only **one-sided-band/recurrence** motif. Build EXIT=0.

## Support Desk — Resolved "The Closeout Desk" (2026-07-05)
`/{admin/support-desk,user/support}/tickets/resolved` — the quality gate (the fix landed; the customer can still veto it for 3 days; verify → collect the rating → seal the record). **`sections/SdResolvedSection.vue`** wired via the explicit `{kind:'ticket-list', comp, scope:'resolved'}` registry entry. Identity = **`--sd-res-*` deep-emerald resolution ink + warm brass seal** (#10b981 core + #d9a441 brass CSAT/seal voice + amber `close` countdown + rose `risk` low-CSAT/bounced; `--sd-res-deep-bg` vault-night in BOTH themes — deliberately PROOF vs Reopened's recurrence: two populations, the SHELF (status=resolved inside the 3-day window) and the 30-day SURVIVING record). **⚑ SIGNATURE = "THE ORIGAMI FOLD-DOWN"** (user picked concept 04 of the 8-artifact gallery: amber strata / constellation / aurora / origami / loom / lanterns / stillwater / runway): `components/SdResolutionInstrument.vue` = DOM/CSS-3D full-bleed hero backdrop (height=0 contract; the desk's only NON-canvas instrument) — real resolved tickets descend as open paper sheets (ticket number + faux text + code chip), crease themselves shut in two true rotateY folds (`preserve-3d` halves, crease-edge tone = resolution-code family: emerald/brass/steel/amber), compact, and glide onto the brass ARCHIVE shelf as sealed parked forms (max 10 stable slots, oldest recycles; landing fires a brass ring pulse); a bounced fix (reopened_count>0) lifts off the shelf, unfolds mid-air in rose, and climbs out. Every sheet/parked form is clickable → drawer. NEVER-EMPTY rule: with a clear desk the ceremony plays as dim unlabeled GHOST sheets that real tickets displace. Vue state-machine (phase strings drive CSS transitions — no rAF, no raw DOM), spawn timers gated by IntersectionObserver + document.hidden, reduced-motion renders a static pre-seeded shelf, all timers cleared on unmount; DOM styling rides --sd-res-* tokens natively (no theme MutationObserver needed — stage dark in both themes). Atmosphere: blueprint fold-grid (repeating diagonals), entry spotlight over the drop zone, rising motes, shelf ground-glow + 'ARCHIVE · N SEALED TODAY' readout. Contract `{tickets, stats, now, reduced, height}` → `open`. Hero `SdResolvedHero` = reopened-shell contract ("The work held. Now seal it.", CTAs **Start closeout run** [emerald primary] / **N closing in 24h** [brass → rail scroll] / N low ratings [risk → lens], RESOLVED-TODAY·MTTR-P50·CSAT-30D·ONE-TOUCH·FIXES-THAT-HELD readouts, 8 docked lenses: on-the-shelf/today/this-week/pending-close/rated/low-CSAT/bounced/one-touch). NEW surfaces: **`SdAutoCloseRail.vue`** (pre-close shelf capsules, soonest seal first: SVG countdown ring draining the 3-day window [amber→warning<24h→ember past-due], CSAT chip, Close-now + Reopen actions — counts down to a POSITIVE event vs TippingPoint/AtRisk breach fuses), **`SdQualityBoard.vue`** (CSAT avg + star distribution bars, three segmented gate meters FCR/survival/SLA-met, MTTR p50+p90+per-priority speed bars, 14-day landed-vs-bounced SVG trend), **`SdResolverLeaderboard.vue`** (top-8 resolvers 30d w/ CSAT + TTR chips; click filters `resolved_by` server-side), **LEDGER view `SdResolutionLedger.vue`** (per-ticket resolution-story cards: fix-code chip → root cause → summary → resolver → TTR → live seal countdown; the positive mirror of SdReopenLoop), **CLOSEOUT RUN** (teleported guided overlay ranked `(6−csat)×3 + close-urgency + bounces×2`, per-card **Verify & close** [SdCloseModal] / **Request rating** [public staff reply] / **inline Reopen verdict chips** [REOPEN_REASON_CODES → reopenTicket → server single-writer apply_reopen] / Open console / Skip). SdTicketTable gained catalog cols `resolutionCode`/`resolvedAt`/`resolvedBy`/`autoClose` (live countdown). Backend shipped alongside (probe_resolved_desk.py **51/51**): **resolution-notes drop-gate** (`require_resolution_summary` in _common — 422 on resolve/bulk/self without a ≥3-char summary; UI was already compliant), **enum-drift fix** (`_RESOLUTION_CODES` now from ResolutionCode — was missing no_response), **bare set-status→resolved/closed now 422** on the single route (mirrors bulk `_WORK_STATUSES`; generic board drag routes through SdResolveModal as the drop-gate companion), **resolver attribution** (`resolved_by_id`/`closed_by_id` cols via `add_ticket_resolution_actor_columns.py` + activity backfill; stamped in _transition_status/self-resolve/withdraw; cleared by apply_reopen; sweeps stay NULL=system), enrichment (`auto_close_at` on RESOLVED rows = resolved_at+3d, `resolved_by_name`, `agent_public_comment_count` for the FCR lens — one grouped comment query), list params `resolved_from/to|resolution_code|resolution_category|resolved_by[seal-guarded]|csat=rated/unrated/low|pending_close|include_closed` + `resolved_at`/`closed_at`/`csat_score`/`ttr` sorts on both panels, **auto-close sweep runs on scope open** (list + stats — the shelf is always honest), team-sealed `GET /me/tickets/resolved/stats` (ResolvedStats: shelf counts + soonest seal, resolved today/7d/30d, **14-day date_trunc trend**, **MTTR avg/p50/p90 via percentile_cont pause-credited**, per-priority MTTR, **FCR** [reopened_count=0 AND ≤1 public staff reply — grouped LEFT JOIN], reopen_rate_30d [Reopened-desk denominator], SLA-met %, CSAT avg/coverage/low/dist, code+root-cause mix, resolver leaderboard, squad). Data = the proven one-window pattern (100 rows `listScoped scope=resolved status=resolved` sorted resolved_at desc + sealed stats + "Include closed" archive toggle + 1s `now` tick). Distinct from every sibling — the only **quality-gate/seal-countdown** desk (and the only one whose rail counts down to a good thing). Build EXIT=0; headless-verified admin+user × dark+light, zero console errors.

## Support Desk — Closed "The Archive of Record" (2026-07-05)
`/{admin/support-desk,user/support}/tickets/closed` — the permanence desk (terminal records: provenance, the verdict of record, what the archive teaches). **`sections/SdClosedSection.vue`** wired via the explicit `{kind:'ticket-list', comp, scope:'closed'}` registry entry (Archived stays on the generic SdTicketListSection). Identity = **`--sd-cls-*` silver-frost record ink + warm brass seal** (#c9cfd8 core + #d9a441 `seal` closure-wax/knowledge voice + rose `risk` exhumed/low-CSAT; `--sd-cls-deep-bg` archive night in BOTH themes; deliberately stays in the house neutral+warm family — NO indigo, per the CLAUDE.md palette rule — and deliberately PERMANENCE vs Resolved's living proof-shelf). **⚑ SIGNATURE = "THE VAULT"** (user picked concept 01 of the 8-artifact gallery: vault / strata core / stacks / amber / monolith / cold storage / seal press / pneumatic archive) — and per user direction it is **THEME-NATIVE, not dark-in-both-themes** (the Pending-Customer/Silence-Chronometer rule): `components/SdClosedInstrument.vue` = Canvas2D full-bleed vault door (height=0 contract) — three concentric tumbler rings whose OUTER notches are the newest sealed records (tint = provenance: brass manual / frost auto / dim merged / warm no-reply / rose exhumed-before; 12-o'clock gate notch lights as rings pass), spring lock-in cascade (outer→inner 90ms stagger) + door shudder + brass bolt slide + glint sweep fired on every new closure in the window (watch on tickets head) AND ambiently ~9s; exhume = REVERSE rose counter-spin; hub = engraved closed_total; outer notches are hover/click hit-tested → open(ticket). Source-over drawing ONLY (no additive) with the palette re-read from --sd-cls-* + data-theme on every flip: night-steel air/plate on dark, porcelain-and-slate on cream (drop-shadow grounds the door on light). The hero shell went theme-native too: dark-glass console/scrim/lenses on dark, frosted-cream console (rgba(255,250,240,.68)) + cream scrim + light lenses + dark slate ink/gradient on light — no more literal-light-ink-in-both-themes. Hero `SdClosedHero` = resolved-shell contract ("Sealed. On the record.", CTAs **Start knowledge harvest** [brass primary + candidate count] / **N live follow-ups** [seal → chained lens] / N exhumed [risk → lens], LIFETIME·SEALED-30D·LIFESPAN-P50·CSAT-OF-RECORD·SEALS-THAT-HELD readouts, 8 docked lenses: archive/sealed-today/this-month/auto-sealed/merged/withdrawn/KB-candidates/exhumed). NEW surfaces: **`SdArchiveBoard.vue`** (closure-intelligence bento: clickable PROVENANCE SPECTRUM [5 mutually-exclusive close-source segments], PERMANENCE dial [closure_survival_pct SVG ring] + exhumed/lifetime minis, THE CHRONICLE [12 monthly date_trunc cohorts, click = closed_from/to month filter], CSAT-of-record dist bars, lifespan p50/p90/avg + per-priority, knowledge & follow-through minis, top-closers strip + System auto-sweep row — every cell emits a refinement so board ≡ list), **LEDGER view `SdClosureLedger.vue`** (per-record closure story: provenance seal-glyph button → certificate, story line renders per-source [merged tombstones say so instead of a fake summary], SEALED/BY/LIVED/VERDICT/CODE meta, exhume ×N + follow-up chain chips), **`SdClosureCertificate.vue`** (teleported READ-ONLY integrity record: animated provenance stamp, THE SEAL kv-grid, per-source resolution rendering + `SdMergeChain.vue` lineage graph for tombstones, customer's-verdict stars, SLA forensics chips + prev-fix exhume history, follow-up chain [parent + children via follow_up_of param] + KB link, gated agent footer Follow-up/Promote/Reopen — zero other mutation), **`SdFollowUpModal.vue`** (Zendesk follow-up: inherited-context strip, new-story textarea, priority seg, assign-me; 201 → deep-link into the child), **`SdKbPromoteModal.vue`** (KCS harvest: live DRAFT article preview, pre-templated body, visibility seg; idempotent server-side), **KNOWLEDGE HARVEST RUN** (teleported guided overlay ranked csat×2+recency over promotable fixes → per-card Harvest/Open/Skip), **EXHUME overlay** (agent-only reopen with ReopenReason verdict chips → reopenTicket → single-writer apply_reopen). Deliberately **NO kanban view** — nothing drags INTO closed. SdTicketTable gained catalog cols `closeSource` (provenance chip → emits `certificate`)/`closedAt`/`closedBy` (System when sweep-sealed)/`lifespan` (pause-credited created→closed)/`followUp` chain chip. Composable gained `fetchClosedStats/createFollowUpTicket/promoteTicketToKb/fetchMergeChain` + `CLOSE_SOURCES`/`closeSourceOf()` (merged > cancelled > no_response > closed_by NULL=auto > manual — mirrors the backend split exactly). Backend shipped alongside (probe_closed_desk.py **65/65**): `ensure_ticket_followup_columns` guarded migration (**follow_up_of_id** self-FK + index; FK-tolerant fallback), team-sealed `GET /me/tickets/closed/stats` (ClosedStats: volume today/7d/30d/lifetime + resolved_waiting, **mutually-exclusive by_close_source** summing to closed_30d, composition + uncoded, **lifespan avg/p50/p90 percentile_cont pause-credited** [real records only — merged tombstones are population but excluded from quality math], **reopened_from_closed_30d via the reopened-activity `from` stamp apply_reopen now writes** + closure_survival_pct, CSAT-of-record block, kb_candidates [JSONB `links ? kb_article_id` NULL-safe] + kb_promoted + follow-up counters, 12-month date_trunc trend, top-8 closers coalesce(closed_by→resolved_by→assignee) + auto_closed System row), `POST /tickets/{id}/follow-up` (terminal-only 409, merged-tombstone 409 → master, child keeps the REQUESTER + fresh SLA + routing + cross-comments), `POST /tickets/{id}/promote-article` (**status=DRAFT server-forced**, idempotent via links.kb_article_id dict-copy reassignment, 422 without a summary; companion catalog.py relax: agents READ articles + edit only their OWN DRAFT, publishing stays editorial), `GET /tickets/{id}/merge-chain` (loop-guarded walk up + duplicates), **CSAT verdict-of-record guard** (agent overwrite of a rated CLOSED ticket → 409; superuser correction allowed — closed the loophole), list/export params `closed_from/to|close_source|follow_up_of` via shared `_common.apply_close_source`, auto-close sweep now also runs on `scope=closed` opens (both panels), export CSV gained Closed-at/Closed-by/Close-source/Resolution-code/CSAT columns, enrichment gained `closed_by_name`/`follow_up_of_number` (batched). Data = the proven one-window pattern (100 rows `listScoped scope=closed status=closed` sorted closed_at desc — merged tombstones KEPT — + sealed stats + 1s `now` tick). Distinct from every sibling — the only **provenance/permanence** desk, and the only one whose guided run harvests knowledge instead of chasing a clock. Build EXIT=0; headless-verified admin+user × dark+light (27/27), zero console errors.

## Support Desk — Archived "Deep Storage" (2026-07-05)
`/{admin/support-desk,user/support}/tickets/archived` — the RECOVERY & RETENTION desk (tombstones: what left circulation, whether it should come back, when the law of the desk lets it be destroyed) — the LAST tab off the generic `SdTicketListSection`. **`sections/SdArchivedSection.vue`** wired via the explicit `{key:'archived', kind:'ticket-list', comp, scope:'archived', agentOnly:true}` registry entry — the FIRST `agentOnly` tab: plain employees never see it (the workspace gained a `revealPending` guard so a deep link to an agent-only tab isn't canonicalized away before the async agent probe lands — `SupportModuleWorkspace.ensureRoute`). Identity = **`--sd-arc-*` bone/parchment record ink + oxidized bronze** (#cbbfa4 + #b08d57; statute-gold #eab308 ⚖ legal hold, ember #f97316 purge danger, emerald restore; ≠ Closed's silver+brass) — **THEME-NATIVE**: `--sd-arc-stage` = sepia-night #0e0c08 on dark / parchment #f1e9d8 on light. **⚑ SIGNATURE = "THE REDACTION WALL"** (user REJECTED gallery v1 — 8 container/library motifs read repetitive — then picked study 07 of gallery v2's eight distinct physical engines: hibernaculum vitals / stopped clocks / star catalogue / river-to-the-falls / locked-groove vinyl / mothball rail yard / redaction docket / silent belfry): `components/SdArchiveInstrument.vue` = DOM/typographic full-bleed hero backdrop (height=0 contract; NO canvas, NO palette observers — rides --sd-arc-* natively, theme-native sepia-night↔parchment) — a censor's docket where every tombstone is a mono line of record text struck by redaction bars (AGE → % struck, deterministic per-id seeded bar layout so renders are stable), reason = stamp glyph + bar tint, ⚖ = gold bars + SEALED tag, destroy-eligible = ember near-total strike, a bronze quill-rule sweeps line to line, an ambient RE-READ ceremony peels one line's bars emerald every ~7s, and a REAL restore removes the line via TransitionGroup leave (bars peel + emerald + lift — no ghost bookkeeping). Hover = hold-the-page-to-the-light (bars fade to 0.42). NEVER-EMPTY: dim unlabeled ghost lines. IO+visibilitychange pause; reduced-motion hides the quill + entrance. Docket right-anchored `left: clamp(20px,44%,520px)` so the glass console owns the left air. Hero `SdArchivedHero` = closed-shell contract ("Out of circulation. *Never out of reach.*", CTAs **Start recovery review** [emerald primary + likely-mistake badge] / **N expiring soon** [ember → retention-rail scroll] / **N on legal hold** [gold → lens], IN-DEEP-STORAGE·SHELVED-30D·RESTORED-30D·DORMANCY-P50·PURGE-ELIGIBLE readouts, 8 docked lenses: stacks/today/month/spam+tests/duplicates/likely-mistakes/⚖hold/purge-eligible). NEW surfaces: **`SdArchiveIntelBoard.vue`** (parchment/bronze bento ≠ Closed's frost dials: clickable REASON SPECTRUM [by_reason_code], AGE STRATA bars, CHRONICLE OF STORAGE [12-mo shelved-vs-restored **stepped-area SVG**, ≠ the closed bar chronicle], RETENTION minis, PULLED-BACK-30D + restorer split, TOP ARCHIVERS + System sweep row — every cell refines the list), **`SdRetentionRail.vue`** ("Retention watch": purge-eligible ∪ expiring capsules as horizontal **burn-down fuse plates** w/ traveling spark [≠ AutoCloseRail rings ≠ TippingPoint orbits], live eligibility countdown, ⚖ shield toggle, superuser purge), **LEDGER view `SdArchiveLedger.vue`** (tombstone story cards: reason-tinted spine → status-at-archive pill → reason chip → archiver → live dormancy → purge countdown / HELD plate → emerald Restore), **RECOVERY REVIEW** (teleported guided overlay ranked `openAtArchive*3 + uncoded*2 + priorityWeight + recency≤7d`; per-card Restore / Open console / **Keep archived** [session-reviewed set] / Skip), **`SdRestoreModal`** (single+bulk, consequences strip, optional note; bulk = server `bulkTickets action:'restore'`), **`SdPurgeModal`** (superuser-only, typed-PURGE arming, required audited reason, held/ineligible rows pre-listed as BLOCKED, per-row 409s by ticket number). Deliberately **NO kanban** — nothing drags INTO the archive. SdTicketTable gained catalog cols `statusAtArchive/archiveReason/archivedAt/archivedBy/dormancy/purgeIn(⚖ HELD)/restore` + `restore` emit; SdTicketDrawer gained the deep-storage banner (who/when/why + hold/purge countdown), an ARCHIVED action dock (Restore = the only act) + More-menu swap (Archive→Restore); SdTicketDeleteModal presets became coded `ARCHIVE_REASON_CODES` chips (sends `reason_code`). Composable gained `fetchArchivedStats/purgeTicket/setLegalHold/bulkRestoreTickets/bulkLegalHold` + `ARCHIVE_REASON_CODES`/`archiveReasonOf/Label/Short/Tone` + `PURGE_RETENTION_DAYS=180`/`AUTO_ARCHIVE_DAYS=120`. Backend shipped alongside (probe_archived_desk.py **66/66**): `add_ticket_archive_columns.py` migration (archived_at/archived_by_id/archive_reason_code/legal_hold + activity backfill — 51 legacy tombstones stamped), DELETE stamps provenance + validated `reason_code` (422 on bogus/manual auto_retention), single-writer `_apply_restore` (prior stamps snapshotted on the activity; 409 non-superuser on held rows), `POST /{id}/legal-hold` (agent places / superuser releases 403), superuser `DELETE /{id}/purge` (409s: not archived / held / retention running / merge-master; audit tombstone BEFORE delete; viewers deleted + follow-up/service-request refs detached; ORM cascade removes comments/activities), **`auto_archive_old_closed` retention sweep** (closed +120d → auto_retention tombstone, legal-hold exempt; cron item 16 + lazy on archived/closed loads + inside both stats) with **closed_stats base WIDENED** (`or_(is_deleted==False, auto_retention tombstone)`) so the Closed desk's lifetime counters never drain, team-sealed `GET /me/tickets/archived/stats` (ArchivedStats: volume/by_reason/by_status_at_archive/age cohorts/dormancy percentile_cont/purge+expiring+hold counters/12-mo archived-vs-restored trend/top archivers/restored_by/policy echo), list+export params `archived_from/to|archive_reason_code[uncoded]|legal_hold|purge_eligible` + `archived_at` sort, bulk `restore`/`legal_hold` (raw-fetch branch for tombstones), EVT_TICKET_ARCHIVED/RESTORED dispatches (assignee/prior-archiver only, never requester), raw READ-ONLY tombstone access on GET detail + activities (mutations keep 404). GOTCHA: `is_deleted` was never in TicketResponse — the drawer banner needs it; and agent-only tabs need the `revealPending` ensureRoute guard or employee-portal deep links bounce. Distinct from every sibling — the only **recovery/retention-governance** desk, and the only one whose destructive act is typed-confirmation superuser-gated. Build EXIT=0; headless-verified admin+agent × dark+light incl. the instrument (**61/61**), zero console errors.

## Support Desk — Team Tickets (agent) "SQUAD COMMAND / The Strip Board" (2026-07-06)
Identity: `--sd-team-*` (burnished gold + emerald sync + rose strain; THEME-NATIVE stage: night deck dark / warm linen light). Signature = **THE STRIP BOARD** (gallery pick 04 of 8; artifact offered Rally Bridge / Switchboard / Formation Flight / Strip Board / Regatta / Beehive / Pit Wall / Ensemble — each a DIFFERENT physical engine per the Archived lesson): an ATC flight-progress board where every ticket is a REAL strip (live queue rows keyed by ticket id on ONE absolute layer → transform transitions make reassignments FLY between the HOLDING rack and per-agent bays), rose barber-pole = breached, dashed GHOST strip previews routing to the calmest bay (never mutates), fluorescent sweep + flickering clock. Files: `SdTeamOpsSection.vue` (orchestrator: switcher chips, lens dock, table⇄SdSquadBoard toggle, bulk bar, ⌘K, 30s soft refresh, ?team=&lens= deep links), `SdTeamOpsHero.vue` (full-bleed #instrument slot like SdBreachedHero), `SdTeamInstrument.vue` (SWAPPABLE internals — SdClosedInstrument approach), `SdRosterDeck.vue` (conic load rings + aging micro-bars + shift dot + drag-to-handoff drop targets), `SdFlowBalance.vue` (14d inflow/outflow SVG scrub chart), `SdHandoffModal.vue` (baton relay + reason chips). PANEL SPLIT: SdTeamTicketsSection renders SdTeamOpsSection when panel!=='admin'; admin config surface byte-identical. Backend: sealed `/me/tickets/team-queue{,/stats,/distribute}` + `POST /{id}/handoff` (HANDOFF_REASON_CODES) in tickets_self.py — scope via `_team_ops_scope` (403 on foreign team_id), pause-aware lens math via `_team_ops_conds`, `viewers` collision pips via SdTicketViewer, distribute = lead-only round-robin/load-balanced reusing assignment.py. SdTicketTable gained additive `viewers`/`handoff` columns + `draggable-rows`. Taken metaphor: ATC strip board / strip choreography.

## Support Desk — Team Command (ADMIN) — the fleet oversight desk (2026-07-06)
`/admin/support-desk/tickets/team` (tab renamed **"Team Command"**, key `team` stable). The ADMIN sibling of Squad Command — same `--sd-team-*` family ON PURPOSE (the two panels are siblings; the INSTRUMENT + composition differentiate, not hue). `SdTeamTicketsSection.vue` is now a ~20-line pure panel dispatcher (employee → SdTeamOpsSection untouched; admin → **`sections/SdTeamCommandSection.vue`**). Composition: `SdTeamCommandHero` (toh-contract clone: full-bleed #instrument slot, glass console "Every crew. *One command.*", New-team CTA, TEAMS·AGENTS-ON-DECK·FLEET-MTTR·CSAT readouts, 7 FLEET lenses: fleet/unowned/breached/due-4h/idle/**off-shift**/**no-crew** — lens DIMS fleet cards, never unmounts; zero-teams = onboarding console w/ 1-2-3-4 primer chips), `SdTeamFleetBoard` (cinematic team cards: sd-deal shell + tilt/glare INNER node, spine, SVG **load ring** open-vs-capacity(agents×3), SVG **24h coverage clock** (business-hours arc + now hand + ON/OFF SHIFT), 7d inflow/outflow **spark**, roster avatars, escalated-in chip, unowned/breached/due/idle chips, MTTR/FRT/CSAT/LOAD footer, rose **orphan ribbon** when agent_count=0, ghost "Raise a crew" card), `SdTeamCommandDrill` (identity header + drill lenses [all/unowned/breaching/due_soon/idle/escalated/paused/critical ≡ backend lens vocab] + collision-watch hotspots strip + **roster health rail** (load bars, aging micro-bars, csat) + SdFlowBalance + resolver podium + SdTicketTable ledger server-paged 50 + Route/Rebalance/Edit actions), `SdGuardNotice` (renders the structured-409 contracts: team_has_active_tickets / members_have_open_assignments / team_has_no_assignable_members), `SdTeamRouteModal` (**BUG FIX: admin `assignTicket` POST /tickets/{id}/assign — never the agent `/me/*` variant** the old surface used; roster-first + outside-agents options), `SdTeamRebalanceModal` (confirm → POST /teams/{id}/rebalance → staggered move list). `SdTeamModal` (kept 4-step wizard) gained: live dup name/code inline errors (fleet snapshot + server-400 mapping back to step 1), **member-impact preflight** (removedIds watcher → GET /teams/{id}/member-impact → single batch directive Auto/Queue/Hand-to riding the PATCH as `reassign_strategy`/`reassign_to`; save blocked until chosen; submit-time 409 → GuardNotice + jump to Members), outside-lead "Seat as Lead" chip, lead-removed transient note, in-modal **delete ceremony** (danger sub-dialog + GuardNotice on 409; emits `deleted`), de-blued swatches, CSS-only pane-child stagger + stepper pop + line draw + READY stamp flip + roster-avatar pops. Section owns its OWN SdTicketDrawer (workspace-global drawer's @changed never reaches sections) + `?team=&lens=&ticket=` deep links + 60s soft refresh (vs agent 30s — oversight, not a work queue). Backend (probe_team_command.py **36/36**): NEW `app/utils/support_desk/team_ops.py` (team_ops_conds/team_on_shift extracted from tickets_self — ONE lens-math source for both panels), teams router hardened — dup-name 400, code-dup-on-PATCH 400 (was 500), assignment_method+member_roles validators, member dedupe + active-user validation, **lead granted is_support_agent**, deactivation guard 409, delete guard now NON-TERMINAL (**on_hold blocks** — old loophole) + **escalated-in blocks** + queue/template team_id detach + is_active=False, **member-removal 409 + reassign_strategy auto|unassign|reassign executed atomically** (activity `by:team_member_removed` + EVT_TICKET_ASSIGNED), audit on update/deactivate/reactivate/delete with diffs, EVT_TEAM_MEMBER_ADDED/REMOVED/LEAD_ASSIGNED roster notifications; NEW endpoints all on `/teams/*`: `GET /overview` (~10 fixed queries, StaticPool-safe: per-team conds sums + escalated-in + load spread + MTTR/FRT percentile_cont + CSAT-30d + 7d flow + coverage + `totals` fleet rollup; agent-sealed to own teams), `GET /{id}/stats` + `GET /{id}/tickets` + `POST /{id}/rebalance` = thin DELEGATES to team_queue_stats/list_team_queue/distribute_team_queue (zero drift), `GET /{id}/member-impact` (same helper as the PATCH 409 → warning ≡ guard). ⚑ SIGNATURE = **"THE PNEUMATIC EXCHANGE"** (user picked study 08 of the 8-study gallery: Switchyard / Grid / Loom / Harbor Master / War Table / Balance House / Elevator Bank / Pneumatic Exchange — each a different physical engine per the Archived lesson): `SdTeamCommandInstrument.vue` = Canvas2D full-bleed capsule post (absolute-fill in the #instrument slot) — riveted brass INTAKE MANIFOLD (sheen sweep + feed funnel + pressure dial whose needle = fleet unowned share) routes ticket-capsules down one glass bezier tube per team to hit-tested station ports (open count center, load arc ring open-vs-capacity, code label, hover/active name plate); LIVE physics: ambient traffic weighted by real inflow+unowned, a team.open/unassigned DIFF fires an emerald capsule burst (route/rebalance made visible), breached tube = rose quivering JAM + pressure-ring stack (≤3) with a periodic emerald clear ceremony (inbound capsules stop behind the jam), idle tube fogs with sliding condensation, off-shift = moon + dimmed tube, orphan station = brass CAPPED cork with capsules bobbing in queue above, unassigned = blinking capsule rack beside the port, lens dims non-matching stations (never removes). Theme-native: palette re-read from --sd-team-* + data-theme MutationObserver (glow ×0.4 on cream, plate/ink flip); ResizeObserver DPR fit; IO+visibilitychange pause; reduced prop = static frame (t frozen, no scheduler). Stations arc the right air (console owns the left); pointer hit-testing on the canvas emits hover-team/select-team (fleet board stays the accessible path). Taken metaphor: pneumatic tube post / capsule exchange.

## Support Desk — Templates (AGENT) "THE CASSETTE EXCHANGE" (2026-07-07)
`/user/support/tickets/templates` — the AGENT sibling of the admin Copperplate Studio (panel-split in `SdTemplatesSection.vue`; admin files byte-untouched). Tab now `agentOnly: true` (closed the loophole where plain employees saw the tab but every fetch 403'd into a fake empty state). NEW token family `--sd-utpl-*` ("filament/VU amber" hot amber-white on studio black; light = daylight vellum; `-fav` mixtape-star gold, `-use` develop-green, `-beam` deck glow, `-scan` playback-display shimmer) — deliberately hotter/whiter than the studio's struck copper. Composition: **REWRITTEN `sections/SdTemplatesEmployeeSection.vue`** orchestrator (ONE `listTemplates({status:'all'})` — backend visibility-SEALS it to global ∪ team ∪ own personal — + sealed `/stats` now carrying `my_use_total/my_use_30d/my_top_used/my_recent` + pickers + `getMe()` for owner checks; lenses DERIVED CLIENT-SIDE from the sealed list so counts reconcile: Working set / Starred / My slides / Recently run / Desk picks / My drafts; Ctrl+K + `/` palette reusing `SdCommandPalette` with per-template Use/Run commands), `SdUtplHero` (uph-contract clone, "Pick a tape. *Press play.*", New personal template + Quick apply ⌘K CTAs, MY-PLAYS readouts), `SdUtplLibrary` (search/sort toolbar, shimmer boot, per-lens empties, first-run "Forge my first template"), `SdUtplCard` (outer Motion shell entrance + INNER tilt/spotlight — never same node; per-user **favorite star** optimistic toggle distinct from admin's global `pinned` "desk pick" chip; visibility badge LIBRARY/TEAM/MINE; rail Use/Run/View/Clone-to-mine + owner-only Edit/Retire; click = preview), `SdUtplPreviewDrawer` (teleported booth: PROJECTED⇄RAW substitution toggle vs SAMPLE_CONTEXT, variables legend + unknown-token warns, revisions negative-strip via lazy getTemplate, copy subject/body), `SdUtplWizard` (personal cutting bench on SdModalShell: Identity/Content(variable chips punch in at caret)/Prefills(priority/type/category ONLY — routing/SLA/assignee stay admin-curated)/Checklist/Review beside a live projection pane; PERSONAL banner; save draft|activate), `SdUtplRunModal` (**Zendesk macro**: picker = `listMyTickets({scope:'my', active_only:true})`, Internal note ⇄ Public reply, body rendered CLIENT-SIDE against the picked ticket's real ctx — `templateVariables.js` gained `{{ticket.number}}/{{ticket.subject}}` — editable, adopt-priority/merge-tags/append-checklist switches). Backend (probe_agent_templates.py **62/62** + studio regression 40/40): `visibility` column (global|team|personal, additive migrate + root script), NEW tables `support_template_favorites` (unique user+template) + `support_template_usage_events` (kind apply|macro, ticket_id) auto-created; templates router — `_scope_filter`/`_template_visible` seal (others' personal = 404, no existence leak), POST/PATCH/DELETE/clone deps → `get_support_agent` with agent branch (create FORCES personal + strips `_ADMIN_ONLY_FIELDS` pinned/sort/visibility/default_*; PATCH/DELETE owner-only-personal; **clone-to-mine** forces personal draft; apply gains own-personal-draft test-drive + usage event), NEW `POST /{id}/favorite` toggle (no audit — preference), `/stats` extended in place (my_* defaulted → admin hero byte-compatible); NEW macro endpoint `POST /me/tickets/{ticket_id}/run-template/{tpl_id}` in tickets_self.py (`_load_visible(require_work)` + `_require_self_actor`; archived-ticket 409; comment mirrors reply_my_ticket incl. `first_responded_at` stamp + EVT_TICKET_REPLIED on public reply; apply_priority/merge_tags REASSIGN JSONB; usage_count atomic + event kind='macro'; activity `template_run` + audit `macro_applied`; body arrives RENDERED — substitution stays frontend-only). GOTCHAS hit: literal `{{…}}` inside a text interpolation breaks the Vue tokenizer (write "moustache variables" instead); inline `if` statements are invalid in template handlers; `@keydown.,` comma modifier is unreliable → explicit e.key check; studio probe cleanup now must delete usage-event/favorite FK rows before templates. ⚑ SIGNATURE = **"THE CASSETTE EXCHANGE"** (user first picked 05 Holo-Lantern, then SWAPPED to 06 same-day — the swap touched `SdUtplInstrument.vue` internals + a copy re-voice pass only, proving the swappable-instrument contract; gallery: Pianola / Phrase Loom / Duplicant Pantograph / Signal Patchbay / Holo-Lantern / Cassette Exchange / Stencilworks / Keysmith Ring): `SdUtplInstrument.vue` = pure CSS tape wall — rack of accent-tinted cassette SPINES with a robotic picker CARRIAGE that rides a rail to the selected spine (0.55s spring), the tape SEATS into the response deck (drop-in Transition, translateY+rotate), two dashed reels spin at mismatched speeds (2s/3.1s) with a glint feeding along the tape band, a scanline playback DISPLAY "develops" the subject + meta chips (clip-path + brightness flash) on a 4.6s metronome, a 16-bar text-WAVEFORM equalizes in the template's accent, and a mechanical tape counter holds my 30-day plays; drafts = unlabeled dashed spines, favorites = mixtape star on the spine; click spine = seat, click display = open (emit pick); IO+visibilitychange split refs (offscreen/hidden) pause the metronome; `reduced` parks a lit still frame. Desk vocabulary is tape-voiced everywhere (tapes/plays/record/shelve/dubs/mixtape). Taken metaphor: cassette wall / tape deck exchange.

## Support Desk — ADMIN Tickets Dashboard "THE CONCOURSE" (2026-07-08)
`/admin/support-desk/tickets/dashboard` — the admin arm of the `SdTicketDashboard.vue` dispatcher, REPLACING the crimson Ops Command Deck (**`SdOpsDashboard.vue` + `SdCommandGlobe.vue` DELETED**; `--ops-*` tokens now orphaned in the theme). User picked 2 of an 8-concept gallery (Switchboard / **Concourse** / Pit Wall / Master Control / Forecast Bureau / Loom / Transit Authority / Expo Line — all warm-family, zero repeats of the 21-engine ledger, space themes banned). NEW token family `--intel-*` ("signage amber" #f2b13d; matte + mechanical vs the Terminal's phosphor GLOW; `--intel-board`/`--intel-flap*` stay a black machine in BOTH themes like `--pulse-stage`). Root `.sd-intel-dash`. ⚑ SIGNATURE = **split-flap Solari departures board**: `SdIntelFlap.vue` (atomic flap char-group — boot cascade on mount, per-cell rotateX flip on value change by bumping a `gen` counter bound into `:key` so the CSS animation re-runs; reduced-motion snaps), `SdIntelBoard.vue` (queue rows QUEUE/LOAD/GATE/SLA/STATUS with BOARDING·CALLING·HOLDING·AWAITING·PARKED·EXPEDITED·FINAL CALL·DELAYED·DEPARTED statuses, blink on hazard, row click deep-links the tab), `SdIntelHero.vue` (drifting skylight shafts, flap station CLOCK ticking 1s, live-presence chip, 7/14/30/90 range switcher, scrolling "tannoy" major-incident marquee → critical). Widgets (all hand-built SVG/CSS, `--intel-*` themed): `SdIntelTrend` (created/resolved lines + breach bars + crosshair readout + FR/resolution SLA meters), `SdIntelTeamBoard` ("Platform status"), `SdIntelLeaderboard` ("Crew rankings", brass #1 plate), `SdIntelHeatmap` (7×24 Mon-first "Timetable"; PG dow 0=Sunday remap; peak outline), `SdIntelChannelMix` ("Entrances" donut, stroke-dasharray sweep), `SdIntelCsat`, `SdIntelAtRisk` ("Final calls" 1s live countdown → FINAL CALL/DELAYED blink; emits open(id)), `SdIntelAging` ("Dwell time"). Section `sections/SdIntelDashboard.vue` keeps the legacy `dashboard` prop for instant paint — the flaps FLIP when the intel payload lands (the mechanic IS the loading state) — then self-fetches + visibility-gated 60s poll. Backend: ONE consolidated `GET /support-desk/dashboard/intel?days=7|14|30|90&tz_offset=±840` (`dashboard.py`, legacy `/` untouched; `_agent_scope` re-applied to EVERY query root INCLUDING the `SdTicketViewer` presence JOIN; grouped aggregates only; merged tombstones excluded; busy-matrix hours bucketed VIEWER-local via `created_at + tz_offset` interval like the sealed calendar). Gotcha: probe JWTs must carry `tv` = users.token_version, and some agents 401 "Account deactivated" at the auth layer despite is_active=true — pick another agent. Taken metaphor: split-flap Solari / departures hall.

## Support Desk — Queues "THE SWITCHYARD" (2026-07-09)
Module `queues` (overview/l1/l2/l3/config, both panels). **Motif CLAIMED: rail classification yard / routing switchboard.** Overview hero = `SdQueueHero` (glass console + MY SIGNAL agent-status lever) over full-bleed canvas `SdSwitchyard` (mainline → switch points = routing rules → one lane per queue, wagon depth stacks, semaphore health lamps, tier bands, packet spawn scaled by inflow; DPR canvas, IO-paused, `--sd-qs-*` tokens re-read on theme flip, lane hit-test → hover tip + @pick). Cards = `SdQueueCard` (tilt+glare via usePointerSpotlight — call at SETUP level, it registers its own onMounted; conic SLA ring, unowned/breach/due gauges, in/out spark). `SdTierFlow` = SVG mini-Sankey (animateMotion pulses; resolve var() colors to hex for SVG gradients). Tier tabs = ONE `SdTierQueueSection` w/ `tier` prop via registry tab `props` (workspace tabProps spreads `t.props` for kind ticket-tool); `SdTierHero` = CSS-only platform stage (converging rails + riding sparks + beacon + giant tier numeral); `SdServeNextDeck` play console (work/skip/escalate/next). Config = "INTERLOCKING TOWER" (`SdQueueConfigSection`, adminOnly): lever-frame nav (rotating lever arms), 7 sections, "Signal Box" dry-run simulator panel. Modals: `SdSkipModal` (reason-gated), `SdTierMoveModal` (both ladder directions, animated L-rung graphic, L3 diagnosis gate). Gotchas hit: motion-v is `AnimatePresence as Presence`; SdPill API is kind/value not label/color; PATCH literal routes before `/{id}`; soft-delete must release unique `code` columns.
**Vertical rail "THE LEVER FRAME" (2026-07-09):** queues module now renders `SdSwitchyardRail.vue` instead of the horizontal `SdWorkspaceTabBar` — registry flag `verticalRail: 'switchyard'` (string variant; `SupportModuleWorkspace.railComp` maps it, `RAIL_ICONS[module.key]` supplies per-tab icons; plain `true` still = Tickets' `SdWorkspaceRail`). Motif (do NOT reuse): signal-box interlocking frame — brushed-metal grain + lantern glow (vs Tickets' aura/blueprint-grid/edge-spine), double-rail track schematic with sleepers + flowing block-current, per-route switch node that THROWS a scaleX branch spur with a traveling headlamp to the active plate, signal lamps (dim → amber hover-flicker → green pulsing when set), tier chips colored by `--sd-qs-t1/2/3`, ticking YARD TIME clock, lever-swing entrance (`syr-throw`, rotate from left origin), semaphore Cinematic lever (arm drops to CLEAR). Same props/emits contract as SdWorkspaceRail.

## Support Desk — L2 desk "THE STORM BUREAU" (2026-07-10, user-picked from an 8-concept artifact gallery)
Registry l2 tab → bespoke `SdL2StormSection` (`props:{tier:2}`); **L1/L3 keep the pit wall — the tier-desk metaphors are now per-tier**. **Motif CLAIMED: meteorological chart room / synoptic weather bureau** (isobars, barometer, fronts, station-model glyphs, barograph). Tokens `--sd-l2-*` (ember core + chart-vellum ink; dark+light blocks; hero stage stays a night chart room in BOTH themes). Signature instrument `SdStormChart` = canvas synoptic chart: pressure field (sum of drifting Gaussians) contoured via MARCHING SQUARES per frame, wind particles advected ALONG contours (perpendicular of the gradient), breach count = storm-cell whorls, escalation = sweeping warm-front ribbon w/ semicircle pips, `pulse` prop gusts particles on serve/refresh, `reduced` → one static frame, colors re-read on `data-theme` flip. Hero `SdStormHero` (barometer SVG w/ sprung needle keyed to stats.health, ACK-pending pulsing chip, duty plate, 7 lens dials). Console `SdStormConsole` = forecaster's desk (rAF stopwatch, live barograph polyline, impact×urgency 3×3 lit-cell readout via `priorityFromMatrix`, quick verbs Log time/Watch/Swarm/Acknowledge + Resolve&Next/Skip/Escalate-L3/Send-back-L1). Cards `SdStormCaseCards` = station reports (barb count = priority, pressure-tendency SLA dial, vellum isoline texture, breach hatch). Wall `SdStormInsightRail` = 10 instruments (ACK clock, active swarms, my logged today, watching…). Modals: `SdWorklogModal` (per-entry ledger + author delete), `SdDiagnosisModal` (L3 handoff dossier — closes the tier-escalate diagnosis 422 with UX; optional RCA + problem link fire BEFORE the single tierEscalate call). Keyboard adds W/L/G. Gotchas hit: **an unimported PascalCase component in a Vue template fails SILENTLY and the build still passes** (SdSelect was dead on the shared tier section — grep tags vs imports after every template); ticket CREATE always routes through the rule engine so a payload `queue_id` never sticks (park via `/assign`); Vue template expressions DO allow `Math` (globals whitelist).

## Support Desk — L3 desk "THE EVIDENCE WALL" (2026-07-10, user-picked concept 03 from an 8-concept artifact gallery)
Registry l3 tab → bespoke `SdL3EvidenceSection` (`props:{tier:3}`); **L1 keeps the pit wall, L2 the storm bureau — all three tier desks now speak different languages**. **Motif CLAIMED: detective cork case-wall / red-string evidence board** (manila cards, push-pins, sagging twine, desk-lamp spotlight, rubber stamps, case-file rack). Tokens `--sd-l3-*` (brass core `#d9a441` + string red `#e0524f` + chalk ivory; card faces stay PAPER-CREAM in both themes; hero `.ewh` stays a night cork wall in both themes via the end-of-file light re-pin block). Signature instrument `SdEvidenceHero` = the live string wall: top-6 serve-order tickets pin up as rotated manila cards, suspect Problem cards sit center, SVG quadratic-sag **red strings draw themselves** from each `linked_problem_id` ticket pin to its problem pin (px-space paths recomputed via ResizeObserver + rows watch; traveling dash pulse rides each string), pointer = desk-lamp radial spotlight, breach = pulsing red card edge; 8 lens chips (adds MAJORS / ACK DUE / FIX IN FLIGHT off the new L3 board stats). Console `SdEvidenceConsole` self-hydrates `GET /tickets/{id}/handoff-dossier` (escalation record + esc-ACK countdown + every lower-tier `[Ln handoff — technical diagnosis]` memo + tier path + linked problem/change snapshots) and carries the Acknowledge stamp-button + P/D/C/W/L/G verbs. Cards `SdEvidenceCards` = evidence polaroids (seeded rotation that straightens on hover, tape corner, pin turns brass when problem-linked, angled SLA BREACHED stamp, SLA drain bar). `SdProblemCaseRack` = the problem workbench: KEDB search (`listProblems({q, known_only})`), expanding case files (grid-rows 0fr→1fr), lifecycle steps, linked-count conic ring, publish-workaround / mark-known-error quick verbs, cascade launcher. `SdEvidenceRail` = 9 plain-language instruments (RCA debt, majors, case archive, fixes in flight, shift summary…). Modals: `SdProblemLinkModal` (link-existing w/ workaround previews OR open-new, also edit mode), `SdRcaModal` (rca_summary/corrective/preventive + breach-reason chips + promote-to-KEDB), `SdCascadeModal` (Zendesk problem→incident cascade via `POST /problems/{pid}/resolve-linked`, two-phase compose→per-ticket verdicts), `SdChangeLinkModal` (link/draft change w/ risk + SdDatePicker + rollback/testing). Backend shipped alongside: `l3_ops.py` (dossier + cascade), KEDB columns migration (`add_support_problem_kedb_columns.py`: workaround/workaround_published/owner_id), `listProblems` q/known_only, tier-board stats += mi_active/missing_rca/problems_open/known_errors/fix_in_progress (mirrored in the hand-built no_queues dict). Keyboard: A doubles as ack-in-desk, B send-back, P/D/C problem/RCA/change. Probe `probe_l3_workbench.py` (28 checks, self-cleaning, never serve-next).

## Support Desk — Create Ticket v3: FULL-WIDTH + "SIGNAL PRISM" woven intelligence (2026-07-10, SUPERSEDES the 3-col copilot layout)
User rejected the 3-panel layout as "cluttered and unorganised" and the copilot as "shitty at predicting". The right-hand `SdIntakeCopilot` column is GONE (component left inert on disk); `.itk-body` is one full-width stage (`grid-template-columns: minmax(0,1fr)`, steps centered at `max-width:1060px`) and the intelligence is WOVEN INTO the steps via NEW `SdSignalRead.vue` with three faces: `variant="live"` inside Describe (prism header SVG — beam→prism→4 spectral bands whose length = live confidence; signal-strength conic ring; 4 prediction cells with per-cell conic confidence rings + Apply/✓; "Apply all · N"; coaching tips → emits focus-desc; entity chips; duplicate + KB lanes), `variant="banner"` inside Classify (recommendation chips: category›sub + impact×urgency⇒P with confidence), `variant="digest"` inside Review (agreement grid + thin-signal warning + dup count). NEW pure-JS engine `intakeEngine.js` (`analyseIntake()` = THE LLM-swap seam): scores all 9 types w/ softmax-ish confidence + evidence strings; reads IMPACT (blast radius incl. "N users" parsing) and URGENCY (time pressure) SEPARATELY then derives priority through the SAME `IMPACT_URGENCY_MATRIX` the form uses (never guesses priority directly — the v1 mistake); scores EVERY category node (parents AND children — a child hit nominates its parent, so subcategory prediction is free) via name tokens + a ~60-key synonym atlas; entities (error codes/apps/devices/affected counts/timeframe); description-quality coaching (7 weighted checks → % + tips); hybrid dup matching (stem-Jaccard + bigram-Dice over subject+desc). Stepper v2 (`SdIntakeStepper`): travelling COMET on the charged rail, faceted tiles w/ per-node conic completion arc, self-drawing SVG done-tick + pop, orbiting dash ring + breathing halo on active (pseudo-elements only — zero layout shift), sliding label underline. Core-level form animation: step transitions gain y/scale/blur, per-step field CASCADE (nth-child stagger, replays via Presence mode="wait" remount), `.f-in` focus lift + label ignition + required-star scale, step-head hairline sweep + number-tile pop. Apply-suggestion now carries impact+urgency WITH priority (matrix lights up as if hand-plotted) and subcategory WITH category. Smoke-tested: "laptop not turning on"→incident/Hardware›Laptop-Desktop/low-impact; "whole infra down… immediately!!"→incident 87%/critical×critical⇒P1; "forgot my password, no rush"→service_request/low×low/Account›Password-Reset; "vpn keeps disconnecting every time"→problem/Network›VPN. Build EXIT=0; dark+light tokens; reduced-motion guarded (`html:not([data-cinematic="on"])`).

## Support Desk — Queue Config "THE DECISION GRAPH" (2026-07-11, user-picked from a 6-concept ULTRA-MODERN gallery after REJECTING an 8-concept vintage-machinery round — lesson: this user wants contemporary/tech-forward engines, not antique metaphors)
Module queues, `config` tab (adminOnly). **Motif CLAIMED: live routing DAG / glass node-flow canvas** (2026 AI-workflow-builder aesthetic: intake orb → rule nodes → lane terminals, packets riding bezier edges). Tokens `--sd-qc-*` (signal amber #f2b64d over graph-void black; spill/overflow = STEEL, never a cool accent; stage + node faces stay a dark machine in BOTH themes per the --intel-board precedent). Signature instrument `SdDecisionGraph.vue` = DPR canvas: layout(intake/rules≤5/lanes≤6 + "+N more"), edges (chain spine / rule→target / intake→default fallback / dashed capacity-spill that lights red at capacity), ambient packets spawn-scaled to live open counts, rule halos flash + lane terminals glow on packet arrival, lane load bars + `open/cap` readouts, hover tooltip + click hit-test → emit pick (lane→lane editor, rule→rule editor), IO-paused, ResizeObserver, `--sd-qc-*` re-read on data-theme/data-cinematic flips, reduced→single static frame. Hero `SdQueueConfigHero.vue` (glass console "Wire the desk. *Route by design.*", 7 clickable telemetry lenses incl. AT CAPACITY warn + LEDGER, CTAs Lay-a-lane/Route-the-backlog/Dry-run, forwards `probe` to the instrument). Section `SdQueueConfigSection.vue` rebuilt: node-dock nav (port-dot pills over a rail gradient), 8 panels (Lanes = tilt+glare terminal cards w/ capacity ring + drain bar + overflow/SLA chips; Rules = chain rail w/ traveling CSS pulse + Probe Console whose dry-run decision ALSO fires a golden probe packet across the hero graph incl. overflow hop; SLA; Skills; Hours; Escalation; **Ledger** = config-audit timeline w/ entity filters + per-rule revision history modal w/ snapshot `<details>`; Wires). Deep-links `?queue=<id>` + NEW `?panel=<key>`. Config-v2 backend shipped same day: SdQueue.sla_package_id/capacity_limit/overflow_queue_id (`add_support_queue_config_v2_columns.py` — RUN on new DBs), apply_overflow (ONE hop, fail-open) + apply_queue_sla (org>queue>default precedence) in assignment.py, business_hours/matches_keywords/impact-urgency-gte-lte rule conditions, SdRuleRevision versioning + write_audit on rule/skill/setting/SLA mutations, GET /queues/config-ledger + /automation-rules/{id}/revisions, simulate surfaces overflow_from + queue_sla_package. Probe `probe_queue_config_v2.py` (ALL CHECKS PASSED).

| Support Desk — Queue Config · Routing Rules panel | "THE INTERCEPT GAUNTLET" (2026-07-11) — `SdRuleGauntlet.vue`: the first-match rule chain as a **vertical particle-interceptor corridor** on a dark-machine stage (dark in BOTH themes, same contract as the Decision Graph hero; fixed `--rg-*` stage accents because light-theme `--sd-qc-*` remaps would go muddy on the void). Intake orb (breathing core + dashed halo + emit rings) → flowing dashed **energy spine** with seeded falling ticket packets → per-rule **interceptor gates** (3D tilt+glare, order plate + nudge chevrons, live lamp, WHEN/THEN logic chips w/ AND-OR joiners, SEALS-CHAIN sheen seal, ×fires SdCountUp, BYPASSED dashed ghosts) each coupled to the spine and firing a **deflection arm** (flowing dashes + traveling spark scaled to run share) into a **destination port chip** (lane-colored, click → opens that lane's editor) → **category-router sink** (imploding maw rings). Signature moment: the Probe Chamber dry-run **plays back as a staged descent** — a golden probe orb drops gate-to-gate (measured offsets + `top` transition), each live gate runs a scanline sweep then stamps MATCH/NO MATCH, the capturing gate flashes `rg-capture` + surges its arm + pops the port, a sealing gate ends the run, otherwise the orb falls to the sink (FALLTHROUGH stamp only when `fallback_used`). Playback is token-cancelled, reduced-motion collapses to instant verdict reveal. Distinct from the hero's horizontal DAG canvas, the Switchyard lever frame, and every sibling — the only vertical interceptor-corridor motif. |

| Support Desk — Queue Config · SLA clocks panel | "THE CADENCE FIELD" (2026-07-11) — `SdSlaCadenceField.vue`: every SLA policy drawn on ONE **logarithmic time axis** (10m‥30d) as a THEME-AWARE instrument stage (per the one-hero-per-page + no-second-dark-stage lessons — only the hero keeps the void; priority accents are the theme-correct `var(--sd-pri-*)`). Per-policy rows of priority **light-traces** (ack head dot → luminous decay band → resolve ring-node; hollow/solo variants for half-timed cells, UNTIMED ghosts), a **sweeping beam** patrols the plot (background-position sweep of a color-mix bar), gridline ticks + staggered axis entrance, and an **inspection HUD** strip that live-reads whatever trace the pointer crosses. Below: the **precedence relay** (RULE/AGENT PICK → ORG CONTRACT → LANE POLICY → DESK DEFAULT, flowing dashes + traveling sparks — mirrors apply_queue_sla) with stop-the-clock freeze chips, then the **chronometer deck** (lift+glow cards — NOT rotateX/Y, grid tilt shears borders — conic coverage dial, per-priority dual log meters w/ scaleX grow, rotating default beacon, lanes/org-contract chips → lanes panel, “N UNCLOCKED” warn on a gapped default). Section hardening: client-side dup-name guard (backend only guards create, not rename), default policy delete-protected in the modal footer (backend 409), delete arm names lanes+org contracts that fall back. The desk’s only time-axis stage — distinct from the Decision Graph DAG, the Intercept Gauntlet corridor and the Breached time-debt meter. |

| Support Desk — Queue Config · Skills panel | "THE CERTIFICATION GRID" (2026-07-11) — `SdSkillGrid.vue`: the agent×skill roster as a **live clearance matrix / mission-control crew-certification board** on THEME-AWARE glass surfaces (`--cg-*` aliases the `--sd-qc-*`/`--sd-*` tokens; solid `--cg-face` for the sticky axes; cream in light mode) — user explicitly rejected a second dark stage under the hero, so the hero stays the ONLY dark machine on the page. Sticky-both-axes CSS grid: skill totems across the top (conic coverage ring w/ holder count, code chip, lane-demand readout, PAUSED / pulsing NO-HOLDER alarm tags), crew rows down the side (avatar tile + cert-count meter), every cell a **certification diode** — click = certify/revoke LIVE (optimistic PATCH of `agent_ids`, per-skill lock, revert+toast on fail, pop + ring-burst keyframe). A **scan beam** sweeps the board every 9s and certified diodes flare in column order on the same period (reads as the beam exciting them). Crosshair row+column highlight tracks the pointer. Below: the **coverage-intelligence deck** — halt COVERAGE GAP (demanded skill w/ zero active holders, or paused-but-demanded: matcher only reads active skills → lanes fail open), warn BUS FACTOR 1, dim UNWIRED SKILLS / UNCERTIFIED CREW, go FULL COVERAGE; chips deep-link to the skill editor / lane editor. Workflow closes: inline roster editing (no more per-skill modal trips), `is_active` pause switch + fail-open warning in the skill modal, dup-code client guard + NEW backend 409 on PATCH code clash (was an IntegrityError 500). Probe `probe_skill_grid.py` (18 checks, self-cleaning). Distinct from every sibling — the only matrix/diode-board motif in the module. |

| Support Desk — Queue Config · Hours & Holidays panel | "THE DAYWHEEL" (2026-07-11) — `SdOpsDaywheel.vue`: the desk’s only RADIAL instrument — a live 24-hour SVG dial (00:00 at top) with **concentric crew rings** whose operating-window arcs draw themselves in (pathLength/dashoffset), a **real-time NOW hand** sweeping in the *selected crew’s timezone* (Intl.formatToParts, 1s tick, smooth linear transition), night wash 18→06, staggered hour ticks, and a live **hub readout** (crew · ticking local clock · OPEN/OFF SHIFT/DESK DARK lamp · closes-in / opens-at countdown walking up to 7 days). Editing is live: **shift plates** (spine-colored, deal-in, day pills w/ tap-scale, time+tz inputs w/ IANA datalist) reshape the wheel as you type; dirty → dashed border + pulsing Commit + Reset. Holidays = the **ECLIPSE BELT**: flowing orbit line, eclipse discs (next one haloed w/ emit rings, today = full eclipse, past dimmed), countdowns, dup-guarded composer. Truth-mirrors the backend gate (team_on_shift): overnight windows NEVER match (start<=now<end string compare) → dashed hazard arc + blocked save; **empty day set = EVERY day** (gate skips the check) → explicit “= EVERY DAY” chip; desk holiday darkens the whole desk; tz validated via Intl before save. Theme-aware stage, lift-not-tilt cards, reduced-motion collapses to a static dial. |

| Support Desk — Queue Config · Escalation panel | "THE ASCENT PROFILE" (2026-07-11) — `SdAscentProfile.vue`: time-based escalation policies as **burn events plotted on a flight-dynamics trajectory** (x = how long a ticket has been open, log time scale 5m→max threshold; gravity-turn ascent curve, generated in JS so nodes sit exactly ON the path). THEME-AWARE surfaces (hero stays the page's only dark stage). SVG stage: draw-in trajectory (pathLength=100 dashoffset), area fill, dashed ground + T+0 launchpad, per-policy burn nodes (breathing halo + orbiting dash ring + core that FLARES exactly when the capsule passes — CSS delay = arc-length fraction × loop duration, `v-bind()` in CSS for the shared period), a capsule + trail riding the path via **SMIL animateMotion/mpath** (gated off under reduced-motion in JS — SMIL ignores the media query), hover → HTML dossier tooltip positioned by viewBox %, dashed red **SLA-BREACH AUTO-LIFT gate** at path end. SVG gradient stops are literal hexes per theme (two defs sets picked by a data-theme MutationObserver — var() in SVG defs is unreliable). Below: the **burn manifest** (threshold-sorted dossier cards: big T+ block, WHEN/BURN chips, ×fires + last-fired, tilt-glare, **ARM/STANDBY switch** → PATCH is_active with no modal round-trip) and **always-on system cards** documenting the built-ins no policy can disable (SLA-breach auto-lift once-only, per-priority esc-ACK response clock 30m/1h/2h/4h/8h chips, sweep semantics: paused tickets sit out, at-tier tickets coast past). Probe `probe_ascent_profile.py`. Distinct from every sibling — the only time-axis trajectory motif in the module. |

| Support Desk — Queue Config · Ledger panel | "THE CLOUD CHAMBER" (2026-07-11) — `SdLedgerChamber.vue`: the config-audit ledger as a **particle detector**. Each mutation (support.{entity}.{op}) is a DETECTION row: a **seeded collision-burst SVG** (4-6 quadratic tracks curved/spread deterministically from the event id hash, entity-channel colored, dashoffset draw-in; created events emit a ring), op **stamps** LAID/TUNED/STRUCK/RESEQUENCED/REASSIGNED/BACKFILL (go/core/halt/hi/spill/warn tones), actor + clock, Δ changed-field signature chips, and a grid-rows-collapse **detection record** exposing action, entity id, origin IP + user-agent (write_audit captures both into details). This frame’s ten events also ride the **BEAMLINE** — blips spaced by real timestamps (even-spacing fallback under a 1-minute spread), hover-synced to rows both ways, click warps + flashes the row. Entity filters = **detector channels** (color-dot pills). Pagination = the **FILM ADVANCE**: sprocket-holed frame chips, windowed 1 ·· 4 5 6 ·· N strip, NEWER/OLDER, 10 per frame, charging scan sweep while loading; section guards snap-to-last-frame when a filter shrinks the set. Distinct from the asset module’s equalizer Forensic Ledger, HR’s Provenance Deck and every queue-config sibling — the desk’s only detector metaphor. |

| Support Desk — Queue Config · Lanes panel | "THE BLADE RACK" (2026-07-11) — `SdLaneBladeRack.vue`: lanes as a **datacenter rack elevation**, the desk’s only hardware metaphor. Each lane = a live **1U blade**: U-rail slot numbers + screw heads, breathing power lamp (retired = POWERED DOWN with the routing consequence spelled out on the blade), lane-color light pipe, tier plate + MASTER tag (default lane), config chips (crew — warn tint when auto_assign has no crew wired — method / serve order / SLA / required skills / drain), a **12-segment LED load meter** that ignites sequentially to open-vs-capacity (lane-color → amber → red ladder, overload LED blinks, **heat-shimmer** drifts across a full blade), and a **SPILL patch-port** (RJ45 jack) that jumps to the overflow blade — pulsing while spilling, DEAD-flagged when the target lane is powered down (the one-hop fails open), and a dashed NO SPILL PATH notice when capacity has no escape. Blades **rack themselves in** (staggered slide + overshoot settle), hover = drawer-pull translateX, sector tabs ALL/FRONTLINE/L2/L3/OFFLINE FLIP-filter via TransitionGroup. Section supplies display-ready `bladeLanes` (tierMeta, skill names, overflow target liveness); delete stays arm-gated in the existing modal, master blade’s pull disabled. |

| Support Desk — Queue Config · Wires panel | "THE UPLINK ARRAY" (2026-07-11) — `SdUplinkArray.vue`: outbound notifications as a **ground-station transmission console** on theme-aware surfaces. DESK CORE emitter (breathing orb + 3 staggered expanding rings) feeds three transmission lines — AGENT PING (assignment), OWNER ALARM (SLA breach), EXTERNAL UPLINK (webhook) — each a flowing dashed wire with signal pulses riding it and a **physical breaker seated ON the wire** (LIVE/CUT pill; on a cut line the pulses visibly DIE at the open breaker via a scale-out keyframe; the receiver bell RINGS on the same period as its line's pulses so arrivals look causal). Receiver plates carry event chips + RECEIVING/SILENT lamps + tilt-glare. Uplink console: validated endpoint input, "Wire it" save, **TEST TRANSMISSION** (golden comet rides the line ≥1 full pass while the real POST runs, then an animated verdict chip: DELIVERED · HTTP 200 / NO CONTACT) + live payload preview plate. THE REAL WORK — the panel's settings were stored but NEVER READ: new `app/utils/support_desk/wires.py` (15s-cached config, `allows()` per-event gate, `post_webhook()` fire-and-forget daemon thread with 30s (event,ticket) burst dedupe, `send_test()`), gates threaded through all three notify wrappers (tickets.dispatch_safe / tickets_self._dispatch_safe / _common._notify_safe), webhook mirrors CREATED/ASSIGNED/ESCALATED/BREACHED/RESOLVED, `upsert_setting` invalidates the cache, NEW `POST /settings/test-webhook`. Probe `probe_uplink_array.py` (12 checks incl. real delivery to a local HTTP listener; gotcha: in-process probes must import `app.models.hr.asset_lifecycle` — AssetCategory isn't in hr/__init__ and an incomplete mapper graph fails EVERY query). Distinct from every sibling — the only emitter→breaker→receiver signal-flow motif. |

| Support Desk — Queue Config · Route-the-backlog modal | "THE RECOVERY SWEEP" (2026-07-11) — `SdBackfillModal.vue`: the backfill dialog as a **four-phase instrument around ONE persistent chain diagram** (stranded pool w/ emitting orb → RULES/CATEGORY/TEAM-LANE/DEFAULT stage tiles w/ SdCountUp + proportional bars + flowing wire joints → destination that lights green; capacity-spill chip). Phases: **scan** (tracer packets ride the chain, "DRY RUN · NOTHING WRITES"), **plan** (stage counts fill; NEW beyond-cap warning — the old modal silently dropped `remaining`; stranded ledger whose reason chips carry **FIX actions**: "Lay a lane for {team}" → opens the lane modal PRE-FILLED with that team, "Lay a default lane" → pre-arms is_default, "Open routing rules" → jumps panels), **run** (chain runs hot, tracers double-speed, min-1.1s paced like the uplink comet), **done** (self-drawing seal ring+tick, ACTUAL results, "Sweep again · N left" while remaining>0, stranded fixes still actionable). **Arm-to-execute** two-step guards the bulk write. Backend: unrouted rows gained `reason_code` (team_no_lane|no_default|no_match) + `team_id`/`team_name`. Gotchas: a modal component's footer can't use the SECTION's scoped `.qcf-btn` — mirror the styles locally; probe fixtures routed by LIVE first-match rules can't force the team-lane branch (assert mechanics, not branch); soft-deleted lanes still FK-reference teams — SQL-reap `support_queues` rows in probe cleanup. Probe `probe_recovery_sweep.py` (ALL PASSED). |

| Support Desk — Queue Config · rule delete confirm | "THE DECOMMISSION CHAMBER" (2026-07-11) — `SdRuleDecomModal.vue`: the yes/no delete box rebuilt as a PROCESS. (1) **Dossier plate** — live lamp, GATE vs BURN·T+ tag, SEALS-CHAIN flag, ×fires SdCountUp + last-fired. (2) **Impact readout** (staggered motion rows): placements never unwind (go), captured traffic *falls through to the NEXT LIVE gate — named — or the category router* (warn when it has fires; burns get "stops escalating on this clock" copy), history survives + inline jump to revisions. (3) **PARK INSTEAD** — first-class reversible alternative (PATCH is_active=false, splice, stay-on-page). (4) **The verdict** — coded reason chips (obsolete/replaced/misrouting/duplicate/cleanup/other; "Other" REQUIRES the note) sent as `?reason=` on DELETE → stored in the audit row (backend: `delete_rule` gained `reason: Query(None, max_length=300)`). (5) **HOLD-TO-DECOMMISSION** — 1.2s rAF press-and-hold with a scaleX danger sweep + a `--dp`-driven red vignette that deepens as you hold; release cancels; reduced-motion falls back to two-step arm. Fall-through computed in the section (`deleteRuleNext`: next `is_active` rule after target in createRules order, else category router). Probe `probe_decom_chamber.py` (8 checks: reason in audit_logs.details, bare delete legal, park round-trip, revisions survive). |

| Support Desk — Queue Config · rule VERSION HISTORY modal | "THE REWIND DECK" (2026-07-11) — `SdRuleHistoryModal.vue`: the revisions JSON dump rebuilt as a playable timeline. Drawing spine (scaleY grow) with **action-coloured version pucks** (created=go / updated=amber / deleted=halt; hover scale+glow; click expands), HEAD chip pulses "CURRENT CONFIG" (or "FINAL CUT" on decommissioned rules). Per-cut **FIELD DIFFS computed client-side** against the previous snapshot — NAME/KIND/THRESHOLD(T+fmt)/MATCH/ON-MATCH(SEALS↔PASSES)/STATE(LIVE↔PARKED)/SLOT/NOTE as `old →strikethrough→ new` chips, conditions/actions as count deltas or REWORKED. Expand (grid-rows 0fr→1fr) → rendered **config plate**: WHEN/THEN chips resolved to live names via section resolvers passed as FUNCTION PROPS (`resolveCond`/`resolveAct` reuse labelOf/nameOfValue/nameOfActionValue — snapshots store raw ids), raw JSON demoted behind a details toggle. **THE WORKFLOW (loophole closed — history was read-only): RESTORE any older cut** = PATCH with the snapshot's fields (never order_index — chain position kept) → mints a NEW head revision, deck refreshes in place; **decommissioned rules** (deck reachable from the Ledger) get a tomb banner + **RECREATE from any cut** = POST a fresh rule at chain end. Both arm-to-confirm. Zero backend changes — restore/recreate ride the existing PATCH/POST and the versioning hooks record them. Probe `probe_rewind_deck.py` (10 checks: v1→v2, restore mints v3 == v1 config, deck readable post-delete, recreate forges fresh id). |

| Support Desk — Queue Config · New/Edit Skill modal (rail v2) | "THE BADGE + SIGNAL CHECKS + HOW IT WIRES" (2026-07-11) — the SdCfgModal left rail's dead space under the skill preview filled with a three-deck stack (`.svp-*`): (1) **THE BADGE** — the skill as the Certification Grid will wear it: color spine + diode that LIGHTS when the roster gains a holder, live coverage ring (holders / crew %, dashoffset-animated), pop-in holder avatar stack, demand row, rotated dashed PAUSED sash when editing inactive. (2) **SIGNAL CHECKS** — reactive readiness rows (NAME/CODE/COLOUR/ROSTER) whose dashed dots turn solid and DRAW a tick as fields complete; dup-name/dup-code/bad-hex fold into the rows as red states + ONE worst-problem strip (replaces the stacked pvw-notes); empty roster = amber "COVERAGE GAP". (3) **HOW IT WIRES** — SKILL→LANES→CREW micro-flow with flowing dashes + three one-line mechanics facts (ALL-skills gate, fail-open, ledger signing). Form upgrades (`.svf-*`): code auto-UPPERCASE + `Wand2` **suggest-code chip** (initials/prefix, uniqueness-checked, appears only while code empty), maxlengths (120/12/500), roster picker gains avatar chips (amber-gradient when certified), `+ALL (FILTERED)` / `NONE` micro-verbs and a coverage meter bar. Zero backend changes. |

## Support Desk — Queues Overview "THE VITALS BAY" (2026-07-11, user-picked Concept 02 from an 8-concept artifact gallery: Exchange/Vitals/Hydraulics/DispatchGrid/Murmuration/Mixdown/FusionCore/LightTable — lesson reconfirmed: contemporary instruments; user also banned space themes)
Module queues, `overview` tab (BOTH panels), SUPERSEDES the Switchyard skin on this tab only (SdQueueHero/SdSwitchyard/SdQueueCard left inert on disk; the Lever Frame rail + L1/L2/L3/config metaphors unchanged). **Motif CLAIMED: ICU telemetry ward / patient-monitor wall** (EKG rhythm = inflow, SLA = SpO2 ring, breach = code-red alarm, backlog age = time on ward). Tokens `--sd-qv-*` (amber core; monitor SCREENS stay night-clinical phosphor `--sd-qv-screen/screen-ink/screen-dim/grid` in BOTH themes per the --intel-board precedent; bezels re-skin). Components: `SdVitalsHero` (fleet monitor: looping stroke-dash EKG whose tempo = real in/hr, 6-cell vitals grid w/ period deltas, MY SIGNAL lever, tier chips, lens dock incl. reopens stat lens), `SdVitalsMonitor` (per-queue bedside monitor: health-colored EKG + flatline-when-empty, OPEN/UNOWN/BRCH/WAIT cells, aging micro-strip, SpO2 conic ring, load bar vs crew×max_agent_load, AT CAP + drain-ETA + reopen chips, tilt+glare setup-level usePointerSpotlight, code-red halo), `SdVitalsCodeBoard` (breach_horizon w/ LIVE 1s countdowns anchored on due_at; click → owning lane's tier desk), `SdVitalsSlaGases` (response vs resolution twin dials + per-priority bars), `SdVitalsCrewRounds` (utilization.top_agents LED segment meters), `SdVitalsFlowTrend` (fleet flow summed client-side, draw-in, honours flow_interval), `SdVitalsAging` (5-bucket histogram, >3d pulses). Section adds 24H hourly range chip (days=1&flow_interval=hour), 60s visibility-paused poll w/ LIVE lamp, ward-chart ledger gains LOAD + DRAIN cols, drawer gains aging strip + crew-load row.
**Backend (queue_ops.py, ALL ADDITIVE on GET /queues/overview; team seal + sweeps untouched):** per-card `aging{lt_1h,h1_4,h4_24,d1_3,gt_3d}` / `burn_rate_hr` / `drain_eta_mins` / `crew_capacity` / `load_pct` / `reopens_range`; fleet `deltas{inflow,outflow,reopens,sla_resolution,sla_response}` (prev equal window; event-based only — no snapshot store so point-in-time backlog has no prev), `aging`, `sla_split{response,resolution,by_priority}` (response denom = answered + missed-open-breached, excludes still-unanswered-unbreached), `burn`, `utilization{open_capped,crew_capacity,load_pct,top_agents(cap = tightest visible max_agent_load)}`, `breach_horizon` (≤8 unbreached/unpaused earliest due, Python min over response/resolution legs after a coalesce-ordered 200-row fetch), `reopens_range`, `flow_interval` param `^(day|hour)$` (hour honoured only days≤2, ≤48 buckets). Probe `probe_queue_overview_vitals.py` (31 checks: keys, fleet-aging==Σcards, aging covers all open, horizon sorted, hourly honoured/fallback/422, agent seal subset) — needs `sys.stdout.reconfigure(encoding="utf-8")` for the cp1252 console.
